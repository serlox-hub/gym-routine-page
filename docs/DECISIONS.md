# Decisiones e implementaciones

Log **append-only** de lo **no derivable del código**: decisiones, trade-offs y gotchas que un
agente/dev futuro no vería solo leyendo los archivos. **Terso y denso**: cada línea aporta algo;
sin relleno ni repetir lo que ya está en el código o en `CLAUDE.md`. Convención de dónde va cada
tipo de contexto: `CLAUDE.md` → "Contexto para futuros agentes".

Formato por entrada: `## AAAA-MM · Título` y bullets `**Clave:** motivo/gotcha` (1 línea c/u).

---

## 2026-07 · GIFs de ejercicios (issue #6)
- **Hosting:** bucket público `exercise-gifs`; la ruta lleva subcarpeta **`gif/`** obligatoria (sin ella → 404): `.../public/exercise-gifs/gif/<gif_key>_<180|360|720>.gif`.
- **Tamaño por superficie** (egress): 180 listas · 360 sesión · 720 pantalla completa.
- **Native usa `expo-image`**, no el `<Image>` de RN (que no anima GIF en Android) → módulo nativo, requiere rebuild del dev client.
- **Sin GIF en ficha/historial** (decisión de producto): va en sesión y catálogo, no en el modal de progreso.
- **GIF apilado sobre las instrucciones**, no al lado (en tarjeta estrecha el texto se estrechaba).
- **`gif_key` NULL = sin animación**; fallback sin hueco roto.
- **Catálogo: lazy-load en viewport, no tap-to-play** (póster estático descartado: exigiría extraer 266 primeros frames).
- ⚠️ **Cache-Control (limitación de plan free, NO es bug nuestro):** el endpoint público sirve `cache-control: no-cache` aunque el objeto tenga `cache_control: max-age=31536000` (verificado vía `/storage/v1/object/info/...`). Causa: el **Smart CDN** que honra el cacheControl del objeto es **solo Pro+** (doc oficial); en free se sirve `no-cache` siempre. **Probado sin efecto (no repetir):** (1) re-upload vía la API con `cacheControl` — deja la metadata correcta (`/object/info` lo confirma) pero el header servido sigue `no-cache`; (2) `UPDATE storage.objects ... metadata->cacheControl` por SQL — tampoco. La metadata SÍ queda bien (1 año) → **si se pasa a Pro o se pone una CDN propia delante, se honra sin tocar nada más**. Matiz: `no-cache`+etag ⇒ el navegador **revalida (304 sin cuerpo)**, no re-descarga el GIF entero; el coste real es un round-trip por vista (latencia en red lenta), no re-descarga masiva — el "egress se dispara" era exagerado. Regla que sí aplica: todo upload debe pasar `cacheControl:'31536000'` para dejar la metadata lista de cara a Pro/CDN.
- **Caché de GIFs en native (`cachePolicy="memory-disk"`):** en los `<Image>` de expo-image (`ExerciseGif`, `ExerciseGifViewer`) para servir desde memoria en remontajes sin releer disco (ayuda al colapsar/revisitar). **Web NO añade caché propio de GIFs a propósito:** una regla Workbox `CacheFirst` obligaría a cachear respuestas opacas (`<img>` no-cors → `status 0` indistinguible de un 4xx/5xx → un fallo transitorio se queda pegado hasta expirar) y el beneficio es marginal: con el fix de notas de abajo el GIF ya no remonta al abrir/cerrar notas, y el `no-cache` del origen solo cuesta una revalidación (304 sin cuerpo), no una re-descarga. Reconsiderar solo si se pasa a Pro/CDN (donde el header ya se honraría solo).
- **Panel de notas de sesión: ocultar, no desmontar.** El GIF vive dentro de `ExerciseCardNotes`, que colgaba de `{showNotes && ...}` → cerrar/abrir las notas desmontaba y remontaba el GIF y lo re-pedía (skeleton en red lenta). Ahora se monta perezosamente en la 1ª apertura y se **oculta con `display:none`** al cerrar (web y native `WorkoutExerciseCard`), sin desmontar. La lógica vive en el hook compartido `useLazyMountToggle(collapsed)` (`packages/shared/src/hooks`, devuelve `{ open, mounted, toggle }`) — DRY web/native: mantiene el contenido montado al alternar y **olvida la apertura al colapsar** la tarjeta, para no re-montar el GIF oculto —ni re-pedirlo— al reexpandir con las notas cerradas. Nota: colapsar (accordion `!collapsed`) sí desmonta el GIF; al reexpandir y reabrir notas se vuelve a pedir (en native lo amortigua el `cachePolicy` de arriba, en web la revalidación 304 del navegador — aceptado).

## 2026-07 · GIF ausente al iniciar sesión desde rutina (sync de campos exercise)
- **Bug:** al arrancar sesión desde una rutina (`DayCard` → `useStartSession`), la query `SESSION_EXERCISES` se **siembra** con `buildSessionExercisesCache` (evita un fetch) en vez de ejecutar `fetchSessionExercises`. Ese builder reconstruye el objeto `exercise` **a mano** y no copiaba `gif_key`; además el origen (`fetchRoutineDayExercises`) tampoco lo seleccionaba. Con el `staleTime` global de 5 min, la caché sin GIF se da por fresca y **no se refetchea** → sin GIF hasta recargar o hasta que una mutación de la sesión invalide la query.
- **Fix:** añadir `gif_key` al select de `fetchRoutineDayExercises` y copiarlo en `buildSessionExercisesCache` (`gif_key: exercise.gif_key ?? null`). De paso se alineó `is_system` en el builder (misma brecha latente, hoy enmascarada por el fallback de `ExerciseFormModal`) para que replique al 100% la forma de `fetchSessionExercises`.
- ⚠️ **Gotcha durable:** `buildSessionExercisesCache` (siembra) DUPLICA la forma del `exercise` de `fetchSessionExercises`. Al añadir un campo nuevo del ejercicio a la sesión hay que tocar **ambos** (+ el select de origen `fetchRoutineDayExercises`), o el campo faltará solo en sesiones recién iniciadas desde rutina y el `staleTime` lo enmascarará.

## 2026-07 · Búsqueda flexible por subsecuencia (buscador de ejercicios)
- **Qué:** el buscador pasó de `includes()` (substring exacto) a matching por **subsecuencia** (`fuzzyMatchScore` en `lib/textUtils.js`): los chars de la query aparecen en orden, no necesariamente contiguos ("pmr" → "Peso Muerto Rumano"; "press banca" encuentra "Press de banca"). Siempre activa (no se limita por longitud de query); es el ranking, no un filtro, quien evita el ruido.
- **Por qué dos punteros y no regex `.*p.*m.*r.*`:** construir un regex desde input del usuario obliga a escapar metacaracteres (`.` `(` `[`…) y expone a ReDoS. El escaneo es equivalente y devuelve puntuación para ordenar.
- **Ranking por tramos** (mayor = más parecido): exacta total > prefijo > interior contiguo > subsecuencia dispersa. Dentro de cada tramo desempata la **cobertura** (`q.length/nombre.length`, escala 0..100 « la separación de 1000 entre tramos → solo desempata), luego dispersión y posición. Así lo contiguo/compacto/más cubierto sale primero (petición explícita del usuario).
- **Orden solo al buscar:** con query vacía se conserva el orden original de la lista (navegación por grupo muscular); `Array.sort` es estable (ES2019) → empates mantienen orden de entrada.
- **DRY:** `fuzzyMatchScore` es la primitiva; `filterExercises(exercises, {search, muscleGroupId, equipmentTypeId, sourceFilter, getName})` (`lib/arrayUtils.js`) es la **única** lógica de filtrado+ranking del buscador, consumida por `ExerciseSearchList` web **y** native (paridad por construcción). `getName` accessor (default `e=>e.name`) → web/native pasan `getExerciseName` para el nombre traducido.
- **Gotcha:** la query colapsa espacios (`\s+` → ''), así que el orden de tokens importa ("banca press" NO encuentra "Press de banca"); reordenar tokens quedó fuera de alcance.

## 2026-07 · Hispanización de instrucciones (LatAm → España)
- **Qué:** `instructions.es` del seed original (025, catálogo v5) estaba en español latino. Normalizado a **español de España** (catálogo **v6**, migración **050**). **62/269** ejercicios afectados; el bloque `en` no se toca. Dos pasadas: (1) voseo→tuteo, (2) léxico.
- **Voseo — no es quitar acentos:** hay cambios de raíz (`volvé→vuelve`, `apretá→aprieta`, `extendé→extiende`, `sentí→siente`, `repetí→repite`) e irregulares (`mantené→mantén`, `sostené→sostén`), y enclíticos con tilde (`bajala→bájala`, `aprovechala→aprovéchala`, `sentate→siéntate`). Se usó **mapa curado** (no regex genérico) para acertar cada forma.
- **`parate` (7×) → `colócate`:** en España "pararse" = detenerse, no ponerse de pie; además `Parate al centro → Colócate en el centro`.
- **Léxico:** `manija→asa` (20×; el corpus ya usaba "asa" 33× → se unifica, con concordancia `una manija→un asa`, `la manija→el asa`); `hacia adelante→hacia delante` (16×; ya había 48 "hacia delante"); `hacia afuera/adentro→hacia fuera/dentro`; `toma→coge` (asir objeto; "tomar" en España es beber/tomar decisión); `regresa→vuelve`.
- **Se MANTUVO `pantorrilla`** (válido en España y coincide con el nombre del grupo muscular en BD; cambiarlo a "gemelos" tocaría la taxonomía de `muscle_groups`, otro alcance). `agarrar` también se mantiene (estándar en España).
- **Migración 050 hace match por `name_en`** (no `name_es`) porque los name_es de prod pueden variar (ver 049); embebe el `instructions` jsonb completo por ejercicio. Idempotente. Incluye `DO` block que avisa si <62 filas casan por nombre.
- **025 NO se editó** (disciplina append-only de migraciones ya aplicadas); el estado final es 025 (español latino) + 050 (fix). El catálogo v6 sí es la fuente para futuros re-seeds.

## 2026-07 · Todas las series editables en sesión activa (issue #7)
- **Qué:** se elimina el registro secuencial. Antes solo la "serie activa" (primera sin completar) mostraba inputs; el resto era texto (`weight || '—'`). Ahora **todas las filas son inputs editables siempre**; `isActive` pasó a ser **solo pista visual** (fondo `successBg` + borde del check en `success`) y ya no controla editable ni la completabilidad. El check completa **cualquier** fila con datos válidos, no solo la activa (se fusionaron las ramas active/future de `renderCheckIndicator`).
- **Editar serie completada (sin desmarcar):** commit **debounced (600ms)** dentro de `SetRow` (no en cada tecla → evita spam de re-render y de escrituras a DB/AsyncStorage). Reúsa `useUpdateCompletedSet` (nuevo hook), que hace `upsert` full-row + optimistic `updateCompletedSetValues` (merge que preserva `dbId/rir/notes/videoUrl/setType/completedAt`) + cola `pendingSets` offline igual que `useCompleteSet`, pero **sin rest-timer ni haptic** (es edición, no completar). No reusamos `useCompleteSet` justamente por el haptic (sonaría en cada commit al teclear).
- **Editar serie futura:** se cachea en `cachedSetData` (`setCachedSetData`, merge) para sobrevivir a colapsar/navegar (desmonta `SetsList`) y a completar en otro orden. Nada de esto toca la DB (solo store). **Vaciar un campo persiste**: se cachea con `null` (no se descarta la clave), si no el merge del store dejaría el valor viejo y reaparecería al reabrir. Caso distancia: `distanceToMeters('')` colapsa a `0`, así que `buildCachedMeasurementValues` fuerza `distanceMeters=null` cuando el input crudo está vacío (distingue vacío de un "0" tecleado).
- **Init de inputs desde `cachedData ?? setData`** vía `getSetInitialInputValues` (antes solo `setData` → bug latente: una serie descompletada + remontada quedaba con inputs vacíos). Convierte `distanceMeters`→unidad de display (antes mostraba metros crudos en completadas).
- **Flush en unmount:** `useEffect(() => () => commitRef.current(), [])` guarda la edición pendiente antes de perder el estado local (el debounce solo no basta si navegas antes de los 600ms). El commit es idempotente (`setMeasurementValuesChanged`), así que timer+unmount no duplican.
- **Placeholder objetivo de reps:** sin historial, el input de reps muestra `reps` del ejercicio (ej. "8-12") en vez de "—" (`formatRepsPlaceholder`, `repsTarget` prop desde `WorkoutExerciseCard`→`SetsList`→`SetRow`). Solo reps; el peso sigue con "—". No se pasa a CALORIES (usa `RepsOnlyInputs` para kcal).
- **Timer de ejecución** (`ExecutionTimer`): pasó de ocultarse por `!disabled` a un prop `showTimer={!isCompleted}` (los inputs ya nunca están `disabled`).
- **Lógica en `lib/setUtils.js`** (DRY web+native, testeada): `getSetMeasurementValues` (extraída de `buildCompletedSetData`, ahora la reusa), `buildCachedMeasurementValues` (campos vacíos→`null`, nunca NaN, para que borrar persista), `getSetInitialInputValues`, `setMeasurementValuesChanged` (NaN≡ausente), `formatRepsPlaceholder`. La orquestación (estado inputs + prefill + commit debounced + flush) vive en el hook compartido `hooks/useSetInputs.js` (consumido por ambos `SetRow`).
- ⚠️ **Gotcha preexistente (no arreglado aquí):** `completed_sets` **no tiene columnas `level` ni `calories_burned`**; `useCompleteSet`/`useUpdateCompletedSet` no las mandan al `upsert`. Los tipos LEVEL_* y CALORIES viven solo en el store durante la sesión (se pierden al restaurar/en historial). Fuera de alcance de #7 → issue #11.
- **Visual (iteración post-#7):** tres estados de fila distintos → **completada** = relleno neutro (`bgHover`) + **barra izquierda lima sólida** (3px) + número/check en lima + inputs ghost (valor como texto), **sugerida/activa** = relleno neutro (`bgHover`) + número lima + **sus inputs (peso y reps) en caja con borde lima** + círculo vacío lima, **pendiente** = transparente + gris + inputs ghost. Se quitó el atenuado (opacity 0.55) de las pendientes: hacía que "por hacer" pareciera bloqueado; el contraste correcto es **resaltar lo hecho**, no atenuar lo pendiente (patrón Strong/Hevy).
- ⚠️ **NO teñir superficies con el lima de marca (`success` `#BEFF00`):** en alpha sobre el navy oscuro (`successBg` 0.12) vira a **oliva/caqui** (≈`rgb(40,48,27)`) porque el lima no tiene azul (B=0), y ningún nivel de opacidad corrige el matiz. Regla: el lima solo se ve limpio en **sólido** (barra/borde/icono/texto a opacidad plena). Por eso "hecho" se marca con **barra izquierda lima sólida** + check lima, no con wash. (Descartado: un token verde esmeralda `rgba(74,222,128,·)` — limpio pero **fuera de la paleta**, el usuario lo rechazó por no ser de marca.) Todas las filas llevan 3px de borde izq. transparente → sin salto de layout; solo la completada lo pone lima. Ojo: el `Card` ya usa `borderLeftWidth:4px` con el color del grupo muscular (coral); la barra lima de fila va **por dentro**, no choca.
- **Inputs "ghost" (`SetInputs.jsx`, ambas plataformas):** en reposo sin caja (borde **transparente**, no ausente → sin salto de layout al enfocar); la caja (`inputStyle`/`numericInputStyle`) aparece **al enfocar** (`useFocusStyle`, borde neutro) o de forma **persistente en TODOS los inputs de la fila activa** (prop `active`, borde lima; baja desde `SetRow` en `props` a todos los wrappers → cada `NumberInput`/`MMSSInput`; sustituye al viejo `weightActive`/`style`/`extraStyle`). Con 4 series × 2 campos, mostrar 8 cajas a la vez se veía "de formulario"; ghost deja la tarjeta calmada y el valor legible. Siguen 100% editables (tocar = enfocar = editar); la fila activa muestra sus cajas siempre para señalar "rellena aquí".

## 2026-07 · PR en sesión: trofeo Y toast derivados de un único recálculo (fix consecuencia de #7)
- **Bug:** al editar una serie ya completada (posible desde #7), el trofeo de PR quedaba congelado: subir el peso no lo encendía, bajarlo no lo apagaba. Causa raíz en `useSessionPRDetection`: `runningBestsRef` era un **acumulador monótono append-only** que `checkSetForPR` plegaba una sola vez al completar; no puede revertir la contribución de una serie editada/descompletada, y las ediciones nunca llegaban a él (`useSetInputs`→`useUpdateCompletedSet` no notificaba al sistema de PR).
- **Fix — trofeo Y celebración DERIVADOS de un único recálculo desde cero** en un `useEffect` que corre al cambiar `completedSets`/`sessionExercises` (completar/editar/descompletar). Fuente única = `computeExercisePRSets(sets, preBests, measurementType, weightUnit)` (pura, en `sessionStatsCalculation.js`, testeada), que repliega `evaluateSetForPR` sobre TODAS las series ordenadas por `setNumber`. Así trofeo y toast **nunca se contradicen** y ambos responden igual a editar (no solo a completar).
- **Por qué derivar y no arreglar el acumulador:** recalcular desde el store es la única forma correcta con ediciones/descompletados; es paridad web+native **por construcción** (todo en el hook compartido); elimina un bug latente (descompletar un PR dejaba el running best inflado); y borra la vía imperativa `checkSetForPR` (ya no se llama desde los layouts).
- **Toast al EDITAR hacia arriba SÍ dispara** (un PR es un PR aunque el número entre por edición; lo contrario —trofeo silencioso— parecía glitch). Sin spam gracias a: (a) **dedup grow-only** `notifiedKeysRef` → cada serie celebra una vez por sesión; (b) el commit de edición ya viene **debounced 600ms** (no por tecla); (c) **baseline**: el primer pase completo siembra `notifiedKeysRef` en silencio → una sesión restaurada no reproduce PRs viejos.
- **`weightUnit` solo se resuelve para la serie que se celebra** (no en cada recálculo de todas): el trofeo no necesita unidad (no afecta a qué series son PR), solo las etiquetas de los records del toast. Por eso `computeExercisePRSetNumbers` (trofeo) delega en `computeExercisePRSets` sin unidad.
- **Cache de bests con promesa compartida** (`getPreSessionBests`): guarda la **promesa en vuelo** por ejercicio para deduplicar peticiones concurrentes (evita un sentinel `'loading'` que haría abortar a llamadas paralelas). Un **fallo de red NO se cachea** (sentinel `'error'`→ se normaliza a `'none'` sin persistir) → un blip no desactiva la detección de PR del ejercicio el resto de la sesión; se reintenta en el próximo recálculo.
- **PRs son por-gimnasio:** cambiar de gym a mitad de sesión (`setSessionGym`) dispara `resetPRState` (efecto con dep `[gymId]`, declarado **antes** del de derivación para limpiar el cache antes de releerlo) → recalcula trofeos contra el historial del nuevo gym y re-siembra baseline en silencio.
- **`clearSetPR`, `checkSetForPR` y `resetPRState` fuera del API del hook:** el trofeo solo se pinta si `isCompleted` y la derivación lo quita al salir la serie de `completedSets`; la celebración sale del diff del recálculo; `resetPRState` es interno (solo lo usa el efecto de cambio de gym).

## 2026-07 · Barra de progreso de sesión por series y segmentada
- **Contador por series, no por ejercicios:** `ExerciseProgressBar` mostraba "Ejercicio X de Y" pero la barra ya se rellenaba por series → mezcla de unidades confusa. Ahora "Serie X de Y" (`workout:session.setProgress`, sustituye a `exerciseProgress`) con `X = setsCompleted`, `Y = setsTotal`.
- **El total de ejercicios NO va en la barra** (petición del usuario): ya se muestra en otro punto de la sesión; duplicarlo aquí sobrecargaba la cabecera.
- **Barra segmentada (un tramo por ejercicio) en vez de relleno único:** cada ejercicio es un segmento con **ancho ∝ su nº de series** (`flexGrow: setsTotal, flexBasis: 0`) + gap 2px de separador. Da granularidad por ejercicio de un vistazo (cuántas series lleva en el actual vs total) sin perder la lectura global: como los anchos son proporcionales a las series, el relleno visible sigue siendo ≈ `setsCompleted/setsTotal`. Con 1 ejercicio se ve como barra continua; con muchos, tramos finos pero legibles.
- **`fillPct` se calcula en `calculateExerciseLevelProgress` (lib), no en el componente:** cada segmento sale con su `fillPct` listo para pintar → geometría en un único sitio testeado, componentes web/native tontos e idénticos (evita divergencia de la fórmula entre plataformas).
- **Calentamiento excluido de los segmentos** (misma regla que los agregados de progreso).

## 2026-07 · Completar serie en un toque + RIR inline (issue #8)
- **Qué:** el check ya **no abre modal**; completa en un toque (registra serie + inicia descanso). El RIR/RPE pasa a **chip inline** en la fila (nuevo `EffortPicker`, web+native) y notas/vídeo/tipo-de-serie a una **hoja de detalles bajo demanda** (el ya existente `SetDetailsModal`, recortado). Patrón Strong/Hevy. Antes las 3 prefs (`show_rir_input`/`show_set_notes`/`show_video_upload`, todas `true` por defecto) forzaban un bottom-sheet en CADA serie → fricción en la acción más frecuente + peso/reps duplicados (inline + modal).
- **Por qué el chip y no el modal, incluso para quien registra RIR siempre:** el modal re-pedía peso/reps ya tecleados y exigía 3 toques + oclusión por un solo dato; el chip inline da el mismo dato en 1 tap (o 0 si no lo usas) sin tapar la lista. El modal solo compensa su coste para notas/vídeo (baja frecuencia) → se difieren.
- **Layout columnar (hoja de cálculo, patrón Strong/Hevy) — decisión final tras iterar.** El modelo de "cluster de badges" a la derecha (dropset/nota/vídeo/RIR/check en fila) no escalaba: en el peor caso reventaba los 132px y los badges bailaban. Se rediseñó a columnas fijas `SET · KG · REPS · RIR · ✓` (grid en web `40px 1fr 1fr 46px 38px` = `GRID_WITH_RIR`, o `GRID_NO_RIR` sin la columna RIR; anchos equivalentes en native, ver `COL_SET/COL_RIR/COL_CHECK`). Esas constantes se **exportan desde `SetRow` y las importa `SetsList`** para su cabecera (fuente única, sin desincronizar).
- **La celda SET es la identidad de la serie + puerta a los detalles:** muestra el **nº**, o **«D»** (naranja) si es dropset —sustituye al número, patrón Hevy—, con un **punto** cuando hay nota o vídeo. Tocarla (en fila **activa o completada**, `canOpenDetails`) abre la hoja de detalles. Absorbe además el estado transitorio de subida de vídeo (%/reintento). Así el número **deja de "sobrar"** (era la duda del usuario): aloja tipo + señal de detalle. Ya NO hay badges «D»/nota/vídeo ni «⋯» en el trailing.
- **Detalles editables ANTES de completar (descubribilidad):** limitar tipo/notas/vídeo a series completadas rompía el flujo natural (en Strong/Hevy marcas el tipo antes de hacer la serie) y escondía la función. Ahora **tipo de serie + notas se pueden fijar en la fila en curso**: `useSetInputs` gestiona `rir/notes/setType` como grupo (estado local + `saveDetails`), cacheándolos si la serie no está completada y aplicándolos en `buildCompletedSetData` al completar (mismo patrón que el RIR y las mediciones). El **vídeo** sí queda post-completado (se adjunta a una serie hecha; evita además la carrera de subida sin fila en BD): la hoja muestra un aviso (`workout:set.videoAfterComplete`) cuando aún no se puede. Quitar vídeo de una completada va por `updateSetVideo(null)` (desacoplado de `updateSetDetails`).
- **Celda SET en texto plano (no pastilla):** se probó enmarcar el número en una pastilla con borde para señalar "tocable", pero enmarcar TODAS las filas metía ruido y —al parecerse a los inputs con borde— el número parecía editable. Se revirtió a **texto plano** (patrón Strong/Hevy): el número manda menos que KG/REPS. Solo destacan el **dropset «D»** (pastilla naranja, estado real) y el **punto de detalle** (`textLight`, nota/vídeo). El área de toque se mantiene con padding transparente (web) + `hitSlop` (native); la descubribilidad de "tocar el nº = opciones" queda al nivel aprendido de Hevy.
- **Affordance del chip RIR:** **solo lleva borde en el vacío de la fila ACTIVA** (prop `active`); en completadas sin RIR queda apagado (`textMuted`, sin borde) para no competir con "rellena esta ahora". El borde en el chip vacío-activo sí se conserva (es el único control que invita en la fila en curso).
- **Columna RIR colapsable:** si el usuario desactiva `show_rir_input`, la columna RIR **y su cabecera desaparecen** (no queda hueco fantasma). El grid es dinámico (`GRID_WITH_RIR`/`GRID_NO_RIR` en web; `View` condicional en native) y `SetsList` lee la misma preferencia para que cabecera y filas coincidan. ⚠️ Si tocas anchos de columna, cambia SetRow **y** SetsList a la vez (ambas plataformas).
- **Touch targets:** los controles de la fila (check, chip RIR, celda SET) son pequeños por el layout columnar. Native usa `hitSlop` para ampliar el área de toque sin agrandar lo visible; web agranda el botón de check (`w-8`) y ensancha su columna. Aun así quedan por debajo de 44px visuales: si se vuelve a apretar el layout, vigilar el par chip-RIR / check (12px de separación).
- **RIR pasa a columna alineada** (no chip flotante): en weight_reps ocupa la columna fija de 46px con cabecera «RIR»; el chip muestra valor **compacto** (RIR `@2`, RPE el **número** 1-5 —la palabra va en el popover) y `–` cuando está vacío (`emptyDash`). En tipos sin reps (sin cabecera) el chip va a ancho natural y muestra la etiqueta «Esfuerzo»/«RIR» en vacío (no `–`, sería ambiguo). Nada se mueve al completar porque cada dato tiene columna fija.
- **PR:** el trofeo sigue sustituyendo al check (mismo hueco), sin columna extra.
- **Concesión aceptada:** en columnar, nota y vídeo comparten una señal (el punto de la celda SET), no dos iconos distintos como en el diseño intermedio. Se eligió la limpieza/alineación de columnas (mercado) sobre distinguirlos de un vistazo; el tipo exacto se ve al abrir la hoja.
- **Estado del RIR en `useSetInputs` (compartido, DRY):** `rir`/`setRir` con **display desde estado local** (feedback inmediato) y persistencia en background. Init `cachedData?.rirActual ?? setData?.rirActual` (`??`, no `||`: 0 y −1=«F» son válidos). Completada → `useUpdateSetDetails` (mutación); **sin `videoUrl`** en esa llamada porque la API (`completedSetsApi.updateSetDetails`) solo reescribe `video_url`/`set_type` si llegan definidos → así se preserva el vídeo y se evita carrera con la subida en background. No completada → `setCachedSetData` (merge, no pisa mediciones; `setMeasurementValuesChanged` solo compara claves de medición, así que un `rirActual` extra en la caché no dispara `updateCompletedSet`).
- **`SetDetailsModal` recortado a tipo/notas/vídeo, siempre modo detalles** (se borró el modo `complete` y las secciones peso/reps —redundante con inline— y esfuerzo —ahora chip). Al guardar re-manda `rirActual: rir` para **preservar el RIR inline** (la API siempre reescribe `rir_actual`+`notes`). Se abre solo desde «⋯» en filas completadas.
- **Store `updateSetDetails` ahora aplica `setType`/`videoUrl` cuando llegan definidos** (antes solo `rirActual`/`notes`): sin esto, marcar dropset o quitar vídeo desde la hoja no se reflejaba en el optimistic store hasta un refetch (con el modelo nuevo el dropset solo se marca post-completado → el bug latente pasaba a notarse). `useUpdateSetDetails.onSuccess` pasa ahora también `videoUrl`.
- **Prefs reinterpretadas, no eliminadas (sin cambio silencioso a usuarios existentes):** ya no gatean un modal bloqueante sino la **visibilidad** de controles: `show_rir_input`→chip, `show_set_notes`→sección notas de la hoja, `show_video_upload` (+`canUploadVideo`)→sección vídeo. Defaults intactos (`DEFAULT_VALUES`). Textos de los toggles actualizados.
- **`getEffortOptions(measurementType)` en `measurementTypes.js`** (fuente única: `RIR_OPTIONS` si usa reps, si no `RPE_OPTIONS`) reusada por el chip. RIR se pinta horizontal (F/0/1/2/3+, corto); RPE vertical (etiquetas de palabra) para que quepa.
- **Popover del chip — presentación por plataforma (paridad de función):** web = popover absoluto anclado al chip; native = `Modal` transparente + `measureInWindow` (escapa el recorte del ScrollView). Ambos **abren hacia el lado con más espacio descontando el header de sesión** (~96px web / ~150px native, safe area incluida) para no quedar tapados por el header sticky cuando la fila está arriba. Descartado bottom-sheet en native: reintroduce la sensación de "modal por serie" que este ticket elimina.
- **Sin tutorial/onboarding** (no existe infra en el repo y un tutorial para la acción central es parche de mala descubribilidad; el rediseño es autoevidente).
- **Fix de paso (native):** `handleModalSubmit` pasaba `videoFile.uri` (string) a `uploadVideoInBackground`, que hacía `uploadVideo(file?.uri)` → `undefined` (subida de vídeo rota en native). Ahora pasa el asset → `uploadVideo(asset.uri)`.

## 2026-07 · Persistir level y calorías en completed_sets (issue #11)
- **Bug (pérdida de datos silenciosa, anterior a #7):** `completed_sets` no tenía columnas `level` ni `calories_burned`. `buildCompletedSetData` ya calculaba `level`/`caloriesBurned` para los tipos cardio (`CALORIES`, `LEVEL_TIME`, `LEVEL_DISTANCE`, `LEVEL_CALORIES`), pero se caían en toda la cadena de guardado/lectura → durante la sesión activa parecía funcionar (store optimista) y al restaurar/en historial/resumen/prefill aparecían vacíos.
- **Migración 052:** `ADD COLUMN IF NOT EXISTS level INT` + `calories_burned INT`, ambas nullable e idempotentes (no rompen filas existentes; los tipos que no las usan las dejan NULL).
- **Cadena completa que había que tocar** (una omisión en cualquier eslabón reabre el bug): `upsertCompletedSet` (insert), los 5 `SELECT` de `completed_sets` (`fetchCompletedSetsForSession`, `fetchSessionDetail`, `fetchExerciseHistorySummary`, `fetchExerciseHistory`, `fetchPreviousWorkout`), `useCompleteSet`/`useUpdateCompletedSet`/`useSyncPendingSets` (destructurar + reenviar en `mutationFn`/`onMutate`/`onError`/cola offline), `buildCompletedSetsMap` (restore → store), `usePreviousWorkout` (mapping → prefill) y `buildWorkoutSummaryFromEndSession` (mapea store→snake_case; también dejaba caer los campos).
- **Naming por capa (gotcha):** el store y `usePreviousWorkout` usan camelCase (`caloriesBurned`); DB/`formatSetValue`/`formatSetValueByType` usan snake_case en unos sitios (`calories_burned`) y camelCase en otros. Al mapear entre capas hay que respetar el naming de destino (p. ej. `buildWorkoutSummaryFromEndSession` produce `calories_burned` porque lo consume `formatSetValue`).
- **NO se añadió a `recalculateSessionStats` (exerciseStatsApi):** su `SELECT` alimenta `calculateSessionExerciseStats`, y `METRIC_MAP` deja `CALORIES`/`LEVEL_CALORIES` vacíos y los `LEVEL_*` solo rastrean time/distance → level/calorías no son métricas de stats. Fetch-earlas ahí sería dead data. Fuera de alcance: **calorías/nivel NO son PR** (no hay `best_calories`/`is_pr_calories` en `exercise_session_stats`); añadir esa dimensión (columnas + flags + tarjetas) sería otro ticket. El display ("resumen", historial) ya los leía; solo faltaba el dato.
- ⚠️ **Pendiente:** aplicar la migración 052 al remoto (`supabase db push`) antes de desplegar; sin las columnas, el `upsert` con `level`/`calories_burned` fallaría.
- **`buildCompletedSetsMap` extraída de `useSession.js` a `lib/workoutTransforms.js`** (con test propio): era una función pura privada del hook y uno de los eslabones donde el dato se perdía (restore DB→store). Ahora testeable sin React y reutiliza `createSetKey` (misma clave `${sessionExerciseId}-${setNumber}` que el store), cerrando la última brecha de regresión de la cadena del bug.
- ⚠️ **`full_reset.sql` está OBSOLETO (~48 migraciones por detrás)** — se marcó como tal (cabecera de aviso) en vez de regenerarlo a mano. Refleja el esquema de ~migración 009 (crea `routine_blocks`, columnas `weight_unit`, nombres en español; no crea `session_exercises`/`gyms`/`exercise_session_stats`/etc.). Regenerarlo a mano replayando 52 migraciones daría un reset "confiadamente incorrecto" (RLS/RPCs/constraints sutiles), peor que uno visiblemente roto. **Forma canónica de resetear/regenerar: `supabase db dump --schema-only`** contra la BD real, no edición manual. Deuda pre-existente, ajena a #11.

## 2026-07 · Sesión anterior inline por fila — columna ANTERIOR (issue #9)
- **Qué:** la referencia a la última sesión pasa de una **tira horizontal scrollable encima** de las series (`PreviousWorkout`, **eliminado**) a una **columna «ANTERIOR» inline por fila** (`SET · ANTERIOR · KG · REPS · [RIR] · ✓`, patrón Strong/Hevy). El «80kg × 12» de la última vez queda pegado al input donde tecleas → cero salto de vista ni mapeo mental "serie N de antes → serie N de ahora".
- **Componente `PreviousSetCell`** (web+native, dumb; **no** en el barrel, igual que `SetsList`): recibe el `previousSet` de esa misma serie (ya se pasaba a `SetRow` para el prefill de `useSetInputs`) y pinta **una sola línea gris** con el valor formateado (reusa `formatPreviousSetValue`→`formatSetValueByType`, sirve para **todos** los `MeasurementType`). Sin lógica nueva en `lib/` (solo reuso de formateadores) → sin tests nuevos.
- **Una línea, no dos (corrección de sobrediseño).** El primer intento apilaba valor + badge de esfuerzo (`@1`) + iconos nota/vídeo en 2 líneas por fila → lista densa y, sobre todo, **RIR duplicado visualmente**: el `@1` (RIR de la última vez) justo al lado de la columna RIR (esfuerzo de la serie actual) se leía como redundancia confusa. El estándar (Strong/Hevy) es **una línea gris con solo el valor**. Se colapsó a eso: valor `textSecondary` peso 500 + un **punto idéntico al de la celda SERIE** (6px, `textLight`) solo si hubo nota/vídeo — mismo significado (esta serie tiene detalle) → mismo indicador. El punto se ancla al **centro de la fila, no al glifo**, para igualar la altura con SERIE (fuente mayor); mecánica exacta en los comentarios inline de `PreviousSetCell`/`SetRow`. El **RIR de la última vez NO se muestra inline** (se quitó a propósito).
- **Rich detail:** nota/vídeo se abren tocando la celda (`SetNotesView`, el mismo que usaba la tira) **solo si hay nota o vídeo** (`interactive = hasNotes || hasVideo`, el punto marca cuáles). Cada celda gestiona su propio `SetNotesView` (Modal devuelve null cerrado → coste nulo). ⚠️ **Concesión:** el esfuerzo/RIR de la última sesión ya NO es accesible desde la fila (matiz del AC del issue sacrificado por limpieza); sigue en el histórico del ejercicio. Si se quiere recuperar, va en el detalle del tap, no inline.
- **La fecha de la última sesión** («Última sesión · hace 3 días» / «Primera vez…») se conserva como **una línea compacta** sobre las series (antes iba en la cabecera de la tira). `SetsList` la deriva del prop `previousWorkout` sin fetch extra: **`undefined`=cargando** (skeleton), **`null`=primera vez**, **objeto**=fecha. Serie sin equivalente (menos series antes / primera vez) → la celda muestra «–».
- **Anchos (optimización móvil, tras medir con render a 360/390px vía `agent-browser`):** las columnas fijas se comían el hueco de KG/REPS en pantallas estrechas. Valores finales `COL_SET=36 · COL_PREV=54 · COL_RIR=42 · COL_CHECK=34` (mínimos legibles: «D» 26px, "70kg × 6", chip 34px, check 26px), **gap 8px** y **padding de fila `px-1`** (antes `px-2`; iguala la cabecera que ya iba `px-1`). PREV exceso raro → elipsis web / `numberOfLines={1}` native. weight_reps usa **columna fija** (alinea con la cabecera); los demás tipos (layout flex, sin cabecera) usan **ancho de contenido** (`flexShrink:0`) para no truncar valores largos (p. ej. ritmo "5km @ 5:00/km"). ⚠️ Igual que en #8: si tocas anchos, cambia `SetRow` **y** `SetsList` en ambas plataformas (`COL_*`/`GRID_*` se exportan de `SetRow`).
- **Sin unidad de peso en ANTERIOR para weight_reps** (`75 × 6`, no `75kg × 6`): la cabecera KG ya indica la unidad → el `kg` era redundante y ocupaba ~11px. Opción `hideWeightUnit` en `formatSetValueByType`/`formatPreviousSetValue` (default false → resto de consumidores intactos), activada por `PreviousSetCell` solo cuando `measurementType === WEIGHT_REPS`. Los demás tipos (flex, **sin cabecera**) conservan la unidad para desambiguar (`5km × 30:00`, `Nv5 × 12:00`). Efecto extra: con `COL_PREV=54` ya no hace falta elipsis ni en valores largos como `82,5 × 12`.
- **`min-w-0` en las celdas de input weight_reps** (`WeightRepsInputs`, `SetInputs.jsx`): causa raíz real del "va justo" en móvil estrecho, más que los píxeles (el `<input>` en grid no encoge por su ancho intrínseco → desborda). Porqué detallado en el comentario inline. Native/`TextInput` no lo sufre (respeta flex).
- **Toque "usar valor anterior" (punto 5 del issue) descartado:** el prefill de `useSetInputs` ya rellena los inputs vacíos con el valor anterior automáticamente → un tap explícito sería redundante y chocaría con el tap-para-detalle.
- **i18n:** sin claves nuevas (`workout:set.previous`/`lastTime`/`lastSession`/`firstTime` ya existían es+en).

## 2026-07 · Esfuerzo (RIR/RPE) de la sesión anterior en la columna ANTERIOR (SUPERSEDE #9: «una sola línea, sin RIR inline»)
- **Qué:** la columna ANTERIOR vuelve a mostrar el esfuerzo de la última vez, ahora como **segunda línea discreta** bajo el valor (`80 × 8` / `@2`), vía `PreviousSetCell` (web+native). **Revierte la decisión de #9** de dejarlo en una sola línea sin RIR (aquellos bullets quedan obsoletos: ya NO es "una sola línea", ya NO "sin lógica nueva en lib/", y la "concesión" de #9 —esfuerzo no accesible desde la fila— deja de aplicar).
- **Por qué se revierte (petición de usuario):** ver solo `peso × reps` dice *qué* hiciste pero no *cómo de duro* fue, que es justo el dato para decidir progresión ("la última vez 80×8 a RIR 2 → hoy subo"). El usuario lo pidió explícitamente y aceptó que ocupe dos líneas si no cabe en una.
- **Por qué ahora SÍ compensa (lo que cambió respecto al rechazo de #9):** el rechazo original fue por "RIR duplicado visualmente / redundancia confusa" cuando el badge quedaba pegado a la columna RIR. En el layout final `SET · ANTERIOR · KG · REPS · [RIR] · ✓` la columna ANTERIOR **no es adyacente** a la columna RIR (median KG y REPS), y la línea es deliberadamente tenue (`textMuted`, fontSize 10) → contexto, no ruido que compita con el esfuerzo actual.
- **Implementación:** helper puro `formatPreviousSetEffort(previousSet, measurementType, showRir)` en `setUtils.js` (con tests), reusa `formatEffortBadge` (misma fuente de formato que el chip actual: `@N` en tipos con reps, etiqueta RPE en el resto). `PreviousSetCell` recibe `showRir` y pinta la segunda línea solo si procede.
- **Gating:** solo si `show_rir_input` está activo (no mostramos RIR histórico si el usuario ocultó la escala de esfuerzo actual) **y** la serie previa registró esfuerzo (`rir != null`, no falsy → preserva el RIR 0). Aplica a todos los tipos con esfuerzo, no solo weight_reps.

## 2026-07 · Unidades de tiempo/distancia: solo s/m (andamiaje intencional, NO es bug)
- **Estado:** el tiempo se almacena y muestra **siempre en segundos**, la distancia **siempre en metros**. NO hay forma de elegir `min`/`km`: sin columna en BD (`user_exercise_overrides` solo tiene `notes`+`weight_unit`; `exercises` no lleva unidad de tiempo/distancia), sin preferencia (`DEFAULT_VALUES` en `usePreferences.js`), sin selector (`EditSessionExerciseModal`), sin `resolveTimeUnit`/`resolveDistanceUnit`. Solo el **peso** (kg/lb) es configurable (`resolveWeightUnit` + override + preferencia global).
- **El andamiaje `timeUnit`/`distanceUnit` es deliberado — NO lo marques como bug.** Los props/params que recorren `SetsList`→`SetRow`→inputs/`PreviousSetCell` y varias funciones de `setUtils.js`, más el check `timeUnit === 'min'`, replican el patrón de `weightUnit` pero **siempre caen a los defaults `'s'`/`'m'`** porque no hay fuente (`WorkoutExerciseCard` solo resuelve `weightUnit`). Una validación pre-commit lo confundió con un "cable suelto" de propagación; es infraestructura dormida, no deuda.
- **Por qué NO se arranca:** borrarlo tocaría funciones núcleo (`buildCompletedSetData`, `getSetMeasurementValues`, `distanceToMeters`, `metersToDistanceUnit`…) usadas en toda la app, con **cero beneficio visible** y perdiendo el punto de enganche de una futura feature min/km.
- **Para añadir min/km en el futuro** (es feature, no fix): columna de unidad en `user_exercise_overrides` (patrón de `weight_unit`) → `resolveTimeUnit`/`resolveDistanceUnit` → resolver en `WorkoutExerciseCard` y propagar → picker en `EditSessionExerciseModal`. El resto de la cadena (conversión, formateo, inputs MM:SS) ya lo soporta.

## 2026-07 · Bests de PR en sesión: prewarm en lote (issue #12)
- **Qué:** al restaurar una sesión con N ejercicios ya completados, `useSessionPRDetection` resolvía los bests previos con `await` **secuencial por ejercicio** (N round-trips en serie antes de pintar los trofeos; retardo perceptible en wifi de gimnasio). Ahora una **única** `fetchExerciseBests(allIds, {gymId})` en lote antes del bucle siembra el cache por-ejercicio.
- **Rename (sustituye la mención en la entrada de #7):** `getPreSessionBests` (fetch-por-id + cache) se partió en **`prewarmPreSessionBests(ids)`** (el único que pide red, en lote) + **`readPreSessionBests(id)`** (lee cache, NO pide). El efecto llama a prewarm una vez y luego el bucle solo lee.
- **Por qué separar lectura de fetch (no dejar que el bucle refetchee):** con el path anterior, si el lote fallaba y borrábamos el slot para reintentar, el `read` del bucle habría lanzado un fetch individual **en el mismo pase** → volvían los N en serie justo en el caso de error. `read` cache-only evita ese doble fetch; el reintento ocurre en el **siguiente** recálculo (ya debounced), igual que antes.
- **Semántica de error/dedup intacta** (dedup de pases concurrentes, sentinel `'error'` no cacheado → reintento, guard de identidad ante `resetPRState` por cambio de gym a mitad de vuelo): igual que el path anterior; el mecanismo está en el comentario de `prewarmPreSessionBests`.
- **Opción B (Promise.all sin batching) descartada:** paraleliza pero mantiene N peticiones/N queries server-side; el array de `fetchExerciseBests` existe justo para pedir en lote (un `.in()` = un index scan). En vivo no aplica (el ejercicio ya está cacheado de un pase previo → `missing=[]`, sin petición extra).

## 2026-07 · Áreas táctiles ≥44px + etiquetas accesibles en la fila de serie (issue #10, puntos 1+2)
- **Suelo de 44px que SUPERSEDE el "minimizar columnas" de #9** para RIR y ✓: `COL_RIR` 42→**44**, `COL_CHECK` 34→**44** (área táctil mínima recomendada; app usada con manos sudadas). ⚠️ Un futuro agente optimizando ancho móvil NO debe bajar estas dos de 44 (choca con a11y); recortar antes `COL_PREV`/gap si hace falta. **Aritmética de ancho** (cadena real de padding: página `p-4` + card `p-4` = 64px + fila `px-1` 8px): a **360px** el track del grid ≈288px → fijas `36+54+44+44`=178 + gaps `8×5`=40 → **~35px por columna KG/REPS**; a **390px** ≈50px. Estrecho pero **sin desbordar** porque los inputs weight_reps son `w-full` + `min-w-0` (ver #9); si 35px resulta corto en uso real, la palanca es bajar `COL_PREV` (54) o el gap, no RIR/✓.
- **Botón grande, icono pequeño (web, sin hitSlop):** el check pasa a `w-11 h-11` (44×44) manteniendo el icono a 26/22px centrado; el `py` de la fila baja `2.5`→`1` para que la fila NO crezca de alto (py-1 + 44px ≈ los 52px previos con py-2.5 + 32px). El chip de esfuerzo y la celda SET usan **wrapper transparente** (chip 44×44; celda SET ancho-auto × ≥44) con el pill/número pequeño dentro (agrandar el fondo lo convertía en un cajón). El ChevronDown de la cabecera colapsada: botón 44×44.
- **Native ya resuelto vía `hitSlop`** (invisible, no crece el layout): check y celda SET ya lo tenían; se añadió al retry y se subió el vertical del chip de esfuerzo (10→13) y del ChevronDown (8→14). Por eso native NO cambia tamaños visuales; solo web.
- **Etiquetas accesibles (`title=` no existe en móvil):** `aria-label` (web) / `accessibilityLabel`+`accessibilityRole="button"` (native) en check (completar/desmarcar), celda SET (más opciones), retry de vídeo, chip de esfuerzo y opciones del popover. El check pendiente y las opciones llevan `accessibilityState` (disabled/selected).
- **Fix del ChevronDown:** su `aria-label` era `common:buttons.next` ("Siguiente") — etiqueta incorrecta para un toggle. Nueva clave `workout:exercise.expand` (es+en); el botón solo aparece colapsado (expandido muestra el menú), así que siempre significa "expandir".
- **Puntos 3, 4, 5 del issue: ya resueltos por #7/#8** (barra de progreso por series coherente con su etiqueta; verde=hecho con barra lima sólida; densidad de la cola disuelta por el grid columnar). No requerían acción. Quedan pendientes 6 (copy del RIR) y 7 (tipos de serie, decisión de producto).

## 2026-07 · RIR autoexplicable en el popover del chip de esfuerzo (issue #10, punto 6)
- **Qué:** el popover del `EffortPicker` (web+native) pasa de **fila de chips crípticos** (`F 0 1 2 3+`, sin explicación) a una **columna** con cabecera (`workout:set.rirTitle` "Reps en reserva" + `rirHelp`) y filas **«código · palabra»** (F · Fallo, 0 · Última rep, 1 · Muy cerca, 2 · Controlado, 3+ · Cómodo), reutilizando las descripciones que YA existían en `data:rir.*` vía `getEffortInfo`. El chip inline de la fila sigue compacto (`@2`); solo cambia el popover. **SUPERSEDE** la nota de #8 ("RIR horizontal / RPE vertical"): ahora **ambos en columna**.
- **Por qué:** los códigos RIR no eran autoexplicables para quien no conoce la notación; las palabras descriptivas ya existían pero solo se veían en `NotesBadge` (historial), no donde se elige. Cierra el punto 6 de #10 (quedaba pendiente).
- **RPE sin cambio de fondo:** para tipos sin reps sigue mostrando la palabra directa (`data:rpe`, ya descriptiva) sin cabecera; solo se unificó a columna. `getEffortInfo` se computa **solo** en RIR (`usesReps ? … : null`).
- **i18n:** repurposada la clave huérfana `workout:set.rirTitle` (antes "@ — Reps en reserva", muerta desde que #8 movió el RIR inline) + nueva `rirHelp` (es+en); **eliminada** la huérfana `workout:set.selected` (sin usos).
- **a11y:** las opciones RIR llevan nombre accesible explícito («código descripción», p. ej. `aria-label`/`accessibilityLabel` "2 Controlado") — sin él, dos `<span>` contiguos en web podían leerse pegados ("2Controlado"). Anchos del popover: 208 (RIR, cabe "código · palabra") / 160 (RPE); `dropUp` igual que #8.
- ⚠️ **Convención i18n en componentes:** usar `useTranslation()` (web **y** native), NO el `t` estático de `@gym/shared` — este último no se re-suscribe a `languageChanged`, así que texto **visible** quedaría en el idioma anterior tras un cambio en vivo. `t` de `@gym/shared` es solo para `lib/`/`api/`/hooks (fuera de render).

## 2026-07 · Tipos de serie: se mantiene {normal, dropset} (issue #10, punto 7 — decisión de producto)
- **Decisión:** NO se amplía `SET_TYPES` (`constants.js`, hoy `normal`/`dropset`). No se añade "failure" ni "warmup" como tipo de serie. Cierra #10.
- **Por qué NO "failure":** el fallo YA se expresa con **RIR = "F"** (valor `-1`, "Fallo") en el selector de esfuerzo (`RIR_OPTIONS`), que además es más rico (escala 0-3+). Un tipo `failure` duplicaría esa intención sin aportar. (Reconsiderar solo si se quisiera un tag de fallo **independiente del RIR** o para tipos de medición sin RIR.)
- **Dos conceptos de "calentamiento" DISTINTOS — no confundirlos:**
  1. **Calentamiento general (pre-rutina)** — YA existe, a nivel **ejercicio/bloque**: `is_warmup` + bloque `BLOCK_NAMES.WARMUP` ("Calentamiento"). Tiene efecto real: los ejercicios warmup **se excluyen de los cálculos de progreso** (`workoutCalculations.js:175,201`).
  2. **Series de aproximación / calentamiento específico (per-serie)** — **NO existe** hoy. Son las rampas de un ejercicio efectivo (p.ej. banca: 40×5, 60×3 antes de las de trabajo) que **no deberían contar como series efectivas**. Es lo que Strong/Hevy llaman "warmup set".
- **Por qué se aplaza (2), no se descarta:** el coste NO es la UI (barata; `set_type` es `TEXT` sin CHECK — migración 023 — así que **añadir un valor no requiere migración**). El coste real es **semántico**: hoy las stats/PR/volumen **ignoran `set_type` por completo** (`sessionStatsCalculation`/`workoutCalculations` no lo miran; el dropset es puramente cosmético). Para que una serie de aproximación "no cuente" habría que hacer esos cálculos **conscientes de `set_type`** (excluir warmup de volumen efectivo, 1RM, PR, best-per-reps…). Ese es el trabajo, y es una decisión de datos con impacto en el histórico. Convive (no choca) con el warmup por-bloque: son niveles distintos (ejercicio entero vs serie suelta).
- **Si algún día se añade el warmup per-serie:** (a) añadir a `SET_TYPES` + labels (`data:setTypes`, `workout:set.type` es+en) + selector en `SetDetailsModal` + badge en `SetRow` (patrón "D" del dropset) — web+native; (b) **lo importante:** propagar `set_type` a `calculateSessionExerciseStats`/`mergeExerciseStats` y a la detección de PR para excluir las de aproximación del volumen y récords efectivos; (c) decidir qué se muestra en historial. Sin (b) sería un badge cosmético que engañaría (contaría como serie efectiva).

## 2026-07 · Progresión automática: doble progresión (issue #13, Fase 2.1)
- **Modelo = doble progresión POR SERIE** (`progressionUtils.js` → `didSetHitTop`). Por qué ese y no otro: es el único que **encaja con el dato existente** (`reps` es un rango libre string tipo "8-12") y **solo necesita la ÚLTIMA sesión** (ya se trae por ejercicio vía `usePreviousWorkout`; la gráfica necesita 2, esto no). Regla: cada serie se compara **1-a-1 con la misma serie** (mismo número) de la última sesión; si esa serie previa llegó a **≥ tope** del rango → sugiere subir el peso de esa serie.
- **Alcance v1: solo `weight_reps`** (único tipo con peso+reps). Otros tipos / rango no parseable ("AMRAP", "8+") / dropsets → sin aviso. `FreeWorkoutSession` no tiene `repsTarget` → sin sugerencia. Preferencia `progression_suggestions` (EAV, default `true`, toggle en Preferencias).
- **Aviso DIRECCIONAL "Sube el peso", SIN cifra (decisión clave con el usuario).** Se descartó prescribir un peso concreto ("Sube a 12,5"): el **salto depende del equipo** (mancuernas que van de 2 en 2, placas, stacks de máquina no lineales, kg/lb) y un número fijo estaría mal en la mayoría → **un número equivocado destruye más confianza que no darlo**. El "cuándo" (lo difícil) lo da el aviso; el "cuánto" lo decide quien conoce su equipo. **Por esto se eliminó toda la maquinaria del incremento configurable** que se llegó a construir (columna `user_exercise_overrides.weight_increment`, migración 053, campo en los paneles, `resolveWeightIncrement`) — era complejidad que no resolvía el problema (el equipo no lineal no cabe en un solo número) y cargaba al usuario con configurar cada ejercicio.
- **Solo aviso, NO toca inputs.** El prefill de "anterior" en `useSetInputs` queda intacto (cero riesgo de registrar un peso no levantado).
- **Sin acción "bajar".** Quedarse dentro o **por debajo** del rango → sin aviso. Por qué NO sugerir bajar aunque una serie quede corta (ej. 7 en 10-12): (a) una serie floja es ruido de un mal día; (b) en series tardías caer del rango es **fatiga normal**, no "demasiado peso" — distinguirlo con una sola serie no se puede; (c) **asimetría**: subir es autolimitado (te haces fuerte y para), bajar por varianza normal puede **espiralar** hacia abajo. El "bajar" bien hecho es intra-sesión/RIR-aware y necesita su propio diseño → follow-up (issue aparte).
- **Display: subfila `ProgressionHint` debajo de la serie** que la ganó (componente web+native gemelo). Una línea compacta **"↗ Sube el peso" + ⓘ**: la acción a la vista, el **porqué a un tap** (ⓘ → modal `workout:progression.why`, interpola reps hechas + rango; regla → evidencia). Se descartaron por más confusas: (a) un ▲/color en la celda ANTERIOR ("no se entendía"); (b) un punto verde tap-to-reveal (esconde la acción + colisiona con el punto gris de notas/vídeo); (c) una flecha dentro del input (promete "rellenar", engaña si solo abre modal). Naranja **sólido** (no wash lima sobre navy).
- **El aviso vive en `SetRow` (no en `SetsList`) y es REACTIVO:** se oculta al **completar la serie** o al **teclear un peso mayor que el anterior** — el nudge se apaga cuando ya lo atendiste. La parte pura está en `shouldSuggestProgression({ previousSet, repsTarget, measurementType, currentWeight })` (= `didSetHitTop` + "el peso de hoy aún no supera al anterior"; vacío/NaN cuenta como "aún no ha subido"). En el componente: `showProgressionHint = progressionEnabled && !isCompleted && shouldSuggestProgression(...)` (los flags de UI se quedan fuera del `lib/`). Necesita el peso en vivo (estado local de `useSetInputs`), por eso el render está en `SetRow`, no en `SetsList` (que no ve lo tecleado). `SetsList` envuelve cada `SetRow` en un `div`/`View` para aislar la subfila del `space-y`/`gap` del contenedor.
- **Iteraciones de UX (por si se reabre):** el issue planteaba un aviso **por ejercicio** ("todas las series al tope"); en la práctica casi nunca saltaba (reps que decaen por fatiga, 15/10/7) y su "Repite" tenue no se leía. Se pasó a **por serie** (salta cuando de verdad procede) y a **direccional** (por el problema del equipo). Follow-ups documentados: "bajar/deload" intra-sesión, progresión `reps_only` (peso corporal → subir reps), RIR-aware, y sugerencia que **rellene** el input de un toque.

## 2026-07 · Onboarding de primer uso → instancia una plantilla (activación / diferenciación)
- **Qué:** usuario nuevo ve en Home un wizard de **3 preguntas** (objetivo / días / nivel) que **recomienda e instancia una de las 4 plantillas existentes** de un toque, la **fija como favorita** y marca el flag. **Gratis, sin gate premium, sin copy-paste** (a diferencia del "Crear con IA", que es premium + pegar-en-ChatGPT). _(Estado actual tras entradas posteriores: 4 preguntas —incluye equipo— y 12 plantillas; ver abajo.)_
- **Por qué:** la app ya competía en profundidad con Strong/Hevy, pero el **primer uso era una pantalla vacía** (0 datos, 0 onboarding) → fuga. El objetivo es bajar el *time-to-first-workout*: de "construir de cero" a "tienes una rutina lista para entrenar".
- **Gate (`useOnboardingGate`):** `shouldShow = !isLoading && !completed && routines==0 && sessions==0`. Se gatea por **rutinas+sesiones** (no solo el flag) para que **usuarios EXISTENTES sin el flag no lo vean**. `isLoading` (las 3 queries) **bloquea la decisión** hasta resolver → evita el parpadeo del wizard sobre un usuario existente antes de que carguen sus rutinas.
- **Fijar como favorita es OBLIGATORIO:** `TodaysWorkout` solo muestra la tarjeta "entrenamiento de hoy" si `is_favorite`. Sin el pin, importar la plantilla dejaría Home mostrando "fija una rutina" en vez de la rutina lista. Por eso tras `importRoutine` → `useSetFavoriteRoutine` (+ invalidate ROUTINES). **Debe hacerse en ambas plataformas** o hay asimetría.
- **Recomendación (`recommendTemplate`, pura + testeada):** el wizard **preselecciona** la recomendada con badge, pero deja **elegir cualquiera** (transparencia). Las reglas concretas quedaron **superadas por la entrada "Plantillas acopladas al catálogo + set ampliado a 8"** (abajo) al ampliar el set — la fuente de verdad es `recommendTemplate.js` (data-driven sobre `goal`/`level`/`daysPerWeek`), no esta enumeración.
- **DRY:** las listas objetivo/nivel se extrajeron a `routineWizardOptions.js` (`GOAL_OPTIONS`/`LEVEL_OPTIONS` + `GOAL_VALUES`/`LEVEL_VALUES`) — antes **duplicadas inline** en los dos `ChatbotView`; ahora las consumen ChatbotView (web+native) **y** el wizard (web+native). Los `value` son las cadenas ES canónicas que ya consume `buildChatbotPrompt` — no traducir.
- **Flag:** `onboarding_completed` en `user_preferences` (EAV genérico → **sin migración**; default `false` en `usePreferences`).
- **Montaje:** overlay `Modal` a nivel de **Home/HomeScreen** (no en routing) → paridad y sin tocar navegación. Se cierra a Home; la rutina fijada ya aparece en "entrenamiento de hoy". `onClose=finish` (Skip) también en native para el back de Android.
- **Siguiente palanca (documentada, no hecha):** convertir "Crear con IA" (hoy copy-paste a un LLM externo + premium) en una **llamada hosted real** (Claude API) de un toque — es la diferenciación de mayor "wow", pero con coste/infra (ver `docs/ai-integration-proposal.md`).

## 2026-07 · Plantillas acopladas al catálogo + set ampliado a 8
- **Problema (bug):** las 4 plantillas usaban nombres genéricos ("Press Banca", "Sentadilla") que **NO existían** en el catálogo del sistema (nombres canónicos cualificados por equipo: "Press de banca con barra", "Sentadilla con barra alta"…). **22/23 no casaban** → al instanciar, `importRoutine` creaba **ejercicios custom huérfanos** (sin GIF, sin instrucciones, sin grupo muscular) — lo contrario de la experiencia que busca el onboarding.
- **Acoplamiento:** todos los `exercise_name` son ahora nombres canónicos exactos del catálogo (`seed/exercise_catalog.json`). El test `routineTemplates.test.js` valida `plantilla ⊆ catálogo` (lee el seed con `fs`+`import.meta.url`, no import JSON, por estar fuera del paquete) → **falla en CI ante cualquier deriva**. Verificado: **55/56** ejercicios tienen `gif_key` en prod (migración 046); único sin GIF: "Sentadilla con peso corporal" (dato pendiente de subir, no problema de acoplamiento).
- **Matching por nombre:** `importRoutine` matchea `name_es` contra los ejercicios del sistema; si coincide, hereda measurement_type/grupo/GIF y NO crea custom. Por eso `data.exercises` se **deriva** de los días con solo `{name_es}` (nada de measurement_type/grupo hardcodeados que serían fuente de deriva) vía el builder `makeTemplate`.
- **Metadata estructurada** (`goal`/`daysPerWeek`/`level`/`equipment`) por plantilla → (a) `recommendTemplate` es **data-driven** (filtra pool por nivel/objetivo y elige la de días más cercanos; añadir plantillas NO toca la función), (b) tags + i18n derivados de ahí.
- **i18n:** nombres/descripciones en `routine:templates.items.<id>.*` (es+en); tags = etiqueta de objetivo (`templates.goalTags.*`) + `common:home.nDays` + `templates.tagBodyweight` si es de peso corporal. Helpers `getTemplateDisplay(template, t)` (picker/onboarding) y `getTemplateImportData(template, t)` (localiza name/description de la rutina AL instanciar, coherente con el picker). Los componentes pasan su `t` de `useTranslation` (no el estático de `@gym/shared`). Fallback estático español en `data.routine.name/description` por si se importa `.data` directo. **Limitación aceptada:** los nombres de DÍA ("Día 1 - Push") siguen en español estático (es contenido editable; localizarlos exigiría muchas claves para poco valor).
- **Set ampliado a 8** (de 4): + `full-body-2` (2d, beginner), + `ppl-3` (3d, intermediate), + `weider-5` (5d, advanced), + `home-bodyweight` (3d, beginner, `equipment:'bodyweight'`). Recomendación (gimnasio): principiante→beginner por días (nunca split 5-6); fuerza→`531`; resto→pool hipertrofia por días (3→ppl-3, 4→upper-lower, 5→weider-5, 6→ppl).
- **Pregunta de equipo (añadida después):** el onboarding pregunta ahora *dónde entrenas* (`EQUIPMENT_OPTIONS`: gimnasio completo / mancuernas / peso corporal). `recommendTemplate` enruta por equipo no-gimnasio → su plantilla de casa; sin señal de equipo, las de casa siguen EXCLUIDAS de la recomendación automática (elegibles en el selector).
- **Set ampliado a 12 (mejora del mapeo, añadida después):** para no colapsar objetivos distintos en las mismas plantillas se añadieron 4 rutinas curadas: `strength-5x5` (fuerza, 3d, beginner — StrongLifts/Starting Strength), `strength-hf` (fuerza, 5d, advanced), `endurance-circuit` (resistencia, 3d, circuito reps altas/descanso corto), `home-dumbbell` (general, 3d, `equipment:'dumbbell'`). Recomendación resultante: **fuerza** → pool por días (novato→5x5; 3→5x5, 4→531, 5+→alta frecuencia); **resistencia** → circuito; **mancuernas** → home-dumbbell. Regla que guió qué añadir: **plantilla nueva solo cuando el *programa* (selección/estructura) cambia de verdad** (fuerza a otra frecuencia, solo-mancuerna, circuito); las variaciones de solo-parámetros (reps/RIR/descanso por objetivo, días exactos) → capa de adaptación, NO más plantillas. Verificado: 40/40 ejercicios nuevos con GIF en prod.
- **Sigue pendiente (issues):** días **exactos** (7→ppl de 6d; fuerza 6-7→hf de 5d) y afinar reps/RIR/descanso por objetivo sobre la plantilla → **capa de adaptación** (#16, con la salvedad de no aplanar la variación de reps ya curada ni romper esquemas especiales); metadata para swaps por equipo → **enriquecer catálogo** (#15). La personalización de verdad excede lo que da seleccionar plantilla sobre 4 respuestas (ver debate).
- **Deriva seed↔prod:** el test valida contra el seed (Mar 16). La migración 046 hizo soft-delete de ~16 nombres **antiguos** ("Sentadilla sin peso", "Círculos de tobillos"…) — ninguno usado por las plantillas (usan los nuevos). Si se re-seedea o se renombran ejercicios en prod, re-verificar el test y los `gif_key` de los ejercicios usados por plantillas.

## 2026-07 · Import JSON endurecido: emparejamiento por clave estable (name_en), esquema v6
- **Problema:** el import de rutinas (JSON) nació antes del catálogo y emparejaba ejercicios por `name_es` (nombre localizado) → frágil: se rompe con otro idioma, tildes perdidas de la IA, mayúsculas o renombres, y creaba **customs huérfanos** ante cualquier desajuste (misma clase de bug que el de las plantillas).
- **Decisión (debatida con el usuario):** clave estable = **`name_en`**, NO un `slug` nuevo. Datos que lo justifican: `name_en` es **único y 100% poblado** en los 269 ejercicios de sistema y **ya es la clave de join** de las migraciones de GIF (046 usa `WHERE name_en=`). Un `slug` sería migración + backfill + disciplina perpetua para ganar poco. Normalización elegida: **minúsculas + trim + espacios + ACENTO-insensible** (la IA y la edición a mano pierden tildes). Alcance: **ambos** — import tolerante **y** export con `name_en` (esquema **v6**).
- **Implementación:**
  - `lib/exerciseMatch.js` (puro, testeado): `normalizeExerciseName` (NFD + quita `\p{Diacritic}` + lower + trim + colapsa espacios); `buildExerciseIndex({systemRows, customRows})` (prioridad de inserción, no se pisan claves: name_en sistema > name_es custom > name_es sistema); `resolveExerciseId(exported, index)` (prioridad de claves del export: name_en → name_es → name).
  - `importRoutine` (`api/routineIOApi.js`): carga **todo** el catálogo de sistema + customs del usuario (una query cada uno, `deleted_at IS NULL`) y construye el índice en memoria — sustituye el frágil `.in('name_es', …)` (se rompía con comas/acentos en nombres) y las 2 queries por ejercicio del bucle de días. Crea custom solo si no hay match. **Retrocompatible v4/v5** (sin `name_en` → casan por `name_es`).
  - **`updateExercises` ahora solo actualiza ejercicios PROPIOS** (set `customIds`) — antes intentaba actualizar también los de sistema (compartidos); RLS lo bloqueaba, pero la intención era errónea.
  - Grupos musculares (solo al crear custom) también se resuelven por `name_en`|`name_es` normalizado.
  - `exportRoutine` incluye `name_en` por ejercicio; `version: ROUTINE_EXPORT_VERSION` (=6). Prompt/doc (`ROUTINE_JSON_FORMAT`) a v6 + campo `name_en` opcional.
- **Referencia día→definición sin cambio:** los días siguen referenciando por `exercise_name` (nombre legible); solo la resolución definición→catálogo usa la clave estable → el fichero sigue autocontenido y legible.
- **Customs entre idiomas:** siguen siendo frágiles por naturaleza (un custom en español no casa con una referencia en inglés) — se acepta.
- **Templates: NO se tocan** — casan por `name_es` exacto (ya validado por su test); el endurecimiento aplica a export/re-import/IA/otras apps, que son los caminos frágiles.
- **Tripwire para un `slug` dedicado (aún no necesario):** renombrar el `name_en` de un ejercicio existente (rompe exports viejos), añadir un 3er idioma, o querer identidad 100% independiente del display. Mitigación intermedia antes del slug: tabla `exercise_aliases` (nombre viejo → id) que el import consulte.

## 2026-07 · Columna «Notas»: entrada única de anotación por serie (RIR + nota + vídeo, autosave)
- **Problema (UX, reportado por el usuario):** anotar una serie repartía 3 acciones en 3 puntos con 3 modelos distintos: check (derecha), chip RIR (columna central, popover), y **comentario escondido tras tocar el NÚMERO de serie** (izquierda) → hoja + botón Guardar. El pulgar cruzaba toda la fila y la entrada al comentario (un número, que es etiqueta, no botón) era **indescubrible**. ~6 toques en 3 sitios para una serie totalmente anotada. En una 2ª pasada el usuario señaló que un diseño intermedio (popover para RIR + filas que abrían la hoja) **mezclaba comportamientos** (unas interacciones cerraban, otras no, otras abrían otra superficie).
- **Qué se conserva de #8 (no se regresa):** completar sigue en **1 toque** (el check no abre nada), el RIR se sigue **viendo** de un vistazo en la columna, y la hoja **nunca** se abre automáticamente al completar (es opt-in).
- **Decisión final (elegida por el usuario): una sola hoja con todo.** La columna «RIR» pasa a llamarse **«Notas»** y su chip es **solo display + disparador** (sin popover): al tocarlo abre **una única hoja** (`SetDetailsModal`) que contiene **todo** — esfuerzo (RIR/RPE), tipo (Normal/Dropset), nota y vídeo. Todo se edita ahí, **nada abre superficies aparte**, y **cerrar = guardado**. La **celda del número es SIEMPRE inerte** (identidad pura; sin fallback).
- **Por qué una hoja y no el popover-con-textarea que sugirió el usuario:** en móvil un textarea en un popover anclado y estrecho lo tapa el teclado; el vídeo necesita superficie (picker/preview/progreso). La única superficie que sirve para texto+media es el bottom-sheet. Coste asumido: editar el RIR pasa a la hoja (adiós al popover de 1 toque de #8), pero es **opt-in** (no se fuerza al completar) → no recrea la fricción de #8; y el valor se sigue viendo en la columna.
- **Modelo de persistencia dentro de la hoja (controlado vs local):** RIR y tipo son **controlados** por el padre (`rir`/`onRirChange=setRir`, `setType`/`onSetTypeChange=setSetType`) → persisten **en vivo** al tocar (patrón #8, caché o mutación según esté completada). Nota y vídeo son **locales** y se confirman **al cerrar** (autosave; nota no escribe por tecla). El usuario no percibe la diferencia: para él todo pasa en la hoja y el cierre confirma. `handleModalSubmit` solo guarda nota (+vídeo), preservando el `setType` vivo.
- **Glifo del chip (decisión del usuario):** prioridad **RIR > nota > vídeo**; una **bolita** marca «hay algo más». Con RIR fijado muestra `@2`/`@F` (+ bolita si además hay nota/vídeo); solo nota → icono de nota; solo vídeo → icono de vídeo; vacío → `–` (o icono de nota tenue si el RIR está off). Así **el RIR se ve de un vistazo por serie** (patrón Strong/Hevy) sin sacrificar la unificación.
- **Presencia de columna = `annotationColumn` (`show_rir_input || show_set_notes || show_video_upload`).** SUPERSEDE #85 (antes dependía SOLO de `show_rir_input`): se colapsa (y recupera ancho) únicamente si se apagan las **tres** prefs. Usa la **PREF** de vídeo (no `canUploadVideo`; el gating fino del vídeo vive en la hoja) para no depender del plan y no desincronizar con `SetsList`. `SetRow` (grid + render de columna) y `SetsList` (cabecera, rotulada `workout:set.notes`) leen la MISMA condición. Ancho sin cambios (`COL_RIR`). ⚠️ Borde aceptado: con las 3 prefs off no hay columna → el dropset (sin interruptor propio) queda inaccesible inline.
- **Autosave:** la hoja no tiene botón Guardar; cerrar confirma nota/vídeo (`handleClose`: si `hasChanges` → `onSubmit`, si no → `onClose`). RIR/tipo ya persistieron en vivo.
- **Terminología UI:** columna y textos usan **«nota(s)»** (no «comentario»), coherente con `workout:set.notes`/`notesPlaceholder`; «comentario» queda para el de la rutina. Cabecera reusa `workout:set.notes`. Concesión asumida: «NOTAS» encima de valores `@2` (la columna es "anotación", el RIR es su contenido por defecto).
- **SUPERSEDE de #8/#10:** el popover del `EffortPicker` **desaparece** (el chip solo dispara la hoja); la hoja recupera esfuerzo + tipo + nota + vídeo en una sola superficie, sin botón Guardar; la celda del número no abre nada.
- **Paridad web/native:** `EffortPicker` reducido a chip (glifo + `onOpenDetails`); `SetDetailsModal` unificado (RIR/tipo controlados vía props, nota/vídeo locales con autosave); `SetRow` pasa `rir/onRirChange/setType/onSetTypeChange/measurementType/showEffortScale` a la hoja y ya no importa `useCanUploadVideo` (lo gestiona la hoja); `useSetInputs` mantiene `setRir`/`setSetType`/`saveDetails` (testeados). Como ya no hay dos `RNModal` (popover+hoja), en native **desaparece el defer de 220ms** (el chip abre la hoja directamente).
- **`SetDetailsModal` es COMPARTIDO por dos consumidores** — la sesión (`SetRow`) y la **edición desde el historial** (`SessionInlineDetail`, web + 2 usos en native). El historial NO usa la hoja completa: la abre con **`showEffortScale={false}` + `showSetType={false}`** (props de gate; por defecto `true`) → allí solo se editan **nota y vídeo**. El RIR en historial se ve con su badge y el tipo se alterna con el número de la fila (peso/reps siguen inline). Decisión del usuario: "mínimo, historial igual que hoy" (no unificar el historial con la sesión) porque son contextos distintos (registrar en vivo vs corregir el pasado).
- **Bug de pérdida de datos corregido (pre-existente de #8, agravado por este cambio):** `SessionInlineDetail.handleDetailsSubmit` destructuraba `rir/setType/weight/reps` del `onSubmit` del modal y los metía en `buildPayload`; como la hoja recortada por #8 ya no los devolvía, al **editar una nota/vídeo de una serie pasada** se borraban `rir_actual`/`weight`/`reps` (y con el rediseño, también `set_type`). Ahora `handleDetailsSubmit({ notes, videoUrl, videoFile })` solo aplica nota/vídeo y **preserva el resto** vía `buildPayload` (no lo pisa con `undefined`). ⚠️ Al cambiar el contrato de un componente compartido, revisar SIEMPRE `SessionInlineDetail` además de `SetRow`.
- **`annotationColumn` extraído a `lib/setUtils.js` → `shouldShowAnnotationColumn(preferences)`** (regla DRY del CLAUDE.md): la condición `RIR || notas || vídeo` vivía duplicada en 4 sitios (`SetRow`/`SetsList` × web/native). Ahora fuente única testeada → cabecera y filas imposibles de desincronizar.

## 2026-07 · Unidad de peso por (ejercicio, gimnasio)
- **Problema (debatido con el usuario):** la misma máquina puede estar en lb en un gym y en kg en otro (remo con placas lb vs discos kg); ocurre **dentro del mismo país** (no es solo cruzar frontera). El override de unidad era **por ejercicio** (agnóstico al gym) → no cubría el mismo `exercise_id` con dos unidades.
- **Por qué NO almacenamiento canónico (`weight_kg`):** stats/PRs/gráficas **ya están segregados por gym** (migración 044: "el peso nominal no es comparable entre gyms"). Con eso, cada serie **(ejercicio, gym) es internamente coherente en una sola unidad** y nunca se compara entre gyms → no hace falta canónico ni reescribir la matemática. Se descartó por redundante (obligaría a reescribir cómputo de stats en BD + convert RPC + backfill para habilitar una agregación cross-gym que se decidió NO hacer).
- **Por qué runtime, NO stamping:** el commit `350c37c` quitó `completed_sets.weight_unit` **a propósito** para resolver la unidad en runtime + convertir en bloque (`convert_user_weights` multiplica todos los pesos de un scope por el factor, sin ambigüedad). Re-stampear la unidad por serie reintroduciría esa complejidad. Se mantiene runtime.
- **Modelo:** tabla nueva `user_exercise_gym_units(user_id, exercise_id, gym_id, weight_unit)` (migración `053`). Separada de `user_exercise_overrides` (que queda **solo con notas**; su `weight_unit` se elimina) porque las notas son por ejercicio, agnósticas al gym → meter `gym_id` en overrides fragmentaría las notas. Backfill: overrides de unidad existentes → `(ejercicio, gym por defecto)`.
- **Resolución (runtime):** `resolveWeightUnit(unidad(ejercicio,gym), prefs)` = `(ejercicio,gym) > preferencia global > 'kg'`. Hook DRY `useResolvedWeightUnit(exerciseId, gymId)`. Gym que se pasa: sesión activa (`workoutStore.gymId`) en la card; `session.gym_id` en el histórico; `gymFilter`/default en el modal.
- **Cambio de unidad:** `useChangeWeightUnit` scope `'exercise'` exige `gymId` → escribe/borra en `user_exercise_gym_units` y `convert_user_weights` filtra `ws.gym_id=p_gym_id` (nuevo param `p_gym_id`). Scope `'global'` (decisión del usuario): convierte **todo lo que hereda la global** en todos los gyms, excluyendo los `(ejercicio, gym)` con unidad explícita. El selector del panel (`SystemExerciseDetailsPanel`) **solo toca el gym activo** (`useSelectedGym`); copy `exercise:weightUnitGymScope` + `messageExercise` lo indican.
- **Overlay multi-gym (decisión del usuario: convertir al vuelo):** solo la **gráfica** del `ExerciseHistoryModal` con "todos los gyms" (chartGymId=null) superpone varios gyms → se convierte en cliente con `convertWeightValue(v,from,to)` (puro) + `unitByGym` (`useExerciseUnitsByGym` + fallback global). El modal **resuelve su unidad de display según el gym filtrado** (antes una prop fija → etiqueta errónea al filtrar a otro gym).
- **Tabla e stats del historial se filtran por gym (decisión del usuario, opción A):** `useExerciseHistory`/`useExerciseHistorySummary` reciben `gymId` (`fetchExerciseHistory`/`Summary` filtran `session.gym_id`). El filtro de gym del modal ahora gobierna **todo** (antes solo la gráfica); en overlay, tabla+stats siguen el gym por defecto. Así nunca se mezclan unidades en la tabla ni en "máx peso/mejor 1RM" (coherente con que los PRs ya son por gym). La gráfica es la única superficie que compara varios gyms a la vez (convertida).
- **Gotcha:** `fetchExerciseAllTimeStats({gymId:null})` agregaría crudo entre gyms (mezcla de unidades), pero **hoy no tiene consumidor** en la UI. Si se usa con `gymId=null`, convertir o forzar filtro de gym.
- **Archivos:** migración `053`; `lib/weightConversion.js` (`convertWeightValue`); `lib/exerciseUtils.js` (`resolveWeightUnit`); `api/exerciseApi.js` (`user_exercise_gym_units`); hooks `useExercises`/`useWeightUnit`/`usePersonalRecords`; UI `SystemExerciseDetailsPanel`, `ExerciseHistoryModal`/`HistoryChart`/`ExerciseProgressChart`, `WorkoutExerciseCard`, `SessionInlineDetail` (web+native).

## 2026-07 · Snapshot de esquema (`schema.sql`) como estado actual canónico (issue #19)
- **Problema:** las 53 migraciones son un ledger append-only (bueno para *aplicar*, malo para *consultar* el estado actual). Saber qué columnas tiene una tabla HOY exigía replicar mentalmente los `DROP`/renames (ej. `routine_blocks` se crea en 001 y se elimina en 031; `weight_unit` lo tocan 11 migraciones). Falta de representación, no de cantidad.
- **Decisión:** un único `apps/web/supabase/schema.sql` (schema-only, commiteado) generado desde las migraciones. Es la fuente de verdad para leer el estado actual; las migraciones siguen siendo el ledger para aplicar. **NO se squashean** las migraciones (destructivo, ya aplicadas en prod, y el problema reaparecería).
- **Drift-proof por construcción:** `npm run db:schema` (`apps/web`) = `supabase db reset` (aplica TODAS las migraciones a la BD local limpia) + `supabase db dump --local --schema public -f supabase/schema.sql`. Como se dumpea una BD reconstruida solo desde migraciones, `schema.sql` == migraciones por construcción, no por disciplina. Requiere Docker corriendo.
- **`db reset` borra la BD local de dev** (es su función); asumido: el seed configurado (`./seed.sql`) ni existe y la BD local es desechable. No toca prod.
- **Al generar el primer snapshot afloró drift real en `CLAUDE.md`:** el diagrama seguía mostrando `routine_blocks` (eliminada en 031). Corregido el diagrama + la deletion strategy: hoy `routine_exercises` cuelga directo de `routine_days` (`routine_day_id`), el calentamiento es `is_warmup` y la superserie `superset_group`. Esto valida el porqué del snapshot.
- **Checklist:** "cuando se modifique el modelo de datos" (`CLAUDE.md`) ahora empieza por regenerar y commitear `schema.sql` junto con la migración.
- **Guard en `/pre-commit`** (`pre-commit-validator`, eje A7): si el diff trae una migración pero NO `schema.sql`, es hallazgo. Heurístico barato (sin Docker): comprueba presencia en el diff, no que el contenido case exacto. Enforcea la convención de arriba en cada commit.
- **Descartado (por ahora):** check en CI que reconstruya la BD y falle si `schema.sql` difiere del dump real (mataría el drift al 100%, pero exige Docker en CI). El guard de pre-commit cubre el 90% del caso (olvido) a coste cero.

## 2026-07 · Referencia "Anterior" segregada por gym + día de rutina (mismo slot) con fallback
- **Problema (planteado por el usuario):** la columna "Anterior" de la sesión (`fetchPreviousWorkout`) solo filtraba por `exercise_id` → la última vez del ejercicio en **cualquier** rutina/día/gym. Dos defectos: (1) cruza gyms, donde la **unidad se resuelve por (ejercicio, gym)** y la máquina cambia → mostraba números en otra unidad/máquina (y contaminaba el prefill y la sugerencia de progresión, que heredan `previousSet`); (2) mezclaba slots con esquemas de reps distintos (mismo ejercicio en Push A 5×5 y Push B 3×12) y el emparejamiento por `setNumber` producía filas sin sentido.
- **Decisión:** referencia = **última sesión completada del ejercicio en ESE gym, priorizando el MISMO día de rutina (mismo slot); si no hay en ese slot, fallback al más reciente en el gym (cualquier día).** El gym es filtro duro (coherente con que stats/PRs/gráficas ya están segregados por gym; nunca comparar pesos crudos entre gyms). El día es preferencia con fallback (no estricto) para no dejar la columna vacía en el primer día en un gym nuevo o si el ejercicio se movió de día.
- **Descartado — filtrar por rutina/día de forma ESTRICTA (sin fallback):** va contra Strong/Hevy (mantienen "Anterior" global por ejercicio a propósito, porque la sobrecarga progresiva quiere el registro más reciente) y sale vacío demasiado a menudo. El fallback conserva la utilidad. También descartado filtrar por `routine_id` (no separa dos días de la misma rutina → no resuelve el problema de esquemas de reps).
- **Emparejamiento de slot por `routine_day_id`, NO por `day_name`** (el usuario señaló que los nombres son editables): renombrar un día es `updateRoutineDay` (UPDATE → `id` estable, nombre cambia); reordenar (`reorder_routine_days`) también mantiene `id`. Así `routine_day_id` es la clave estable ante renombrados; `day_name` (snapshot en `workout_sessions`) NO sirve como clave. La FK `workout_sessions.routine_day_id` es `ON DELETE SET NULL`: al borrar un día las sesiones viejas sobreviven con `routine_day_id = NULL` conservando `day_name`.
- **Señal de fallback en UI (decisión del usuario):** cuando la referencia viene de otro día se muestra el **nombre del día origen** en la cabecera de "Anterior" (`hace 5 días · Push B`); nada cuando coincide con el día actual. El nombre mostrado es el **ACTUAL** del día (join `routine_day:routine_days(name)`), cayendo al snapshot `day_name` solo si el día ya no existe → respeta renombrados posteriores.
- **Implementación:** `fetchPreviousWorkout({ exerciseId, gymId, routineDayId })` lanza 1 query (fallback, cualquier día) + una 2ª (mismo slot) solo si hay `routineDayId`, en paralelo; devuelve `{ sameSlot, fallback }` crudos. La elección + etiqueta viven en `buildPreviousWorkoutRef` (puro, en `lib/workoutTransforms.js`, testeado): elige `sameSlot ?? fallback`, calcula `fromDifferentDay` (por `routine_day_id`) y `sourceDayName`. `usePreviousWorkout(exerciseId, { gymId, routineDayId })` (key ampliada con gym+día; invalidación por prefijo sigue cubriendo). Callers: `WorkoutExerciseCard` (web+native) pasan `gymId`+`routineDayId` del `workoutStore`.
- **Gotcha a vigilar:** el embed es 2 niveles (`session_exercises → workout_sessions!inner → routine_days`) con filtro sobre el nivel intermedio (`session.gym_id`/`session.routine_day_id`). PostgREST lo soporta y cada parte ya se usa por separado en el repo, pero es la única query que combina ambas cosas; si "Anterior" empezara a fallar, mirar aquí primero.

## 2026-07 · Cambio de gym a mitad de sesión: optimista + conversión de pesos (RPC atómico + cola)
- **Problema (planteado por el usuario):** la sesión es **atómica respecto al gym** (`workout_sessions.gym_id` es un único campo; no hay gym por serie). El chip de cabecera permitía cambiar el gym en vivo sin aviso. Como la unidad de peso se resuelve en **runtime por (ejercicio, gym)** y `completed_sets` guarda **peso crudo sin unidad**, cambiar a un gym con otra unidad **reinterpretaba** los números ya introducidos (un `190` metido en lb pasaba a leerse `190 kg`) sin conversión, corrompiendo en silencio las series ya registradas. Es el flujo "me equivoqué de gym al empezar" (corregir), no "me mudé a media sesión" (no existe); la sesión entera pertenece a un gym.
- **Decisión (evolucionó con el usuario):** al mover la sesión a otro gym, si la unidad de algún ejercicio con series de peso ya registradas cambia, se **convierten** esos pesos (preserva el peso real levantado). **Sin modal**: auto-convertir + toast (la conversión es correctiva, no destructiva). Todo **optimista y offline-safe**. Descartadas por el camino: modal de confirmación (fricción innecesaria para algo correctivo) y fail-closed offline (bloquearía el caso común de misma-unidad sin red).
- **El bloqueo real era la DETECCIÓN, no la escritura.** Para saber si/qué convertir hacen falta las unidades `(ejercicio, gym)` del gym destino → una lectura de red que offline falla. Solución: **prefetch de toda `user_exercise_gym_units` del usuario** (`useAllUserExerciseGymUnits`, tabla diminuta, RLS por usuario) al entrar en la sesión → la detección pasa a **local e instantánea**, también offline. Sin esto no hay UI optimista correcta offline (no sabrías la dirección de conversión).
- **Flujo (todo en `useChangeSessionGym.changeGym`, sin red):** detecta en local (prefetch + `planSessionWeightConversion`, puro) → aplica **optimista** en el store (`applyWeightConversions`) + cambia gym + `setSelectedGym` → **encola** `pendingGymChange` para persistir → toast si hubo conversión.
- **Persistencia: RPC atómico `change_session_gym(p_session_id, p_gym_id, p_weights jsonb)`** (migración 054, SECURITY DEFINER, comprueba propiedad de la sesión). Cambia el gym Y aplica los pesos en **una transacción**. El **cliente calcula los pesos** (tiene las unidades cacheadas) y los pasa como `[{session_exercise_id, set_number, weight}]` → cliente y servidor coinciden exactos (sin re-derivar unidades en SQL ni divergencia de redondeo). Se decidió mantener el RPC pese a que la cola idempotente ya converge, para no dejar nunca estado transitorio inconsistente en BD.
- **Cola con reintentos `useSyncPendingGymChange`** (montada a nivel app como `useSyncPendingSets`; wrappers por plataforma inyectan `onConnectivityChange`/`onVisibilityChange`): persiste `pendingGymChange` con el RPC y reintenta en intervalo + al reconectar + al volver a primer plano. `pendingGymChange` se **persiste** (Zustand persist) → sobrevive a cierres offline. Convergente: `changeGym` guarda el **snapshot completo** de pesos actuales (idempotente; correcto aunque haya varios cambios seguidos offline, incluido arrastrar una conversión previa aún sin sincronizar).
- **Conversión por ejercicio, no global:** la dirección la fija cada `(ejercicio, gym)`; en un mismo cambio un ejercicio puede ir kg→lb y otro lb→kg (overrides independientes). Solo se convierten series con `weight != null`.
- ⚠️ **Gotcha clave (no obvio, costó un bug): convertir el store NO basta.** El input de peso de cada serie vive en **estado local de React** en `useSetInputs` (se siembra **una sola vez** al montar con `useState(() => ...)`, no re-lee `setData`). Además `commit` (debounce) y el flush en unmount reescriben ese estado local encima del store. Por eso, tras convertir en el store, la fila seguía mostrando el número viejo (`190 lb → 190 kg` en vez de `86,18 kg`) y el debounce revertía la conversión. Solución: `applyWeightConversions` actualiza `completedSets`+`cachedSetData` y **bumpea `weightConversionNonce`**; `useSetInputs` observa ese nonce y **re-siembra su input local** desde el store ya convertido (tras lo cual local == store, así que el commit debounced no reescribe). Regla durable: cualquier mutación externa de un valor de serie **en vivo** debe señalizarse por este nonce, o `useSetInputs` la ignorará/revertirá.
- **Coherencia de cachés de unidades (offline extremo a extremo):** `useAllUserExerciseGymUnits` usa `staleTime: Infinity` y se **invalida explícitamente** desde `useChangeWeightUnit` (scope exercise) → caché viva toda la sesión sin refetch. Además **siembra** la caché puntual `[EXERCISES,'gym-unit',ex,gym]` desde el bulk, para que la ETIQUETA kg/lb (`useResolvedWeightUnit`) resuelva también offline, coherente con el número ya convertido (si no, offline el número se convertía pero el label caía a la global).
- **`applyWeightConversions` convierte también `pendingSets`:** una serie completada offline (aún en la cola de sync con el peso viejo) debe convertirse a la vez; si no, al drenar `useSyncPendingSets` insertaría el peso sin convertir en BD (store/stats quedaban bien, pero el histórico leído de BD no).
- **DRY web/native:** lógica en `useChangeSessionGym`/`useSyncPendingGymChange` (`hooks/useSession.js`) + puro `lib/sessionGymChange.js` (`planSessionWeightConversion`, `resolveUnitsForExercises`, `buildGymChangeJob`, testeado). Los componentes (`WorkoutSessionLayout` web+native) solo llaman a `changeGym`. El "Anterior" se re-consulta solo (su query key incluye `gymId`).
- **Sugerencias de series NO completadas (decisión del usuario): refrescar, no convertir.** El prefill de una serie pendiente es "lo que hiciste la última vez EN ESE gym", así que al cambiar de gym NO se convierte (sería el dato del gym viejo) sino que se re-prellena con el "Anterior" del gym nuevo (que ya se re-consulta solo por su query key con `gymId`). En `useSetInputs`, el efecto de prefill guarda el último `previousSet` en un ref y re-aplica sobre las series aún no tocadas cuando **cambia**, sin pisar lo completado (`setData`) ni lo que el usuario ya guardó (`cachedData`). ⚠️ **Clave (costó un bug):** la query del "Anterior" (key con `gymId`, sin `keepPreviousData`) pasa por `undefined` mientras recarga al cambiar de gym; el efecto **NO actualiza el ref con ese `undefined` transitorio** (guarda `if (!previousSet) return` antes de tocar el ref), o la detección por identidad fallaría en caché fría (el caso común "me equivoqué de gym al empezar"). Edges aceptados: si el gym nuevo no tiene historial, la sugerencia vieja se mantiene; y un valor tecleado sin commitear aún (ventana de ~600 ms del debounce, sin `cachedData`) podría pisarse al cambiar de gym. Distinto de la conversión de series registradas (esas sí llevan el peso físico convertido).
- **Gotcha menor:** si el prefetch de unidades aún no cargó cuando el usuario cambia de gym muy rápido nada más abrir la sesión, la detección cae a "sin conversión" (degrada al comportamiento previo para ese caso). El prefetch resuelve en ~ms al montar la sesión; edge aceptado.
