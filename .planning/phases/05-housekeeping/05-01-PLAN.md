---
phase: 05-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/Workout/WorkoutExerciseCard.jsx
  - apps/web/src/components/Workout/ExerciseCardHeader.jsx
  - apps/web/src/components/Workout/ExerciseCardNotes.jsx
  - apps/web/src/components/Workout/SetsList.jsx
  - apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx
  - apps/gym-native/src/components/Workout/ExerciseCardHeader.jsx
  - apps/gym-native/src/components/Workout/ExerciseCardNotes.jsx
  - apps/gym-native/src/components/Workout/SetsList.jsx
autonomous: true
requirements:
  - SIZE-03

must_haves:
  truths:
    - "WorkoutExerciseCard (web) orchestrates sub-components and stays under 150 lines"
    - "WorkoutExerciseCard (RN) orchestrates sub-components and stays under 150 lines"
    - "SetsList uses stable composite keys (exerciseKey + setNumber), not array indices"
    - "All state remains in the orchestrator — sub-components are stateless (except WarmupExerciseCard showNotes)"
    - "npm run build passes for both apps without errors"
  artifacts:
    - path: "apps/web/src/components/Workout/ExerciseCardHeader.jsx"
      provides: "Header row: collapse toggle, exercise name, progress badge, dropdown menu"
    - path: "apps/web/src/components/Workout/ExerciseCardNotes.jsx"
      provides: "Badges row (series, RIR, tempo, rest) + collapsible notes panel"
    - path: "apps/web/src/components/Workout/SetsList.jsx"
      provides: "PreviousWorkout + SetRow list with stable keys"
    - path: "apps/gym-native/src/components/Workout/ExerciseCardHeader.jsx"
      provides: "RN header: Pressable row with name, progress badge, dropdown"
    - path: "apps/gym-native/src/components/Workout/ExerciseCardNotes.jsx"
      provides: "RN badges + collapsible notes panel"
    - path: "apps/gym-native/src/components/Workout/SetsList.jsx"
      provides: "RN PreviousWorkout + SetRow list with stable keys"
  key_links:
    - from: "apps/web/src/components/Workout/WorkoutExerciseCard.jsx"
      to: "ExerciseCardHeader.jsx, ExerciseCardNotes.jsx, SetsList.jsx"
      via: "direct import and prop passing"
      pattern: "import .*/ExerciseCardHeader"
    - from: "apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx"
      to: "ExerciseCardHeader.jsx, ExerciseCardNotes.jsx, SetsList.jsx"
      via: "direct import and prop passing"
      pattern: "import .*/ExerciseCardHeader"
---

<objective>
Split WorkoutExerciseCard into sub-components in both web and RN apps.

Purpose: Enforce the one-component-per-file rule and keep all files under 150 lines. Fix unstable array-index keys in SetRow lists.
Output: 6 new sub-component files (3 per app), 2 slimmed orchestrator files.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-housekeeping/05-RESEARCH.md

@apps/web/src/components/Workout/WorkoutExerciseCard.jsx
@apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract web WorkoutExerciseCard into sub-components</name>
  <files>
    apps/web/src/components/Workout/ExerciseCardHeader.jsx,
    apps/web/src/components/Workout/ExerciseCardNotes.jsx,
    apps/web/src/components/Workout/SetsList.jsx,
    apps/web/src/components/Workout/WorkoutExerciseCard.jsx
  </files>
  <action>
Split the main WorkoutExerciseCard function (lines 14-261) into three sub-components. Keep WarmupExerciseCard in WorkoutExerciseCard.jsx (it's already small at ~60 lines).

**ExerciseCardHeader.jsx** (~50 lines) — Extract the header row (lines 104-148 of current file):
- Props: `exerciseName, completedCount, setsCount, isCompleted, collapsed, onToggleCollapse, menuItems` (pre-built array from orchestrator)
- Renders: collapse arrow, exercise name, progress badge, DropdownMenu (only when not collapsed)
- Import: `DropdownMenu` from ui, `colors` from styles
- `menuItems` is pre-built in the orchestrator (the existing array at lines 126-144) and passed as a prop. DropdownMenu items array contains the icon imports too, so ExerciseCardHeader only needs the DropdownMenu import.

**ExerciseCardNotes.jsx** (~65 lines) — Extract badges + notes panel (lines 152-192 of current file):
- Props: `series, reps, rir, tempo, rest_seconds, showNotes, onToggleNotes, exercise, tempoRazon, notes`
- Renders: Badge row (series x reps, RIR, tempo, rest) + "Ver notas" toggle button + collapsible notes content
- Import: `Badge` from ui, `colors` from styles

**SetsList.jsx** (~30 lines) — Extract the PreviousWorkout + SetRow iteration (lines 194-220):
- Props: `exerciseKey, exercise, setsCount, previousWorkout, measurementType, weightUnit, timeUnit, distanceUnit, rest_seconds, onCompleteSet, onUncompleteSet, onRemoveSet`
- CRITICAL: Change `key={i + 1}` to `key={`${exerciseKey}-${i + 1}`}` for stable composite keys
- Import: `SetRow` from ./SetRow.jsx, `PreviousWorkout` from ./PreviousWorkout.jsx

**WorkoutExerciseCard.jsx** (orchestrator, ~130 lines) — Keep:
- All useState, useEffect, useRef, useMemo hooks
- All store reads (completedSets, routineDayId, etc.)
- addSet/removeSet functions
- warmup check + WarmupExerciseCard (keep in same file)
- Wrapper logic (Card vs div for superset)
- Modals (ExerciseHistoryModal, ExercisePickerModal, ConfirmModal) — keep inline
- Build menuItems array here and pass to ExerciseCardHeader
- Import and compose ExerciseCardHeader, ExerciseCardNotes, SetsList

Do NOT add new sub-components to `Workout/index.js` — they are internal to WorkoutExerciseCard.
Do NOT modify SetRow, PreviousWorkout, or any other existing component.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run build -w apps/web && wc -l apps/web/src/components/Workout/WorkoutExerciseCard.jsx apps/web/src/components/Workout/ExerciseCardHeader.jsx apps/web/src/components/Workout/ExerciseCardNotes.jsx apps/web/src/components/Workout/SetsList.jsx</automated>
  </verify>
  <done>
    - WorkoutExerciseCard.jsx is under 150 lines (orchestrator + WarmupExerciseCard)
    - ExerciseCardHeader.jsx, ExerciseCardNotes.jsx, SetsList.jsx each under 150 lines
    - SetsList uses `key={`${exerciseKey}-${i + 1}`}` (stable composite keys)
    - `npm run build -w apps/web` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract RN WorkoutExerciseCard into sub-components</name>
  <files>
    apps/gym-native/src/components/Workout/ExerciseCardHeader.jsx,
    apps/gym-native/src/components/Workout/ExerciseCardNotes.jsx,
    apps/gym-native/src/components/Workout/SetsList.jsx,
    apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx
  </files>
  <action>
Split the RN file following the same pattern as Task 1. The RN file has three functions: WarmupExerciseCard (lines 15-64), RegularExerciseCard (lines 66-303), WorkoutExerciseCard (lines 305-342).

Keep WarmupExerciseCard and the outer WorkoutExerciseCard wrapper in WorkoutExerciseCard.jsx. Split RegularExerciseCard into sub-components.

**ExerciseCardHeader.jsx** (~55 lines) — Extract the header Pressable (lines 143-167 of current):
- Props: `exerciseName, completedCount, setsCount, isCompleted, collapsed, onToggleCollapse, menuItems`
- Uses: `View, Text, Pressable` from react-native, `DropdownMenu` from ui, `colors` from styles
- Same pattern as web but with RN primitives

**ExerciseCardNotes.jsx** (~65 lines) — Extract badges + notes (lines 171-207):
- Props: `series, reps, rir, tempo, rest_seconds, showNotes, onToggleNotes, exercise, tempoRazon, notes`
- Uses: `View, Text, Pressable` from react-native, `Badge` from ui, `colors` from styles

**SetsList.jsx** (~30 lines) — Extract PreviousWorkout + SetRow list (lines 209-235):
- Props: same as web version but RN primitives (View instead of div)
- CRITICAL: Change `key={i + 1}` to `key={`${exerciseKey}-${i + 1}`}` for stable composite keys
- Uses: `View` from react-native, `SetRow` from ./SetRow, `PreviousWorkout` from ./PreviousWorkout

**WorkoutExerciseCard.jsx** (orchestrator, ~140 lines) — Keep:
- WarmupExerciseCard function (lines 15-64, ~50 lines)
- WorkoutExerciseCard outer wrapper (lines 305-342, ~38 lines)
- RegularExerciseCard slimmed to orchestrator: all hooks, state, effects, menuItems building, Wrapper logic
- Modals (ExerciseHistoryModal, ConfirmModal, ExercisePickerModal, ReorderModal) — keep inline
- `export default memo(WorkoutExerciseCard)` preserved

Do NOT add new sub-components to `Workout/index.js`.
Do NOT modify SetRow, PreviousWorkout, or any other existing component.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npx expo export --platform web --output-dir /tmp/expo-check 2>&1 | head -5; wc -l apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx apps/gym-native/src/components/Workout/ExerciseCardHeader.jsx apps/gym-native/src/components/Workout/ExerciseCardNotes.jsx apps/gym-native/src/components/Workout/SetsList.jsx</automated>
  </verify>
  <done>
    - RegularExerciseCard orchestrator section is under 150 lines (total file with WarmupExerciseCard + wrapper may be ~140-150)
    - ExerciseCardHeader.jsx, ExerciseCardNotes.jsx, SetsList.jsx each under 150 lines
    - SetsList uses `key={`${exerciseKey}-${i + 1}`}` (stable composite keys)
    - No import errors in the RN app
  </done>
</task>

</tasks>

<verification>
- `npm run build -w apps/web` passes (web build check)
- `wc -l` on all new and modified files shows each under 150 lines
- `grep 'key={i + 1}' apps/*/src/components/Workout/SetsList.jsx` returns no matches (no array index keys)
- `grep -c 'key={\`\${exerciseKey}' apps/*/src/components/Workout/SetsList.jsx` returns 1 per file (stable keys present)
- Workout/index.js unchanged in both apps
</verification>

<success_criteria>
WorkoutExerciseCard is split into ExerciseCardHeader, ExerciseCardNotes, and SetsList in both web and RN. All files under 150 lines. SetsList uses stable composite keys. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-housekeeping/05-01-SUMMARY.md`
</output>
