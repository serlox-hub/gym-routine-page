---
phase: 06-dx-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: true
requirements:
  - DX-04

must_haves:
  truths:
    - "Zero duplicated non-platform-specific files exist between apps/web/src/lib/ and apps/gym-native/src/lib/"
    - "All root scripts (dev, native, test, lint) work correctly"
    - "Both apps build and run without errors"
  artifacts: []
  key_links: []
---

<objective>
File audit and final verification of zero duplication

Purpose: Confirm DX-04 -- after Plans 01 and 02, the only files remaining in both apps' src/lib/ are the declared platform-specific exclusions. Verify all root scripts work.
Output: Audit passes, all root scripts verified, phase complete.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dx-cleanup/06-01-SUMMARY.md
@.planning/phases/06-dx-cleanup/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run file audit and verify zero unexpected duplicates</name>
  <files></files>
  <action>
1. Run the file audit command to find duplicated filenames in both apps' lib directories:
```bash
comm -12 \
  <(ls apps/web/src/lib/ | sort) \
  <(ls apps/gym-native/src/lib/ | sort)
```

2. Expected result: the only overlapping files should be the declared platform-specific exclusions:
   - `supabase.js` -- different env vars, AsyncStorage adapter
   - `styles.js` -- CSS strings vs RN numbers
   - `videoStorage.js` -- DOM APIs vs expo-file-system
   - `routineIO.js` -- downloadRoutineAsJson uses DOM (web) / throws (RN)
   - `muscleGroupStyles.js` -- CSS string '3px' vs RN number 3 (renamed from constants.js in Plan 01)

3. If ANY file appears in both apps that is NOT in the list above, it is an unexpected duplicate that must be resolved.

4. Also verify no stale imports remain:
```bash
grep -r "from.*lib/queryClient" apps/ --include="*.js" --include="*.jsx"
grep -r "from.*lib/constants" apps/ --include="*.js" --include="*.jsx"
```
Both should return empty.

5. Verify root scripts:
```bash
npm run test:run -w apps/web   # tests pass
npm run lint                    # both apps lint
npm run build -w apps/web       # web builds
```

6. If any unexpected duplicate is found, investigate and either:
   a. Move it to packages/shared if it should be shared
   b. Add it to the exclusion list with justification if it's platform-specific
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && comm -12 <(ls apps/web/src/lib/ | sort) <(ls apps/gym-native/src/lib/ | sort) | grep -v -E 'supabase|styles|videoStorage|routineIO|muscleGroupStyles' | wc -l | tr -d ' ' | xargs -I{} test {} -eq 0 && echo "AUDIT_PASS" || echo "AUDIT_FAIL"</automated>
  </verify>
  <done>File audit passes with zero unexpected duplicates, all root scripts work, both apps verified functional</done>
</task>

</tasks>

<verification>
- File audit command returns only declared exclusions (supabase.js, styles.js, videoStorage.js, routineIO.js, muscleGroupStyles.js)
- `npm run test:run -w apps/web` passes
- `npm run lint` passes for both apps
- `npm run build -w apps/web` succeeds
- No stale queryClient or constants imports remain
</verification>

<success_criteria>
- Zero unexpected file duplicates between apps
- All declared exclusions justified and documented
- All root scripts functional
- Phase 6 goals fully met: DX-01 through DX-04 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-dx-cleanup/06-03-SUMMARY.md`
</output>
