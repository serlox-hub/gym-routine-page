---
phase: 05-housekeeping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/Workout/ExerciseHistoryModal.jsx
  - apps/web/src/components/Workout/HistoryChart.jsx
  - apps/web/src/components/Workout/HistoryTable.jsx
autonomous: true
requirements:
  - SIZE-04

must_haves:
  truths:
    - "ExerciseHistoryModal orchestrator is under 150 lines"
    - "HistoryChart and HistoryTable are each under 150 lines"
    - "The Progress tab shows stats grid + chart when 2+ sessions exist"
    - "The History tab shows session list with load-more pagination"
    - "npm run build passes without errors"
  artifacts:
    - path: "apps/web/src/components/Workout/HistoryChart.jsx"
      provides: "ProgressTab content: StatCard grid + ExerciseProgressChart"
    - path: "apps/web/src/components/Workout/HistoryTable.jsx"
      provides: "HistoryTab content: session list + load-more button"
  key_links:
    - from: "apps/web/src/components/Workout/ExerciseHistoryModal.jsx"
      to: "HistoryChart.jsx, HistoryTable.jsx"
      via: "direct import, conditional render based on activeTab"
      pattern: "import .*/HistoryChart"
---

<objective>
Split ExerciseHistoryModal (330 lines) into sub-components.

Purpose: Enforce one-component-per-file and <150 line limit. The file already has internal functions (ProgressTab, StatCard, HistoryTab) that map directly to the target sub-components.
Output: 2 new sub-component files, 1 slimmed orchestrator.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-housekeeping/05-RESEARCH.md

@apps/web/src/components/Workout/ExerciseHistoryModal.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract HistoryChart and HistoryTable from ExerciseHistoryModal</name>
  <files>
    apps/web/src/components/Workout/HistoryChart.jsx,
    apps/web/src/components/Workout/HistoryTable.jsx,
    apps/web/src/components/Workout/ExerciseHistoryModal.jsx
  </files>
  <action>
The file has 4 internal functions. Extract two of them to separate files. Keep scope toggle and tabs in the orchestrator (they are tightly bound to modal header state).

**HistoryChart.jsx** (~70 lines) — Move `ProgressTab` (lines 180-240) and `StatCard` (lines 242-251):
- Rename `ProgressTab` to `HistoryChart` (the default export name)
- Keep `StatCard` as a local function inside HistoryChart.jsx (only used there)
- Props: `sessions, stats, measurementType, weightUnit`
- Import: `Card` from ui, `colors` from styles, `ExerciseProgressChart` from ./ExerciseProgressChart.jsx

**HistoryTable.jsx** (~75 lines) — Move `HistoryTab` (lines 253-328):
- Rename `HistoryTab` to `HistoryTable` (the default export name)
- Props: `sessions, timeUnit, distanceUnit, onSelectSet, onSessionClick, hasNextPage, isFetchingNextPage, onLoadMore`
- Import: `ChevronRight, FileText` from lucide-react, `colors` from styles, `formatSetValue, formatShortDate` from @gym/shared
- Move `RIR_LABELS` constant into this file (only used here)

**ExerciseHistoryModal.jsx** (orchestrator, ~90 lines) — Keep:
- `TABS` and `SCOPE` constants
- All state (selectedSet, activeTab, scope)
- Data fetching (useExerciseHistorySummary, useExerciseHistory)
- `handleSessionClick`, `stats` useMemo
- Modal shell with header (title, scope toggle, tab buttons)
- Conditional render of `<HistoryChart>` or `<HistoryTable>` based on activeTab
- `<SetNotesView>` at bottom
- Import HistoryChart and HistoryTable

Do NOT add HistoryChart or HistoryTable to `Workout/index.js` — they are internal to ExerciseHistoryModal.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run build -w apps/web && wc -l apps/web/src/components/Workout/ExerciseHistoryModal.jsx apps/web/src/components/Workout/HistoryChart.jsx apps/web/src/components/Workout/HistoryTable.jsx</automated>
  </verify>
  <done>
    - ExerciseHistoryModal.jsx under 150 lines (orchestrator with header + tabs + modal shell)
    - HistoryChart.jsx under 150 lines (stats grid + chart)
    - HistoryTable.jsx under 150 lines (session list + load-more)
    - `npm run build -w apps/web` passes
  </done>
</task>

</tasks>

<verification>
- `npm run build -w apps/web` passes
- `wc -l` on all three files shows each under 150 lines
- Workout/index.js unchanged
</verification>

<success_criteria>
ExerciseHistoryModal is split into HistoryChart and HistoryTable. All three files under 150 lines. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-housekeeping/05-02-SUMMARY.md`
</output>
