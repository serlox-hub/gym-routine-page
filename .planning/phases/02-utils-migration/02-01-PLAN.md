---
phase: 02-utils-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/index.js
  - packages/shared/src/lib/arrayUtils.js
  - packages/shared/src/lib/arrayUtils.test.js
  - packages/shared/src/lib/bodyMeasurementCalculations.js
  - packages/shared/src/lib/bodyMeasurementCalculations.test.js
  - packages/shared/src/lib/bodyWeightCalculations.js
  - packages/shared/src/lib/bodyWeightCalculations.test.js
  - packages/shared/src/lib/calendarUtils.js
  - packages/shared/src/lib/calendarUtils.test.js
  - packages/shared/src/lib/constants.js
  - packages/shared/src/lib/dateUtils.js
  - packages/shared/src/lib/dateUtils.test.js
  - packages/shared/src/lib/measurementConstants.js
  - packages/shared/src/lib/measurementConstants.test.js
  - packages/shared/src/lib/measurementTypes.js
  - packages/shared/src/lib/measurementTypes.test.js
  - packages/shared/src/lib/routineExerciseForm.js
  - packages/shared/src/lib/routineExerciseForm.test.js
  - packages/shared/src/lib/routineIO.js
  - packages/shared/src/lib/routineIO.test.js
  - packages/shared/src/lib/routineTemplates.js
  - packages/shared/src/lib/setUtils.js
  - packages/shared/src/lib/setUtils.test.js
  - packages/shared/src/lib/supersetUtils.js
  - packages/shared/src/lib/supersetUtils.test.js
  - packages/shared/src/lib/textUtils.js
  - packages/shared/src/lib/textUtils.test.js
  - packages/shared/src/lib/timeUtils.js
  - packages/shared/src/lib/timeUtils.test.js
  - packages/shared/src/lib/validation.js
  - packages/shared/src/lib/validation.test.js
  - packages/shared/src/lib/workoutCalculations.js
  - packages/shared/src/lib/workoutCalculations.test.js
  - packages/shared/src/lib/workoutTransforms.js
  - packages/shared/src/lib/workoutTransforms.test.js
  - apps/web/vite.config.js
  - apps/web/src/lib/constants.js
  - apps/web/src/lib/routineIO.js
  - apps/gym-native/src/lib/constants.js
  - apps/gym-native/src/lib/routineIO.js
autonomous: true
requirements:
  - UTIL-01
  - UTIL-02
  - UTIL-03
  - UTIL-04

must_haves:
  truths:
    - "16 pure utility files exist in packages/shared/src/lib/ and are deleted from both apps"
    - "18 test files are co-located with their utils in packages/shared/src/lib/"
    - "Barrel index.js re-exports all shared modules via export * from"
    - "supabase.js, styles.js, videoStorage.js are NOT in packages/shared"
    - "Vitest discovers and runs all 18 test files from the root npm test command"
    - "constants.js getMuscleGroupBorderStyle stays in each app (platform-specific)"
    - "routineIO.js pure parts (constants + prompt builders) move to shared; supabase-dependent functions stay in each app"
  artifacts:
    - path: "packages/shared/src/index.js"
      provides: "Barrel re-exports of all shared modules"
      contains: "export * from './lib/"
    - path: "packages/shared/src/lib/arrayUtils.js"
      provides: "One of 16 migrated utils (representative)"
    - path: "apps/web/src/lib/constants.js"
      provides: "Platform-specific getMuscleGroupBorderStyle for web"
      contains: "getMuscleGroupBorderStyle"
    - path: "apps/web/src/lib/routineIO.js"
      provides: "Platform-specific downloadRoutineAsJson + readJsonFile + supabase-dependent functions for web"
      contains: "downloadRoutineAsJson"
    - path: "apps/gym-native/src/lib/constants.js"
      provides: "Platform-specific getMuscleGroupBorderStyle for RN"
      contains: "getMuscleGroupBorderStyle"
    - path: "apps/gym-native/src/lib/routineIO.js"
      provides: "Platform-specific supabase-dependent functions for RN"
  key_links:
    - from: "packages/shared/src/index.js"
      to: "packages/shared/src/lib/*.js"
      via: "export * from './lib/foo.js'"
      pattern: "export \\* from '\\./lib/"
    - from: "apps/web/vite.config.js"
      to: "packages/shared/src/**/*.test.js"
      via: "vitest include glob"
      pattern: "packages/shared/src"
---

<objective>
Move 16 pure utility files and 18 test files to packages/shared, handle borderline files (constants.js, routineIO.js), update barrel and vitest config.

Purpose: Centralize all shared pure logic in one package so both apps import from @gym/shared — the core requirement of Phase 2.
Output: All shared utils in packages/shared/src/lib/, barrel index.js with full re-exports, vitest config covering shared tests, borderline files properly split.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-utils-migration/02-CONTEXT.md
@.planning/phases/02-utils-migration/02-RESEARCH.md

@packages/shared/src/index.js
@apps/web/vite.config.js

<interfaces>
<!-- Current barrel (to be replaced): -->
From packages/shared/src/index.js:
```js
export const GYM_SHARED_VERSION = '1.0.0'
```

<!-- Vitest config section (to be updated): -->
From apps/web/vite.config.js test section:
```js
test: {
  globals: true,
  environment: 'jsdom',
  setupFiles: ['./src/test/setup.js'],
  exclude: ['**/node_modules/**', 'e2e'],
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move files via git mv, handle borderline files, delete RN duplicates</name>
  <files>
    packages/shared/src/lib/ (all 16 source + 18 test files)
    apps/web/src/lib/constants.js
    apps/web/src/lib/routineIO.js
    apps/gym-native/src/lib/constants.js
    apps/gym-native/src/lib/routineIO.js
  </files>
  <action>
    **Step 1 — Prepare borderline files BEFORE moving:**

    **constants.js reconciliation:**
    - In `apps/web/src/lib/constants.js`: Remove `getMuscleGroupBorderStyle` and `getMuscleGroupColor` (used only by getMuscleGroupBorderStyle internally). Actually — check: `getMuscleGroupColor` is exported independently and used elsewhere, so ONLY remove `getMuscleGroupBorderStyle`. Also check if `MUSCLE_GROUP_COLORS` is used by getMuscleGroupBorderStyle only or independently — it's exported and used by `getMuscleGroupColor`, so keep it.
    - Create a minimal `apps/web/src/lib/constants.js` stub that exports ONLY `getMuscleGroupBorderStyle` (the CSS-string version, importing `getMuscleGroupColor` from `@gym/shared`). WAIT — this import won't work yet because we haven't moved the file. Instead: after the move, the stub will import from `@gym/shared`.
    - Actually, the correct sequence is:
      1. Edit web's `constants.js` to remove `getMuscleGroupBorderStyle` function
      2. `git mv apps/web/src/lib/constants.js packages/shared/src/lib/constants.js` — moves the cleaned version
      3. Create NEW `apps/web/src/lib/constants.js` with just `getMuscleGroupBorderStyle` that imports `MUSCLE_GROUP_COLORS, getMuscleGroupColor` from `@gym/shared`
    - For RN: `git rm apps/gym-native/src/lib/constants.js`, then create NEW `apps/gym-native/src/lib/constants.js` with just `getMuscleGroupBorderStyle` (RN version using numeric borderLeftWidth: 3)
    - Use web's `'Controlado'` text for the shared RIR_OPTIONS (per research recommendation)

    **routineIO.js split (pure parts only to shared, per research recommendation):**
    - The safest approach: move ONLY the pure parts to shared. Supabase-dependent functions (`exportRoutine`, `importRoutine`, `duplicateRoutine`) and DOM-dependent functions (`downloadRoutineAsJson`, `readJsonFile`) stay in each app.
    - Edit `apps/web/src/lib/routineIO.js`: extract `ROUTINE_JSON_FORMAT`, `ROUTINE_JSON_RULES`, `buildChatbotPrompt`, `buildAdaptRoutinePrompt` into a new file. Then:
      1. Create `packages/shared/src/lib/routineIO.js` with the 4 pure exports (constants + prompt builders). Also move `MeasurementType` import to `./measurementTypes.js` (both files are in shared).
      2. Keep `apps/web/src/lib/routineIO.js` with supabase-dependent + DOM-dependent functions. Update its imports: import the pure constants/functions from `@gym/shared`, keep `import { supabase } from './supabase.js'`.
      3. Keep `apps/gym-native/src/lib/routineIO.js` with its supabase-dependent functions. Update its imports similarly: import pure parts from `@gym/shared`.
    - Move the test file: `git mv apps/web/src/lib/routineIO.test.js packages/shared/src/lib/routineIO.test.js`. Update the test to only test the pure functions (constants existence, prompt builder outputs). If the existing test tests supabase functions, those tests stay in web (create `apps/web/src/lib/routineIO.test.js` with those tests) or are removed (Phase 3 will handle API testing).

    **Step 2 — git mv the 14 straightforward files (source + tests):**
    These are byte-for-byte identical between apps. Use web as source (web has tests, RN doesn't):
    ```
    arrayUtils, bodyMeasurementCalculations, bodyWeightCalculations, calendarUtils,
    dateUtils, measurementConstants, measurementTypes, routineExerciseForm,
    routineTemplates, setUtils, supersetUtils, textUtils, timeUtils, validation,
    workoutCalculations
    ```
    For each: `git mv apps/web/src/lib/FILE.js packages/shared/src/lib/FILE.js`
    For each with test: `git mv apps/web/src/lib/FILE.test.js packages/shared/src/lib/FILE.test.js`

    **workoutTransforms.js** (locked: share it, take web version):
    `git mv apps/web/src/lib/workoutTransforms.js packages/shared/src/lib/workoutTransforms.js`
    `git mv apps/web/src/lib/workoutTransforms.test.js packages/shared/src/lib/workoutTransforms.test.js`

    **Step 3 — git rm all RN duplicate source files** (16 files minus the 2 that were already handled as borderline):
    `git rm apps/gym-native/src/lib/arrayUtils.js` (repeat for: bodyMeasurementCalculations, bodyWeightCalculations, calendarUtils, dateUtils, measurementConstants, measurementTypes, routineExerciseForm, routineTemplates, setUtils, supersetUtils, textUtils, timeUtils, validation, workoutCalculations, workoutTransforms)

    **Step 4 — Verify inter-lib imports resolve:**
    After the move, check that sibling imports within packages/shared/src/lib/ still work:
    - `arrayUtils.js` imports `./textUtils.js` — both in shared, OK
    - `workoutCalculations.js` imports `./measurementTypes.js` — both in shared, OK
    - `setUtils.js` imports `./measurementTypes.js` — both in shared, OK
    - `routineIO.js` (shared) should NOT import `./supabase.js` — must be clean

    **Files that MUST NOT move to packages/shared:** supabase.js, styles.js, videoStorage.js, queryClient.js (deferred to Phase 6 per DX-02).
  </action>
  <verify>
    <automated>ls packages/shared/src/lib/*.js | grep -v test | wc -l</automated>
    Expected: 18 (16 original + constants.js + routineIO.js pure parts). Actually count: the 16 identified files are arrayUtils, bodyMeasurementCalculations, bodyWeightCalculations, calendarUtils, constants, dateUtils, measurementConstants, measurementTypes, routineExerciseForm, routineIO, routineTemplates, setUtils, supersetUtils, textUtils, timeUtils, validation, workoutCalculations, workoutTransforms = 18 source files. Expected output: 18.
    Also: `ls packages/shared/src/lib/*.test.js | wc -l` expected: 18 test files.
    Also: `ls apps/web/src/lib/ | grep -v supabase | grep -v styles | grep -v videoStorage | grep -v queryClient | grep -v routineIO | grep -v constants` — should show nothing (all shared utils deleted from web). Actually routineIO.js and constants.js stay as stubs, so filter those. The remaining should be empty or only have platform-specific files.
  </verify>
  <done>
    - 18 source files exist in packages/shared/src/lib/ (16 identical + constants + routineIO pure parts)
    - 18 test files exist in packages/shared/src/lib/
    - Old copies removed from apps/web/src/lib/ (except stubs for constants.js, routineIO.js) and apps/gym-native/src/lib/
    - Platform-specific files (supabase.js, styles.js, videoStorage.js, queryClient.js) untouched in each app
    - apps/web/src/lib/constants.js is a stub exporting only getMuscleGroupBorderStyle
    - apps/web/src/lib/routineIO.js retains supabase-dependent + DOM functions
    - apps/gym-native/src/lib/constants.js is a stub exporting only getMuscleGroupBorderStyle (RN numeric version)
    - apps/gym-native/src/lib/routineIO.js retains supabase-dependent functions
  </done>
</task>

<task type="auto">
  <name>Task 2: Update barrel index.js and vitest config</name>
  <files>
    packages/shared/src/index.js
    apps/web/vite.config.js
  </files>
  <action>
    **Barrel index.js:**
    Replace the smoke-test export in `packages/shared/src/index.js` with full re-exports:
    ```js
    // packages/shared/src/index.js
    export * from './lib/arrayUtils.js'
    export * from './lib/bodyMeasurementCalculations.js'
    export * from './lib/bodyWeightCalculations.js'
    export * from './lib/calendarUtils.js'
    export * from './lib/constants.js'
    export * from './lib/dateUtils.js'
    export * from './lib/measurementConstants.js'
    export * from './lib/measurementTypes.js'
    export * from './lib/routineExerciseForm.js'
    export * from './lib/routineIO.js'
    export * from './lib/routineTemplates.js'
    export * from './lib/setUtils.js'
    export * from './lib/supersetUtils.js'
    export * from './lib/textUtils.js'
    export * from './lib/timeUtils.js'
    export * from './lib/validation.js'
    export * from './lib/workoutCalculations.js'
    export * from './lib/workoutTransforms.js'
    ```
    Remove `GYM_SHARED_VERSION` (smoke test artifact from Phase 1; no longer needed).

    **Check for export name collisions:** Before writing, verify no two files export the same name. If collision exists, use `export { namedExport as alias } from './lib/foo.js'` pattern. Known risk: `constants.js` exports generic names like `SESSION_STATUS` — verify no other file exports the same.

    **Vitest config (Option A — simplest, per research recommendation):**
    In `apps/web/vite.config.js`, update the `test` section to add an `include` array:
    ```js
    test: {
      globals: true,
      environment: 'jsdom',
      setupFiles: ['./src/test/setup.js'],
      include: [
        'src/**/*.test.js',
        '../../packages/shared/src/**/*.test.js',
      ],
      exclude: ['**/node_modules/**', 'e2e'],
    },
    ```
    This ensures `npm test` (which delegates to `npm run test:run -w apps/web`) discovers tests in both web and packages/shared.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:run -w apps/web 2>&1 | tail -5</automated>
    Expected: All 18+ test files discovered and passing. Look for "Tests" line showing the count.
    Also verify barrel works: `node -e "import('@gym/shared').then(m => console.log(Object.keys(m).length))" --input-type=module` or use `node --experimental-vm-modules` if needed. Alternatively: `cd apps/web && node -e "const m = await import('@gym/shared'); console.log(Object.keys(m).length)" --input-type=module` — expect > 50 named exports.
  </verify>
  <done>
    - packages/shared/src/index.js has export * from for all 18 util modules
    - No export name collisions
    - vitest include glob covers packages/shared/src/**/*.test.js
    - npm run test:run -w apps/web discovers and passes all shared tests
  </done>
</task>

</tasks>

<verification>
After both tasks, before committing (this is all one atomic commit per locked decision):
1. `ls packages/shared/src/lib/*.js | grep -v test | wc -l` — expect 18
2. `ls packages/shared/src/lib/*.test.js | wc -l` — expect 18
3. `ls packages/shared/src/lib/ | grep -E 'supabase|styles|videoStorage'` — expect empty
4. `npm run test:run -w apps/web` — all tests pass
5. No git-tracked files reference `import.*from.*'\.\.?/lib/(arrayUtils|bodyMeasurementCalculations|bodyWeightCalculations|calendarUtils|dateUtils|measurementConstants|measurementTypes|routineExerciseForm|routineTemplates|setUtils|supersetUtils|textUtils|timeUtils|validation|workoutCalculations|workoutTransforms)'` — those should all be gone (import updates happen in Plan 02)

NOTE: This plan does NOT update import statements in consumer files. That is Plan 02. After this plan, the shared utils exist in packages/shared and the barrel re-exports them, but web/RN consumer files still have old relative imports (which will be broken until Plan 02 runs). The vitest config update and test pass confirm the shared package itself is correct.
</verification>

<success_criteria>
- 18 source files in packages/shared/src/lib/
- 18 test files co-located in packages/shared/src/lib/
- Barrel index.js with 18 export * from statements
- Vitest discovers tests in packages/shared
- Platform-specific files NOT in packages/shared (supabase.js, styles.js, videoStorage.js)
- constants.js and routineIO.js properly split (shared pure logic + app-specific stubs)
</success_criteria>

<output>
After completion, create `.planning/phases/02-utils-migration/02-01-SUMMARY.md`
</output>
