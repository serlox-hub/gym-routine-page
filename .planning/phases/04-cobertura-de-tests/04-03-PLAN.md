---
phase: 04-cobertura-de-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/hooks/useRoutines.test.js
  - packages/shared/src/hooks/useExercises.test.js
  - packages/shared/src/hooks/useSessionExercises.test.js
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "useRoutines hooks fetch and mutate routine data correctly"
    - "useExercises hooks fetch exercises and muscle groups correctly"
    - "useSessionExercises hooks manage session exercise CRUD correctly"
    - "All hook tests mock _stores.js to avoid initStores() requirement"
  artifacts:
    - path: "packages/shared/src/hooks/useRoutines.test.js"
      provides: "Tests for useRoutines query and mutation hooks"
      min_lines: 100
    - path: "packages/shared/src/hooks/useExercises.test.js"
      provides: "Tests for useExercises query and mutation hooks"
      min_lines: 60
    - path: "packages/shared/src/hooks/useSessionExercises.test.js"
      provides: "Tests for useSessionExercises hooks"
      min_lines: 80
  key_links:
    - from: "packages/shared/src/hooks/useRoutines.test.js"
      to: "packages/shared/src/api/routineApi.js"
      via: "vi.mock('../api/routineApi.js')"
      pattern: "vi\\.mock.*routineApi"
    - from: "packages/shared/src/hooks/useRoutines.test.js"
      to: "packages/shared/src/hooks/_stores.js"
      via: "vi.mock('./_stores.js') for useAuthStore"
      pattern: "vi\\.mock.*_stores"
---

<objective>
Write comprehensive tests for the three critical shared hooks: useRoutines, useExercises, and useSessionExercises.

Purpose: Cover the hook layer with tests using renderHook + QueryClientProvider, following the established pattern from useWorkoutHistory.test.js and useCompletedSets.test.js.
Output: Three test files for the hooks specified in TEST-03.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cobertura-de-tests/04-CONTEXT.md
@.planning/phases/04-cobertura-de-tests/04-RESEARCH.md

@packages/shared/src/hooks/useRoutines.js
@packages/shared/src/hooks/useExercises.js
@packages/shared/src/hooks/useSessionExercises.js
@packages/shared/src/hooks/_stores.js
@packages/shared/src/hooks/useWorkoutHistory.test.js
@packages/shared/src/hooks/useCompletedSets.test.js

<interfaces>
<!-- CRITICAL: _stores.js mock pattern (from useCompletedSets.test.js): -->
<!--
vi.mock('./_stores.js', () => {
  const mockAuthStore = vi.fn((selector) => {
    const state = { user: { id: 'user-123' } }
    return selector ? selector(state) : state
  })
  return { useAuthStore: mockAuthStore, useWorkoutStore: vi.fn() }
})
-->

<!-- createWrapper pattern (from useWorkoutHistory.test.js): -->
<!--
function createWrapper() {
  const queryClient = new QueryClient({
    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
  })
  return ({ children }) =>
    React.createElement(QueryClientProvider, { client: queryClient }, children)
}
-->

<!-- useRoutines.js exports (251 lines): -->
<!-- Queries: useRoutines(), useRoutine(routineId), useRoutineDays(routineId), useRoutineDay(dayId), useRoutineBlocks(dayId), useRoutineAllExercises(dayId) -->
<!-- Mutations: useCreateRoutine(), useCreateRoutineDay(), useUpdateRoutine(), useDeleteRoutine(), useDeleteRoutines(), useSetFavoriteRoutine(), useUpdateRoutineDay(), useDeleteRoutineDay(), useReorderRoutineDays(), useDeleteRoutineExercise(), useUpdateRoutineExercise(), useReorderRoutineExercises(), useAddExerciseToDay(), useDuplicateRoutineExercise(), useMoveRoutineExerciseToDay() -->

<!-- useExercises.js exports (89 lines): -->
<!-- Queries: useExercises(), useMuscleGroups() -->
<!-- Mutations: useCreateExercise(), useUpdateExercise(), useDeleteExercise() -->

<!-- useSessionExercises.js exports (145 lines): -->
<!-- Queries: useSessionExercises(sessionId), useSessionExerciseBlockName(sessionExerciseId) -->
<!-- Mutations: useAddSessionExercise(), useDeleteSessionExercise(), useReorderSessionExercises(), useUpdateSessionExerciseExerciseId() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test useRoutines hooks</name>
  <files>packages/shared/src/hooks/useRoutines.test.js</files>
  <action>
Create `packages/shared/src/hooks/useRoutines.test.js` following the useWorkoutHistory.test.js pattern.

Setup:
- `vi.mock('../api/routineApi.js', () => ({...}))` — mock ALL exported API functions
- `vi.mock('./_stores.js', () => ({ useAuthStore: vi.fn(...), useWorkoutStore: vi.fn() }))` — return user with id 'user-123'
- `createWrapper()` with fresh QueryClient per test, retry: false
- `beforeEach(() => { vi.clearAllMocks() })`

Test cases (focus on most important hooks, not all 15+ mutations):

**Queries (all of these):**
- useRoutines: returns routines list from fetchRoutines, passes userId
- useRoutine: returns single routine, passes routineId
- useRoutineDays: returns days for routine
- useRoutineDay: returns single day
- useRoutineBlocks: returns blocks for day
- useRoutineAllExercises: returns exercises for day

**Mutations (representative sample — 3-4 most important):**
- useCreateRoutine: calls createRoutine with correct args, test success
- useDeleteRoutine: calls deleteRoutine with routineId
- useUpdateRoutineExercise: calls updateRoutineExercise with correct args
- useAddExerciseToDay: calls addExerciseToDay with correct args

For queries: `renderHook(() => useRoutines(), { wrapper: createWrapper() })` then `await waitFor(() => expect(result.current.data).toBeDefined())`.
For mutations: `await act(async () => { await result.current.mutateAsync(args) })` then verify mock called with correct args.

IMPORTANT: Do NOT test every single mutation — there are 15 of them and they all follow the same pattern. Test 3-4 representative ones covering different patterns (create, delete, update, composed operation).
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose 2>&1 | grep -E "(useRoutines|PASS|FAIL)" | head -20</automated>
  </verify>
  <done>useRoutines.test.js covers all 6 query hooks and 3-4 representative mutation hooks. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Test useExercises and useSessionExercises hooks</name>
  <files>packages/shared/src/hooks/useExercises.test.js, packages/shared/src/hooks/useSessionExercises.test.js</files>
  <action>
Create two test files following the same pattern as Task 1.

**useExercises.test.js:**
- Mock `../api/exerciseApi.js` (fetchExercisesWithMuscleGroup, fetchMuscleGroups, createExercise, updateExercise, deleteExercise)
- Mock `_stores.js` for useAuthStore
- Test useExercises: returns exercises list, passes userId
- Test useMuscleGroups: returns muscle groups
- Test useCreateExercise: calls createExercise with correct args
- Test useUpdateExercise: calls updateExercise
- Test useDeleteExercise: calls deleteExercise (soft delete)

**useSessionExercises.test.js:**
- Mock `../api/workoutApi.js` — this is the barrel that re-exports session exercise functions
- Mock `_stores.js` for useAuthStore and useWorkoutStore (useSessionExercises may need workoutStore for sessionId)
- Test useSessionExercises: returns session exercises for given sessionId
- Test useSessionExerciseBlockName: returns block name for session exercise
- Test useAddSessionExercise: calls insertSessionExercise/addSessionExercise with correct args (this has complex sort_order logic — verify the API function is called, not the internal logic)
- Test useDeleteSessionExercise: calls deleteSessionExercise
- Test useReorderSessionExercises: calls reorderSessionExercises

Check which API module useSessionExercises.js actually imports from (it may import from `../api/workoutApi.js` or from individual sub-modules). Mock the correct module path.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose 2>&1 | grep -E "(useExercises|useSessionExercises|PASS|FAIL)" | head -20</automated>
  </verify>
  <done>useExercises.test.js covers 2 queries and 3 mutations. useSessionExercises.test.js covers 2 queries and 3 mutations. All tests pass. Existing hook tests (useWorkoutHistory, useCompletedSets) still pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose
```
All hook tests green (new + existing useWorkoutHistory + useCompletedSets).
</verification>

<success_criteria>
- useRoutines.test.js covers all query hooks and representative mutations
- useExercises.test.js covers queries and mutations
- useSessionExercises.test.js covers queries and mutations
- All tests mock _stores.js correctly (no initStores error)
- `npm run test:shared` passes with all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04-cobertura-de-tests/04-03-SUMMARY.md`
</output>
