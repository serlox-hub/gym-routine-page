---
phase: 02-deduplicar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/hooks/useCompletedSets.js
  - packages/shared/src/hooks/useSession.js
  - packages/shared/src/index.js
  - apps/web/src/hooks/useCompletedSets.js
  - apps/web/src/hooks/useSession.js
  - apps/gym-native/src/hooks/useCompletedSets.js
  - apps/gym-native/src/hooks/useSession.js
autonomous: true
requirements: [DUP-01]

must_haves:
  truths:
    - "useCompletedSets hooks (all 5 exports) available from packages/shared"
    - "useSession hooks (all 4 exports) available from packages/shared"
    - "useSyncPendingSets accepts onVisibilityChange and onConnectivityChange callbacks"
    - "useRestoreActiveSession accepts onVisibilityChange callback"
    - "Each app wrapper is <=10 lines per platform-divergent function"
    - "No platform APIs (AppState, document, NetInfo, navigator) in shared code"
    - "npm run check passes"
  artifacts:
    - path: "packages/shared/src/hooks/useCompletedSets.js"
      provides: "useCompleteSet, useSyncPendingSets, useUpdateSetVideo, useUpdateSetDetails, useUncompleteSet"
      contains: "export function useSyncPendingSets"
    - path: "packages/shared/src/hooks/useSession.js"
      provides: "useRestoreActiveSession, useStartSession, useEndSession, useAbandonSession"
      contains: "export function useRestoreActiveSession"
    - path: "apps/web/src/hooks/useCompletedSets.js"
      provides: "Thin wrapper wiring document.visibilitychange + navigator.onLine"
    - path: "apps/gym-native/src/hooks/useCompletedSets.js"
      provides: "Thin wrapper wiring AppState + NetInfo"
  key_links:
    - from: "apps/web/src/hooks/useWorkout.js"
      to: "apps/web/src/hooks/useSession.js"
      via: "export * re-export chain"
      pattern: "export \\* from './useSession.js'"
    - from: "apps/web/src/hooks/useWorkout.js"
      to: "apps/web/src/hooks/useCompletedSets.js"
      via: "export * re-export chain"
      pattern: "export \\* from './useCompletedSets.js'"
    - from: "packages/shared/src/index.js"
      to: "packages/shared/src/hooks/useCompletedSets.js"
      via: "barrel export"
      pattern: "export \\* from './hooks/useCompletedSets.js'"
---

<objective>
Move useCompletedSets (5 hooks) and useSession (4 hooks) from per-app files to packages/shared, using callback injection for platform-divergent parts (visibility, connectivity events). Each app retains thin wrappers (<=10 lines) that wire platform APIs.

Purpose: Eliminate ~700 lines of duplication. The platform-abstraction pattern (callback injection) already exists in useTimerEngine.
Output: Shared hooks with callback injection, thin per-app wrappers, npm run check green.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deduplicar/02-CONTEXT.md
@.planning/phases/02-deduplicar/02-RESEARCH.md

@apps/web/src/hooks/useCompletedSets.js
@apps/gym-native/src/hooks/useCompletedSets.js
@apps/web/src/hooks/useSession.js
@apps/gym-native/src/hooks/useSession.js
@packages/shared/src/hooks/useRestTimer.js
@packages/shared/src/hooks/_stores.js
@packages/shared/src/index.js
@apps/web/src/hooks/useWorkout.js
@apps/gym-native/src/hooks/useWorkout.js

<interfaces>
<!-- From packages/shared/src/hooks/useRestTimer.js — callback injection precedent -->
export function useTimerEngine({ playSound, vibrateDevice, onTimerStart, onTimerEnd, isSoundEnabled }) { ... }

<!-- From packages/shared/src/hooks/_stores.js — store injection -->
export function initStores({ workoutStore, authStore }) { ... }
// Shared hooks use useWorkoutStore from _stores.js

<!-- From apps/web/src/hooks/useCompletedSets.js — 5 exports -->
export function useCompleteSet() { ... }          // identical web/RN
export function useSyncPendingSets() { ... }      // DIVERGES: visibility + connectivity events
export function useUpdateSetVideo() { ... }       // identical web/RN
export function useUpdateSetDetails() { ... }     // identical web/RN
export function useUncompleteSet() { ... }        // identical web/RN

<!-- From apps/web/src/hooks/useSession.js — 4 exports -->
export function useRestoreActiveSession() { ... } // DIVERGES: visibility event
export function useStartSession() { ... }         // near-identical (RN has extra onError callback)
export function useEndSession() { ... }           // identical web/RN
export function useAbandonSession() { ... }       // identical web/RN

<!-- Callback signature convention (setup function returning cleanup): -->
// onVisibilityChange(callback): () => cleanup
// onConnectivityChange(callback): () => cleanup

<!-- useWorkout.js re-export chain (stays intact — per-app files become wrappers): -->
// apps/web/src/hooks/useWorkout.js: export * from './useSession.js'
// apps/web/src/hooks/useWorkout.js: export * from './useCompletedSets.js'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared useCompletedSets.js and useSession.js with callback injection</name>
  <files>
    packages/shared/src/hooks/useCompletedSets.js,
    packages/shared/src/hooks/useSession.js,
    packages/shared/src/index.js
  </files>
  <action>
1. Read both web and RN versions of `useCompletedSets.js` fully, diffing to identify exact divergence points.

2. Create `packages/shared/src/hooks/useCompletedSets.js`:
   - Copy the web version as base (or RN — they're identical except for platform events).
   - For `useSyncPendingSets`: add `{ onVisibilityChange, onConnectivityChange } = {}` parameter. Replace the hardcoded `document.addEventListener('visibilitychange', ...)` with `onVisibilityChange(syncPending)` inside a useEffect (returns cleanup). Replace `window.addEventListener('online', ...)` with `onConnectivityChange(syncPending)` inside a useEffect (returns cleanup). Guard both with `if (!callback) return`.
   - `useCompleteSet`, `useUpdateSetVideo`, `useUpdateSetDetails`, `useUncompleteSet` — copy as-is (no platform code).
   - Import `getClient` from `'../_client.js'` if these functions use supabase directly, OR import API functions they call. Check the actual code to see which pattern they use (likely they call API functions from workoutApi.js which are already in shared).
   - Import store access via `_stores.js` pattern if they use workoutStore.

3. Read both web and RN versions of `useSession.js` fully.

4. Create `packages/shared/src/hooks/useSession.js`:
   - For `useRestoreActiveSession`: add `{ onVisibilityChange } = {}` parameter. Replace platform-specific visibility listener with `onVisibilityChange(() => syncRef.current())` inside useEffect.
   - For `useStartSession`: add optional `{ onStartError } = {}` parameter. RN passes `() => useWorkoutStore.getState().hideWorkout()`, web passes nothing. In the shared hook, call `onStartError?.()` in the `onError` handler.
   - `useEndSession`, `useAbandonSession` — copy as-is.

5. Update `packages/shared/src/index.js` — add:
   ```js
   export * from './hooks/useCompletedSets.js'
   export * from './hooks/useSession.js'
   ```
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared</automated>
  </verify>
  <done>Both shared hook files exist with callback injection parameters. No platform imports (AppState, document, NetInfo, navigator) in shared. Barrel export updated. test:shared passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace per-app hooks with thin wrappers</name>
  <files>
    apps/web/src/hooks/useCompletedSets.js,
    apps/web/src/hooks/useSession.js,
    apps/gym-native/src/hooks/useCompletedSets.js,
    apps/gym-native/src/hooks/useSession.js
  </files>
  <action>
1. **Replace** `apps/web/src/hooks/useCompletedSets.js` with thin wrapper:
   - Re-export identical hooks directly: `export { useCompleteSet, useUpdateSetVideo, useUpdateSetDetails, useUncompleteSet } from '@gym/shared'`
   - Wrap `useSyncPendingSets`: import `useSyncPendingSets as _useSyncPendingSets` from `@gym/shared`, export a function that calls it with:
     - `onVisibilityChange: (cb) => { const h = () => { if (document.visibilityState === 'visible') cb() }; document.addEventListener('visibilitychange', h); return () => document.removeEventListener('visibilitychange', h) }`
     - `onConnectivityChange: (cb) => { window.addEventListener('online', cb); return () => window.removeEventListener('online', cb) }`

2. **Replace** `apps/web/src/hooks/useSession.js` with thin wrapper:
   - Re-export: `export { useStartSession, useEndSession, useAbandonSession } from '@gym/shared'`
   - Wrap `useRestoreActiveSession`: pass `onVisibilityChange` callback wiring `document.visibilitychange`.

3. **Replace** `apps/gym-native/src/hooks/useCompletedSets.js` with thin wrapper:
   - Re-export identical hooks from `@gym/shared`.
   - Wrap `useSyncPendingSets` with:
     - `onVisibilityChange: (cb) => { const sub = AppState.addEventListener('change', s => { if (s === 'active') cb() }); return () => sub.remove() }`
     - `onConnectivityChange: (cb) => NetInfo.addEventListener(s => { if (s.isConnected) cb() })`

4. **Replace** `apps/gym-native/src/hooks/useSession.js` with thin wrapper:
   - Re-export `useEndSession`, `useAbandonSession` from `@gym/shared`.
   - Wrap `useRestoreActiveSession` with AppState visibility callback.
   - Wrap `useStartSession` passing `onStartError: () => useWorkoutStore.getState().hideWorkout()` (verify this is what RN does by reading the actual code).

5. Verify each wrapper file is <=10 lines per wrapped function. The `useWorkout.js` re-export chain in each app (`export * from './useSession.js'` etc.) stays unchanged — it re-exports from the per-app wrappers which now delegate to shared.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run check</automated>
  </verify>
  <done>Each per-app file is a thin wrapper (<=10 lines per platform-divergent function). useWorkout.js re-export chain intact. No platform APIs in shared. npm run check passes.</done>
</task>

</tasks>

<verification>
- `npm run check` passes (lint + test + build for all workspaces)
- `grep -rn "AppState\|NetInfo\|document\.\|navigator\.\|window\." packages/shared/src/hooks/useCompletedSets.js packages/shared/src/hooks/useSession.js` returns 0 matches
- Each per-app wrapper file is <30 lines total
- All 5 useCompletedSets exports and 4 useSession exports resolve via `@gym/shared`
</verification>

<success_criteria>
- packages/shared/src/hooks/useCompletedSets.js exports all 5 hooks
- packages/shared/src/hooks/useSession.js exports all 4 hooks
- useSyncPendingSets accepts { onVisibilityChange, onConnectivityChange } callbacks
- useRestoreActiveSession accepts { onVisibilityChange } callback
- useStartSession accepts optional { onStartError } callback
- Per-app wrappers are <=10 lines per wrapped function
- No platform APIs in shared code
- npm run check green
</success_criteria>

<output>
After completion, create `.planning/phases/02-deduplicar/02-02-SUMMARY.md`
</output>
