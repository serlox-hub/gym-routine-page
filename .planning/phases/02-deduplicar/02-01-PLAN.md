---
phase: 02-deduplicar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/api/routineApi.js
  - packages/shared/src/index.js
  - apps/web/src/lib/routineIO.js
  - apps/web/src/hooks/useRoutines.js
  - apps/web/src/components/Routine/RoutineHeader.jsx
  - apps/web/src/components/Home/NewRoutineFlow.jsx
  - apps/gym-native/src/lib/routineIO.js
  - apps/gym-native/src/hooks/useRoutines.js
  - apps/gym-native/src/components/Routine/RoutineHeader.jsx
  - apps/gym-native/src/components/Home/NewRoutineFlow.jsx
autonomous: true
requirements: [DUP-03]

must_haves:
  truths:
    - "exportRoutine, importRoutine, duplicateRoutine live in packages/shared and use getClient()"
    - "exportRoutine makes N+2 queries (routine + days + N blocks) instead of N+2+N"
    - "Web retains downloadRoutineAsJson and readJsonFile as platform-specific functions"
    - "RN routineIO.js is near-empty or deleted"
    - "npm run check passes with no errors"
  artifacts:
    - path: "packages/shared/src/api/routineApi.js"
      provides: "exportRoutine, importRoutine, duplicateRoutine functions using getClient()"
      contains: "export async function exportRoutine"
    - path: "apps/web/src/lib/routineIO.js"
      provides: "Platform-specific downloadRoutineAsJson, readJsonFile only"
      contains: "downloadRoutineAsJson"
  key_links:
    - from: "apps/web/src/components/Routine/RoutineHeader.jsx"
      to: "packages/shared/src/api/routineApi.js"
      via: "import { exportRoutine } from '@gym/shared' (or via routineIO re-export)"
      pattern: "exportRoutine"
    - from: "apps/web/src/hooks/useRoutines.js"
      to: "packages/shared/src/api/routineApi.js"
      via: "import { duplicateRoutine } from '@gym/shared'"
      pattern: "duplicateRoutine"
    - from: "apps/web/src/components/Home/NewRoutineFlow.jsx"
      to: "packages/shared/src/api/routineApi.js"
      via: "import { importRoutine } from '@gym/shared'"
      pattern: "importRoutine"
---

<objective>
Move exportRoutine, importRoutine, and duplicateRoutine from per-app routineIO.js to packages/shared/src/api/routineApi.js, fixing the N+1 query in exportRoutine during the move. Platform-specific functions (downloadRoutineAsJson, readJsonFile) stay per-app.

Purpose: Eliminate ~500 lines of duplication and fix a known performance bug (N+1 query) in the process.
Output: Three functions consolidated in shared, all consumers updated, npm run check green.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deduplicar/02-CONTEXT.md
@.planning/phases/02-deduplicar/02-RESEARCH.md

@packages/shared/src/api/routineApi.js
@packages/shared/src/api/_client.js
@packages/shared/src/index.js
@apps/web/src/lib/routineIO.js
@apps/gym-native/src/lib/routineIO.js
@apps/web/src/hooks/useRoutines.js
@apps/gym-native/src/hooks/useRoutines.js
@apps/web/src/components/Routine/RoutineHeader.jsx
@apps/web/src/components/Routine/ImportRoutineModal.jsx
@apps/web/src/components/Home/NewRoutineFlow.jsx
@apps/gym-native/src/components/Routine/RoutineHeader.jsx
@apps/gym-native/src/components/Home/NewRoutineFlow.jsx

<interfaces>
<!-- From packages/shared/src/api/_client.js -->
export function initApi(supabaseClient) { ... }
export function getClient() { ... }  // Returns supabase client; throws if not initialized

<!-- From packages/shared/src/index.js — routineApi already exported -->
export * from './api/routineApi.js'

<!-- From apps/web/src/lib/routineIO.js — functions to move vs keep -->
// MOVE TO SHARED: exportRoutine, importRoutine, duplicateRoutine
// KEEP PER-APP: downloadRoutineAsJson (DOM Blob), readJsonFile (FileReader)
// ALREADY RE-EXPORTED FROM SHARED: ROUTINE_JSON_FORMAT, ROUTINE_JSON_RULES, buildChatbotPrompt, buildAdaptRoutinePrompt

<!-- Current consumers (must update imports): -->
// apps/web/src/hooks/useRoutines.js:       import { duplicateRoutine } from '../lib/routineIO.js'
// apps/web/src/components/Routine/RoutineHeader.jsx: import { downloadRoutineAsJson, exportRoutine } from '../../lib/routineIO.js'
// apps/web/src/components/Home/NewRoutineFlow.jsx:   import { importRoutine } from '../../lib/routineIO.js'
// apps/web/src/components/Routine/ImportRoutineModal.jsx: import { readJsonFile } from '../../lib/routineIO.js'
// apps/gym-native/src/hooks/useRoutines.js:            import { duplicateRoutine } from '../lib/routineIO.js'
// apps/gym-native/src/components/Routine/RoutineHeader.jsx: import { exportRoutine } from '../../lib/routineIO'
// apps/gym-native/src/components/Home/NewRoutineFlow.jsx:   import { importRoutine } from '../../lib/routineIO'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move exportRoutine/importRoutine/duplicateRoutine to shared with N+1 fix</name>
  <files>
    packages/shared/src/api/routineApi.js,
    apps/web/src/lib/routineIO.js,
    apps/gym-native/src/lib/routineIO.js
  </files>
  <action>
1. Read both `apps/web/src/lib/routineIO.js` and `apps/gym-native/src/lib/routineIO.js` fully to understand the three functions.

2. **Append** to `packages/shared/src/api/routineApi.js` the following three functions, converting `supabase.from(...)` calls to `getClient().from(...)`:
   - `exportRoutine(routineId)` — with N+1 fix: change the initial `routine_days` query to `select('id, name, estimated_duration_min, sort_order')` (add `id`), then use `day.id` directly for the `routine_blocks` query instead of doing a separate round-trip to get `dayData.id`. The returned JSON shape must remain identical (version 4 format).
   - `importRoutine(jsonData, userId, options)` — direct port, replace `supabase` with `getClient()`.
   - `duplicateRoutine(routineId, userId, newName)` — direct port, it calls `exportRoutine` + `importRoutine` internally.

3. Import `MeasurementType` or any shared lib references needed (check what the functions use from `measurementTypes.js` etc.).

4. Verify the `getClient()` import already exists at the top of `routineApi.js`. If not, add it.

5. **Remove** `exportRoutine`, `importRoutine`, `duplicateRoutine` from `apps/web/src/lib/routineIO.js`. Keep: `downloadRoutineAsJson`, `readJsonFile`, and the existing re-exports from `@gym/shared`. Add re-exports for the three moved functions from `@gym/shared` so that any remaining local imports still resolve:
   ```js
   export { exportRoutine, importRoutine, duplicateRoutine } from '@gym/shared'
   ```

6. **Remove** `exportRoutine`, `importRoutine`, `duplicateRoutine` from `apps/gym-native/src/lib/routineIO.js`. The file should become near-empty (only re-exports from `@gym/shared` if needed, or delete if nothing remains). Check what remains after removing these three functions.

7. `packages/shared/src/index.js` already has `export * from './api/routineApi.js'` — no change needed. The new functions are automatically exported.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run check</automated>
  </verify>
  <done>Three functions live in shared routineApi.js using getClient(), N+1 fix applied (no per-day ID query), both app routineIO.js files are reduced to platform-specific functions + re-exports only, npm run check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumer imports to use @gym/shared directly</name>
  <files>
    apps/web/src/hooks/useRoutines.js,
    apps/web/src/components/Routine/RoutineHeader.jsx,
    apps/web/src/components/Home/NewRoutineFlow.jsx,
    apps/gym-native/src/hooks/useRoutines.js,
    apps/gym-native/src/components/Routine/RoutineHeader.jsx,
    apps/gym-native/src/components/Home/NewRoutineFlow.jsx
  </files>
  <action>
Update consumer files to import the moved functions from `@gym/shared` instead of from the local `routineIO.js`:

1. `apps/web/src/hooks/useRoutines.js`: Change `import { duplicateRoutine } from '../lib/routineIO.js'` to `import { duplicateRoutine } from '@gym/shared'`.

2. `apps/web/src/components/Routine/RoutineHeader.jsx`: Currently imports `{ downloadRoutineAsJson, exportRoutine }` from `routineIO.js`. Split: import `exportRoutine` from `@gym/shared`, keep `downloadRoutineAsJson` from `../../lib/routineIO.js`.

3. `apps/web/src/components/Home/NewRoutineFlow.jsx`: Change `import { importRoutine } from '../../lib/routineIO.js'` to `import { importRoutine } from '@gym/shared'`.

4. `apps/web/src/components/Routine/ImportRoutineModal.jsx`: Confirm it only imports `readJsonFile` — no change needed (stays per-app).

5. `apps/gym-native/src/hooks/useRoutines.js`: Change `import { duplicateRoutine } from '../lib/routineIO.js'` to `import { duplicateRoutine } from '@gym/shared'`.

6. `apps/gym-native/src/components/Routine/RoutineHeader.jsx`: Change `import { exportRoutine } from '../../lib/routineIO'` to `import { exportRoutine } from '@gym/shared'`.

7. `apps/gym-native/src/components/Home/NewRoutineFlow.jsx`: Change `import { importRoutine } from '../../lib/routineIO'` to `import { importRoutine } from '@gym/shared'`.

After all imports updated, the re-exports added in Task 1 to per-app routineIO.js are technically no longer needed by these consumers. However, keep them as safety net in case there are other importers not identified.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run check</automated>
  </verify>
  <done>All consumers import exportRoutine/importRoutine/duplicateRoutine from @gym/shared. No consumer imports these three from per-app routineIO.js. npm run check passes. Platform-specific functions (downloadRoutineAsJson, readJsonFile) still imported from per-app routineIO.js where needed.</done>
</task>

</tasks>

<verification>
- `npm run check` passes (lint + test + build for all workspaces)
- `grep -r "exportRoutine\|importRoutine\|duplicateRoutine" packages/shared/src/api/routineApi.js` shows all three functions
- `grep -r "supabase\." packages/shared/src/api/routineApi.js` returns 0 matches (all use getClient())
- The N+1 pattern is gone: no `routine_days.select('id')` inside a `.map(async` in routineApi.js
</verification>

<success_criteria>
- exportRoutine, importRoutine, duplicateRoutine exist in packages/shared/src/api/routineApi.js
- All three use getClient() instead of direct supabase import
- exportRoutine N+1 fix: routine_days query includes id field, no per-day id query
- Web routineIO.js contains only downloadRoutineAsJson + readJsonFile + re-exports
- RN routineIO.js is near-empty (re-exports only or deleted)
- npm run check green
</success_criteria>

<output>
After completion, create `.planning/phases/02-deduplicar/02-01-SUMMARY.md`
</output>
