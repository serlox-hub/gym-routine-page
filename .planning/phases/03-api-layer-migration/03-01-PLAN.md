---
phase: 03-api-layer-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/api/_client.js
  - packages/shared/src/api/exerciseApi.js
  - packages/shared/src/api/routineApi.js
  - packages/shared/src/api/workoutApi.js
  - packages/shared/src/api/bodyWeightApi.js
  - packages/shared/src/api/bodyMeasurementsApi.js
  - packages/shared/src/api/preferencesApi.js
  - packages/shared/src/api/adminApi.js
  - packages/shared/src/index.js
  - apps/web/src/main.jsx
  - apps/gym-native/App.js
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "7 API files exist in packages/shared/src/api/ (not in apps/web/src/lib/api/)"
    - "No API file imports supabase.js directly — all use getClient()"
    - "initApi(supabase) is called at module level in both app entry points"
    - "Barrel index.js exports all API functions and initApi"
  artifacts:
    - path: "packages/shared/src/api/_client.js"
      provides: "initApi/getClient injection mechanism"
      exports: ["initApi", "getClient"]
    - path: "packages/shared/src/api/exerciseApi.js"
      provides: "Exercise CRUD API functions"
    - path: "packages/shared/src/api/routineApi.js"
      provides: "Routine CRUD API functions"
    - path: "packages/shared/src/api/workoutApi.js"
      provides: "Workout session API functions"
    - path: "packages/shared/src/api/bodyWeightApi.js"
      provides: "Body weight API functions"
    - path: "packages/shared/src/api/bodyMeasurementsApi.js"
      provides: "Body measurements API functions"
    - path: "packages/shared/src/api/preferencesApi.js"
      provides: "Preferences API functions"
    - path: "packages/shared/src/api/adminApi.js"
      provides: "Admin API functions"
    - path: "packages/shared/src/index.js"
      provides: "Barrel with API exports"
  key_links:
    - from: "packages/shared/src/api/*.js"
      to: "packages/shared/src/api/_client.js"
      via: "import { getClient } from './_client.js'"
      pattern: "getClient\\(\\)"
    - from: "apps/web/src/main.jsx"
      to: "packages/shared/src/api/_client.js"
      via: "import { initApi } from '@gym/shared'"
      pattern: "initApi\\(supabase\\)"
    - from: "apps/gym-native/App.js"
      to: "packages/shared/src/api/_client.js"
      via: "import { initApi } from '@gym/shared'"
      pattern: "initApi\\(supabase\\)"
---

<objective>
Move the 7 web API files to `packages/shared/src/api/`, create the `_client.js` injection module, refactor all `supabase` imports to `getClient()`, update the barrel, and wire `initApi()` in both app entry points.

Purpose: Establishes the shared API layer that both apps will consume. After this plan, API functions are platform-agnostic and importable from `@gym/shared`.
Output: 8 files in `packages/shared/src/api/`, updated barrel, both entry points calling `initApi()`.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-layer-migration/03-RESEARCH.md

@apps/web/src/lib/api/exerciseApi.js
@apps/web/src/lib/api/routineApi.js
@apps/web/src/lib/api/workoutApi.js
@apps/web/src/lib/api/bodyWeightApi.js
@apps/web/src/lib/api/bodyMeasurementsApi.js
@apps/web/src/lib/api/preferencesApi.js
@apps/web/src/lib/api/adminApi.js
@packages/shared/src/index.js
@apps/web/src/main.jsx
@apps/gym-native/App.js

<interfaces>
<!-- Current supabase import in every API file (line 1): -->
import { supabase } from '../supabase.js'

<!-- exerciseApi.js also has (line 2) — MUST become relative import: -->
import { MeasurementType } from '@gym/shared'

<!-- Current barrel (packages/shared/src/index.js): only lib/ exports, no api/ yet -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _client.js and move 7 API files with refactored imports</name>
  <files>
    packages/shared/src/api/_client.js
    packages/shared/src/api/exerciseApi.js
    packages/shared/src/api/routineApi.js
    packages/shared/src/api/workoutApi.js
    packages/shared/src/api/bodyWeightApi.js
    packages/shared/src/api/bodyMeasurementsApi.js
    packages/shared/src/api/preferencesApi.js
    packages/shared/src/api/adminApi.js
  </files>
  <action>
    1. Create `packages/shared/src/api/_client.js` with the init/getClient pattern:
       ```js
       let _supabase = null
       export function initApi(supabaseClient) { _supabase = supabaseClient }
       export function getClient() {
         if (!_supabase) throw new Error('[gym/shared] initApi() must be called before using API functions')
         return _supabase
       }
       ```

    2. Move all 7 API files from `apps/web/src/lib/api/` to `packages/shared/src/api/` using `git mv`.

    3. In each moved file, replace `import { supabase } from '../supabase.js'` with `import { getClient } from './_client.js'`.

    4. In each moved file, replace every bare `supabase.` call (e.g., `supabase.from(`, `supabase.rpc(`, `supabase.auth.`) with `getClient().` — specifically `getClient().from(`, `getClient().rpc(`, etc.

    5. CRITICAL: In `exerciseApi.js`, change `import { MeasurementType } from '@gym/shared'` to `import { MeasurementType } from '../lib/measurementTypes.js'` to avoid circular self-import within the package.

    6. Audit all 7 files for any other `@gym/shared` imports and convert them to relative paths within the package.

    7. Do NOT delete `apps/web/src/lib/api/` yet — web hooks still import from there. The delete happens in plan 02 after hook imports are updated.

    IMPORTANT: After `git mv`, the files no longer exist at the old path. Web build WILL fail after this task until plan 02 re-wires imports. This is expected — plan 02 follows immediately.
  </action>
  <verify>
    <automated>ls packages/shared/src/api/*.js | grep -v _client | wc -l | grep -q 7 && grep -r "from.*supabase" packages/shared/src/api/ | grep -v _client || echo "PASS: 7 API files, no supabase imports"</automated>
  </verify>
  <done>7 API files in packages/shared/src/api/, all using getClient() instead of direct supabase import, no circular @gym/shared imports</done>
</task>

<task type="auto">
  <name>Task 2: Update barrel exports and wire initApi in both entry points</name>
  <files>
    packages/shared/src/index.js
    apps/web/src/main.jsx
    apps/gym-native/App.js
  </files>
  <action>
    1. Add API exports to `packages/shared/src/index.js` after the existing lib exports:
       ```js
       // API layer
       export * from './api/exerciseApi.js'
       export * from './api/routineApi.js'
       export * from './api/workoutApi.js'
       export * from './api/bodyWeightApi.js'
       export * from './api/bodyMeasurementsApi.js'
       export * from './api/preferencesApi.js'
       export * from './api/adminApi.js'
       export { initApi } from './api/_client.js'
       ```
       Do NOT export `getClient` — it is internal infrastructure.

    2. In `apps/web/src/main.jsx`, add at module level (before `ReactDOM.createRoot`):
       ```js
       import { supabase } from './lib/supabase.js'
       import { initApi } from '@gym/shared'
       initApi(supabase)
       ```
       Place BEFORE any rendering. The supabase import may already exist — check first.

    3. In `apps/gym-native/App.js`, add at module level (before the App component definition):
       ```js
       import { supabase } from './src/lib/supabase'
       import { initApi } from '@gym/shared'
       initApi(supabase)
       ```
       Place at module scope, not inside the component function. The supabase import may already exist — check first.
  </action>
  <verify>
    <automated>node -e "const idx = require('fs').readFileSync('packages/shared/src/index.js','utf8'); const has = ['exerciseApi','routineApi','workoutApi','bodyWeightApi','bodyMeasurementsApi','preferencesApi','adminApi','initApi'].every(x => idx.includes(x)); console.log(has ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <done>Barrel exports all API functions + initApi. Both app entry points call initApi(supabase) at module level before rendering.</done>
</task>

</tasks>

<verification>
- `ls packages/shared/src/api/*.js | wc -l` returns 8 (7 API + _client)
- `grep -r "from.*supabase" packages/shared/src/api/` returns nothing (all use getClient)
- `grep "initApi" packages/shared/src/index.js` shows the export
- `grep "initApi" apps/web/src/main.jsx` shows the call
- `grep "initApi" apps/gym-native/App.js` shows the call
- `npm run test:run -w apps/web` passes (existing tests unaffected)
</verification>

<success_criteria>
7 API files moved and refactored in packages/shared/src/api/. _client.js provides injection. Barrel exports everything. Both apps call initApi() at startup. No supabase.js import exists in any shared API file.
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-layer-migration/03-01-SUMMARY.md`
</output>
