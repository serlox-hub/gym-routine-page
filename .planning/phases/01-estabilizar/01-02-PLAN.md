---
phase: 01-estabilizar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - DX-03

must_haves:
  truths:
    - "npm run check desde la raiz ejecuta lint, test y build y retorna exit code correcto"
    - "npm run test:shared ejecuta solo los tests de packages/shared, no los de apps/web"
  artifacts:
    - path: "package.json"
      provides: "Root scripts check y test:shared"
      contains: "test:shared"
  key_links:
    - from: "package.json check script"
      to: "lint, test, build"
      via: "sequential && chaining"
      pattern: "npm run lint && npm run test && npm run build"
---

<objective>
Add unified verification scripts to the monorepo root: `check` (full pipeline) and `test:shared` (shared package tests only).

Purpose: Enable a single command to verify the entire monorepo, used as the gate for all subsequent phases.
Output: Root package.json with two new scripts.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-estabilizar/01-RESEARCH.md

@package.json

<interfaces>
Current root package.json scripts:
```json
{
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "native": "npm run start -w apps/gym-native",
    "test": "npm run test:run -w apps/web",
    "lint": "npm run lint -w apps/web && npm run lint -w apps/gym-native"
  }
}
```

Vitest in apps/web/vite.config.js includes shared tests via:
`../../packages/shared/src/**/*.test.js`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add check and test:shared scripts to root package.json (DX-03)</name>
  <files>package.json</files>
  <action>
Add two new scripts to the root `package.json` scripts section:

1. **`check`**: Full verification pipeline running lint, test, and build sequentially:
   ```json
   "check": "npm run lint && npm run test && npm run build"
   ```
   Order: lint first (fast, catches syntax/style), test second (catches logic), build last (catches bundling). Uses `&&` so it fails fast on first error.

2. **`test:shared`**: Run only shared package tests. Start with:
   ```json
   "test:shared": "npm run test:run -w apps/web -- packages/shared/src"
   ```
   This passes `packages/shared/src` as a filter glob to vitest via the web workspace.

After adding the scripts, run `npm run test:shared` to verify it picks up only shared tests (not web app tests). If the glob filter does not work correctly (runs zero tests or includes web tests), use the fallback approach:
   - Add `"test": "vitest run"` to `packages/shared/package.json`
   - Change root script to `"test:shared": "npm run test -w packages/shared"`
   - This requires vitest as a devDependency in packages/shared — add it if needed

3. Run `npm run check` to verify the full pipeline works end-to-end.

Keep existing scripts unchanged. Place `check` after `lint` and `test:shared` after `test` for logical ordering.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared 2>&1 | tail -10 && npm run check 2>&1 | tail -5</automated>
  </verify>
  <done>npm run check exits 0 running lint+test+build. npm run test:shared runs only shared package tests and exits 0.</done>
</task>

</tasks>

<verification>
```bash
# Verify check runs the full pipeline
npm run check

# Verify test:shared runs only shared tests (not web app tests)
npm run test:shared 2>&1 | grep -E "Tests|test files"
# Should show only shared test files, not web app test files
```
</verification>

<success_criteria>
- `npm run check` exits 0 from monorepo root
- `npm run test:shared` exits 0 and runs only packages/shared tests
- Both scripts exist in root package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-estabilizar/01-02-SUMMARY.md`
</output>
