---
phase: 04-cobertura-de-tests
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/shared/src/api/sessionExercisesApi.test.js
  - packages/shared/src/api/routineQueryApi.test.js
  - packages/shared/src/api/routineMutationApi.test.js
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "sessionExercisesApi functions return correct data and throw on errors"
    - "routineQueryApi functions return correct data and throw on errors"
    - "routineMutationApi functions (create, update, delete, composed) work correctly"
  artifacts:
    - path: "packages/shared/src/api/sessionExercisesApi.test.js"
      provides: "Tests for all 10 sessionExercisesApi functions"
      min_lines: 150
    - path: "packages/shared/src/api/routineQueryApi.test.js"
      provides: "Tests for all 6 routineQueryApi functions"
      min_lines: 80
    - path: "packages/shared/src/api/routineMutationApi.test.js"
      provides: "Tests for all 15 routineMutationApi functions"
      min_lines: 200
  key_links:
    - from: "packages/shared/src/api/sessionExercisesApi.test.js"
      to: "packages/shared/src/api/_testUtils.js"
      via: "import { makeQueryMock, makeClientMock }"
      pattern: "import.*_testUtils"
    - from: "packages/shared/src/api/routineQueryApi.test.js"
      to: "packages/shared/src/api/_testUtils.js"
      via: "import { makeQueryMock, makeClientMock }"
      pattern: "import.*_testUtils"
---

<objective>
Write comprehensive tests for sessionExercisesApi, routineQueryApi, and routineMutationApi using the shared _testUtils.js from Plan 01.

Purpose: Cover the session exercises and routine API modules — these are the medium-to-large modules with complex operations (composed mutations, rpc calls, multi-step operations).
Output: Three test files covering 31 functions total.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cobertura-de-tests/04-CONTEXT.md
@.planning/phases/04-cobertura-de-tests/04-RESEARCH.md
@.planning/phases/04-cobertura-de-tests/04-01-SUMMARY.md

@packages/shared/src/api/_testUtils.js
@packages/shared/src/api/sessionExercisesApi.js
@packages/shared/src/api/routineQueryApi.js
@packages/shared/src/api/routineMutationApi.js

<interfaces>
<!-- From _testUtils.js (created in Plan 01): -->
<!-- export function makeQueryMock(resolveWith) { ... } -->
<!-- export function makeClientMock(tableResponses) { ... } -->

<!-- sessionExercisesApi.js (220 lines, 10 functions): -->
<!-- fetchSessionExercises, fetchSessionExercisesSortOrder, fetchSessionExerciseBlockName -->
<!-- updateSessionExerciseSortOrder, insertSessionExercise, deleteCompletedSetsByExercise -->
<!-- updateSessionExerciseExerciseId, addSessionExercise (composed), deleteSessionExercise, reorderSessionExercises (rpc) -->

<!-- routineQueryApi.js (106 lines, 6 functions): -->
<!-- fetchRoutines, fetchRoutine, fetchRoutineDays, fetchRoutineDay, fetchRoutineBlocks, fetchRoutineAllExercises -->

<!-- routineMutationApi.js (322 lines, 15 functions): -->
<!-- createRoutine, createRoutineDay, updateRoutine, deleteRoutine, deleteRoutines -->
<!-- setFavoriteRoutine, updateRoutineDay, deleteRoutineDay, reorderRoutineDays (rpc) -->
<!-- deleteRoutineExercise, updateRoutineExercise, reorderRoutineExercises (composed) -->
<!-- addExerciseToDay (composed), duplicateRoutineExercise (composed), moveRoutineExerciseToDay (composed) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test sessionExercisesApi and routineQueryApi</name>
  <files>packages/shared/src/api/sessionExercisesApi.test.js, packages/shared/src/api/routineQueryApi.test.js</files>
  <action>
**sessionExercisesApi.test.js** — 10 functions:
- Pattern: `vi.mock('./_client.js', ...)`, import from `_testUtils.js`
- fetchSessionExercises: returns array, throws on error
- fetchSessionExercisesSortOrder: returns sort data, throws on error
- fetchSessionExerciseBlockName: returns block name (uses .single), throws on error
- updateSessionExerciseSortOrder: success, throws on error
- insertSessionExercise: returns inserted record (uses .single), throws on error
- deleteCompletedSetsByExercise: success (uses .eq terminal), throws on error
- updateSessionExerciseExerciseId: returns updated record, throws on error
- addSessionExercise: composed function — calls fetchSessionExercisesSortOrder then insertSessionExercise. Test that it calculates correct sort_order. Mock getClient to handle multiple from() calls.
- deleteSessionExercise: success, throws on error
- reorderSessionExercises: uses .rpc — test success, throws on error

**routineQueryApi.test.js** — 6 functions:
- fetchRoutines: returns array ordered, throws on error
- fetchRoutine: returns single routine (uses .single), throws on error
- fetchRoutineDays: returns days array, throws on error
- fetchRoutineDay: returns single day, throws on error
- fetchRoutineBlocks: returns blocks (note: has JS sort after query), throws on error
- fetchRoutineAllExercises: returns exercises, throws on error

For both files: happy path + error + at least one edge case per function.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose 2>&1 | grep -E "(sessionExercisesApi|routineQueryApi|PASS|FAIL)" | head -20</automated>
  </verify>
  <done>sessionExercisesApi.test.js covers 10 functions. routineQueryApi.test.js covers 6 functions. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Test routineMutationApi</name>
  <files>packages/shared/src/api/routineMutationApi.test.js</files>
  <action>
**routineMutationApi.test.js** — 15 functions:
- Pattern: `vi.mock('./_client.js', ...)`, import from `_testUtils.js`

Simple CRUD (happy path + error each):
- createRoutine: .insert.select.single — returns created routine
- createRoutineDay: .insert.select.single — returns created day
- updateRoutine: .update.eq.single — returns updated routine
- deleteRoutine: .delete.eq — success
- deleteRoutines: .delete.in — success
- setFavoriteRoutine: two calls (unset all, set one) — test both succeed
- updateRoutineDay: .update.eq.single — returns updated day
- deleteRoutineDay: .delete.eq — success
- reorderRoutineDays: .rpc — success
- deleteRoutineExercise: .delete.eq — success
- updateRoutineExercise: .update.eq.single — returns updated exercise

Composed operations (these call getClient multiple times — use mockImplementation or mockReturnValue that handles multiple from() calls):
- reorderRoutineExercises: reads current order, updates multiple records — test success flow
- addExerciseToDay: fetches max sort_order, inserts with sort_order+1 — test success
- duplicateRoutineExercise: fetches original, inserts copy — test success
- moveRoutineExerciseToDay: updates block_id, reorders — test success

For composed functions, use `getClient.mockImplementation(() => makeClientMock({...}))` with appropriate per-table responses. Focus on verifying the function calls from() with the right table names and the operations succeed end-to-end.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose 2>&1 | grep -E "(routineMutationApi|PASS|FAIL)" | head -20</automated>
  </verify>
  <done>routineMutationApi.test.js covers all 15 functions with happy path and error tests. Composed operations verify multi-step flows. All pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose
```
All API tests green. All existing tests still pass.
</verification>

<success_criteria>
- sessionExercisesApi.test.js covers 10 functions including composed addSessionExercise
- routineQueryApi.test.js covers 6 functions
- routineMutationApi.test.js covers 15 functions including composed operations
- All use _testUtils.js helpers
- `npm run test:shared` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-cobertura-de-tests/04-04-SUMMARY.md`
</output>
