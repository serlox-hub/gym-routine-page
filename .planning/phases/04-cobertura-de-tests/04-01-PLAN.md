---
phase: 04-cobertura-de-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/api/_testUtils.js
  - packages/shared/src/api/workoutSessionApi.test.js
  - packages/shared/src/api/completedSetsApi.test.js
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "makeQueryMock and makeClientMock are available as shared test helpers"
    - "workoutSessionApi functions return correct data on success and throw on Supabase error"
    - "completedSetsApi functions return correct data on success and throw on Supabase error"
    - "Edge cases (null inputs, empty arrays, missing fields) are covered for both modules"
  artifacts:
    - path: "packages/shared/src/api/_testUtils.js"
      provides: "Shared Supabase mock helpers (makeQueryMock, makeClientMock)"
      exports: ["makeQueryMock", "makeClientMock"]
    - path: "packages/shared/src/api/workoutSessionApi.test.js"
      provides: "Tests for all 12 workoutSessionApi functions"
      min_lines: 200
    - path: "packages/shared/src/api/completedSetsApi.test.js"
      provides: "Tests for all 4 completedSetsApi functions"
      min_lines: 80
  key_links:
    - from: "packages/shared/src/api/_testUtils.js"
      to: "packages/shared/src/api/workoutSessionApi.test.js"
      via: "import { makeQueryMock, makeClientMock }"
      pattern: "import.*_testUtils"
    - from: "packages/shared/src/api/_testUtils.js"
      to: "packages/shared/src/api/completedSetsApi.test.js"
      via: "import { makeQueryMock, makeClientMock }"
      pattern: "import.*_testUtils"
---

<objective>
Create shared test utilities and write comprehensive tests for the workout session and completed sets API modules.

Purpose: Establish the _testUtils.js foundation that all subsequent API test plans depend on, and cover the most critical API module (workoutSessionApi — 304 lines, 12 functions) plus completedSetsApi (76 lines, 4 functions).
Output: _testUtils.js with makeQueryMock/makeClientMock, plus test files for workoutSessionApi and completedSetsApi.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cobertura-de-tests/04-CONTEXT.md
@.planning/phases/04-cobertura-de-tests/04-RESEARCH.md

@packages/shared/src/api/_client.js
@packages/shared/src/api/workoutSessionApi.js
@packages/shared/src/api/completedSetsApi.js
@packages/shared/src/api/routineApi.test.js

<interfaces>
<!-- Extract makeQueryMock from routineApi.test.js and expand it in _testUtils.js -->
<!-- The expanded version from RESEARCH.md adds: neq, not, is, gte, lte, limit, range, update, insert, delete, upsert -->
<!-- makeClientMock wraps makeQueryMock with per-table response discrimination via from() -->

Key terminal calls used by workoutSessionApi functions:
- fetchActiveSession: .maybeSingle()
- fetchCompletedSetsForSession: .order()
- startWorkoutSession: .rpc()
- fetchExerciseIdsWithSets: .eq() terminal
- deleteSessionExercisesWithoutSets: .not() terminal
- completeWorkoutSession: .update().eq().single()
- deleteWorkoutSession: .eq() terminal (delete chain)
- fetchWorkoutHistory: .order() terminal
- fetchSessionDetail: .single()
- fetchExerciseHistorySummary: .order() terminal
- fetchExerciseHistory: .range() terminal
- fetchPreviousWorkout: .order() terminal

Key terminal calls used by completedSetsApi functions:
- upsertCompletedSet: .single()
- updateSetVideo: .single()
- updateSetDetails: .eq() terminal (no return value used)
- deleteCompletedSet: .eq() terminal (no return value used)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _testUtils.js and test workoutSessionApi</name>
  <files>packages/shared/src/api/_testUtils.js, packages/shared/src/api/workoutSessionApi.test.js</files>
  <action>
1. Create `packages/shared/src/api/_testUtils.js` with the expanded `makeQueryMock` and `makeClientMock` helpers as specified in 04-RESEARCH.md Pattern 1. The helper must:
   - Mock ALL chainable methods as `.mockReturnThis()`: select, eq, neq, order, in, not, is, gte, lte, limit, range, update, insert, delete, upsert
   - Mock ALL terminal methods as `.mockResolvedValue(resolveWith)`: single, maybeSingle
   - Also make non-terminal methods resolve directly for cases where they ARE terminal: chain.order.mockResolvedValue, chain.in.mockResolvedValue, chain.eq.mockResolvedValue, chain.not.mockResolvedValue
   - `makeClientMock(tableResponses)` returns `{ from: vi.fn((table) => ...), rpc: vi.fn().mockResolvedValue(...) }`

2. Create `packages/shared/src/api/workoutSessionApi.test.js` with comprehensive tests for all 12 functions:
   - fetchActiveSession (uses .maybeSingle — test: returns session, returns null when none, throws on error)
   - fetchCompletedSetsForSession (uses .order — test: returns sets array, empty array, throws on error)
   - startWorkoutSession (uses .rpc — test: returns session data, throws on error. Use `rpc` mock from makeClientMock)
   - fetchExerciseIdsWithSets (test: returns exercise ids, empty result)
   - deleteSessionExercisesWithoutSets (uses .not terminal — test: success, throws on error)
   - completeWorkoutSession (uses .update.eq.single — test: returns updated session, throws on error)
   - deleteWorkoutSession (uses .delete.eq — test: success, throws on error)
   - fetchWorkoutHistory (uses .order terminal — test: returns sessions array, empty, throws on error)
   - fetchSessionDetail (uses .single — test: returns detail, throws on error)
   - fetchExerciseHistorySummary (test: returns summary data, throws on error)
   - fetchExerciseHistory (uses .range — test: returns paginated history, throws on error)
   - fetchPreviousWorkout (test: returns previous workout data, null when none, throws on error)

   Pattern: `vi.mock('./_client.js', () => ({ getClient: vi.fn() }))`, import getClient, use makeClientMock/makeQueryMock in each test. `beforeEach(() => { vi.clearAllMocks() })`.

   IMPORTANT: Do NOT modify `routineApi.test.js`. It keeps its local makeQueryMock. New test files import from `_testUtils.js`.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <done>_testUtils.js exists with exported makeQueryMock and makeClientMock. workoutSessionApi.test.js has tests for all 12 functions covering happy path, error handling, and edge cases. All tests pass. routineApi.test.js still passes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Test completedSetsApi</name>
  <files>packages/shared/src/api/completedSetsApi.test.js</files>
  <action>
Create `packages/shared/src/api/completedSetsApi.test.js` with comprehensive tests for all 4 functions:
- upsertCompletedSet (uses .upsert.single — test: returns upserted set, throws on error, handles null fields)
- updateSetVideo (uses .update.eq.single — test: returns updated set, throws on error)
- updateSetDetails (uses .update.eq — test: success with no return value, throws on error)
- deleteCompletedSet (uses .delete.eq — test: success, throws on error)

Pattern: Same as Task 1. Import from `_testUtils.js`. Mock `_client.js`. Each function gets describe block with happy path + error + edge case tests.

Edge cases to cover: null video URL in updateSetVideo, partial update in updateSetDetails (only some fields provided).
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>completedSetsApi.test.js covers all 4 functions with happy path, error, and edge case tests. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose
```
All existing tests (routineApi.test.js, useWorkoutHistory.test.js, useCompletedSets.test.js, createWorkoutStore.test.js) still pass. New tests all green.
</verification>

<success_criteria>
- _testUtils.js exports makeQueryMock and makeClientMock
- workoutSessionApi.test.js covers 12 functions with ~80%+ coverage
- completedSetsApi.test.js covers 4 functions with ~80%+ coverage
- All existing tests remain green
- `npm run test:shared` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-cobertura-de-tests/04-01-SUMMARY.md`
</output>
