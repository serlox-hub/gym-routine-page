---
phase: 03-api-layer-migration
plan: 04
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - apps/gym-native/src/hooks/useSession.js
  - apps/gym-native/src/hooks/useCompletedSets.js
  - apps/gym-native/src/hooks/useSessionExercises.js
  - packages/shared/src/api/workoutApi.js
  - packages/shared/src/index.js
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "useSession.js calls shared API functions for all Supabase data operations"
    - "useCompletedSets.js calls shared API functions for all set CRUD (retry/queue/optimistic logic stays in hook)"
    - "useSessionExercises.js calls shared API functions for session exercise CRUD"
    - "No import { supabase } remains in any of these 3 RN hook files"
    - "addSessionExercise function exists in shared workoutApi.js with sort-order shifting logic"
  artifacts:
    - path: "apps/gym-native/src/hooks/useSession.js"
      provides: "RN session hooks using shared API"
    - path: "apps/gym-native/src/hooks/useCompletedSets.js"
      provides: "RN completed sets hooks using shared API"
    - path: "apps/gym-native/src/hooks/useSessionExercises.js"
      provides: "RN session exercises hooks using shared API"
    - path: "packages/shared/src/api/workoutApi.js"
      provides: "New addSessionExercise function with sort-order logic"
      contains: "addSessionExercise"
  key_links:
    - from: "apps/gym-native/src/hooks/useSession.js"
      to: "packages/shared/src/api/workoutApi.js"
      via: "import { fetchActiveSession, ... } from '@gym/shared'"
      pattern: "from '@gym/shared'"
    - from: "apps/gym-native/src/hooks/useCompletedSets.js"
      to: "packages/shared/src/api/workoutApi.js"
      via: "import { upsertCompletedSet, ... } from '@gym/shared'"
      pattern: "from '@gym/shared'"
    - from: "apps/gym-native/src/hooks/useSessionExercises.js"
      to: "packages/shared/src/api/workoutApi.js"
      via: "import { addSessionExercise, ... } from '@gym/shared'"
      pattern: "addSessionExercise"
---

<objective>
Refactor the 3 complex RN hook files (useSession, useCompletedSets, useSessionExercises) from inline Supabase queries to shared API calls. Add `addSessionExercise` to shared workoutApi.js for the sort-order shifting logic.

Purpose: Completes the RN side of the API migration. These hooks have complex behavior (optimistic updates, pending set queues, superset sort-order) that stays in the hook layer, but the underlying data operations move to the shared API.
Output: 3 RN hook files fully refactored, new `addSessionExercise` in shared API, zero inline Supabase queries remaining in any RN hook.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-api-layer-migration/03-RESEARCH.md
@.planning/phases/03-api-layer-migration/03-01-SUMMARY.md

@apps/gym-native/src/hooks/useSession.js
@apps/gym-native/src/hooks/useCompletedSets.js
@apps/gym-native/src/hooks/useSessionExercises.js
@packages/shared/src/api/workoutApi.js

<interfaces>
<!-- workoutApi.js existing exports (from plan 01): -->
<!-- fetchWorkoutHistory, fetchSessionDetail, fetchExerciseHistory, fetchPreviousWorkout -->
<!-- startWorkoutSession, fetchActiveSession, fetchCompletedSetsForSession -->
<!-- completeWorkoutSession, deleteWorkoutSession -->
<!-- upsertCompletedSet, updateSetVideo, updateSetDetails, deleteCompletedSet -->
<!-- insertSessionExercise, deleteSessionExercise, reorderSessionExercises -->
<!-- fetchExerciseIdsWithSets, deleteSessionExercisesWithoutSets -->
<!-- fetchSessionExercises -->

<!-- NEW function to add: addSessionExercise (sort-order shifting for superset insertion) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add addSessionExercise to workoutApi.js and refactor useSessionExercises.js</name>
  <files>
    packages/shared/src/api/workoutApi.js
    apps/gym-native/src/hooks/useSessionExercises.js
  </files>
  <action>
    1. Read `apps/gym-native/src/hooks/useSessionExercises.js` carefully. Identify the `useAddSessionExercise` mutation's inline logic — it:
       - Fetches current session exercises to determine sort order
       - Handles superset insertion by shifting sort orders of subsequent exercises
       - Inserts the new session exercise with computed sort order

    2. Extract the data-access portion of this logic into a new `addSessionExercise` function in `packages/shared/src/api/workoutApi.js`. The function should accept `{ sessionId, exercise, series, reps, rir, rest_seconds, notes, tempo, superset_group }` (match the RN hook's existing parameter shape). It should:
       - Fetch current session exercises for the session
       - Compute the insert sort order (handling superset positioning)
       - Shift subsequent exercise sort orders if needed
       - Insert the new session exercise
       - Return the inserted record

    3. Keep `insertSessionExercise` (the simpler web version) as-is — web callers pass a pre-computed `sortOrder`. Both functions coexist.

    4. Refactor `useSessionExercises.js`:
       - Remove `import { supabase } from '../lib/supabase.js'`
       - Add `import { addSessionExercise, fetchSessionExercises, deleteSessionExercise, reorderSessionExercises, insertSessionExercise } from '@gym/shared'`
       - Replace `useSessionExercises` query's inline block with `queryFn: () => fetchSessionExercises(sessionId)`
       - Replace `useAddSessionExercise` mutation's inline block with call to `addSessionExercise`
       - Replace `useReplaceSessionExercise` inline logic with calls to `deleteSessionExercise` + `insertSessionExercise`
       - Replace `useRemoveSessionExercise` inline with `deleteSessionExercise`
       - Replace `useReorderSessionExercises` inline with `reorderSessionExercises`

    5. Verify no `supabase` import remains in useSessionExercises.js.

    6. Update barrel if `addSessionExercise` is a new export (it should be auto-exported via `export *` from workoutApi.js).
  </action>
  <verify>
    <automated>grep -l "from.*supabase" apps/gym-native/src/hooks/useSessionExercises.js 2>/dev/null || echo "PASS" && grep "addSessionExercise" packages/shared/src/api/workoutApi.js | head -1</automated>
  </verify>
  <done>addSessionExercise function in shared workoutApi.js. useSessionExercises.js fully refactored to shared API calls. Zero supabase imports.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor useSession.js and useCompletedSets.js</name>
  <files>
    apps/gym-native/src/hooks/useSession.js
    apps/gym-native/src/hooks/useCompletedSets.js
  </files>
  <action>
    **useSession.js (200 lines):**
    1. Remove `import { supabase } from '../lib/supabase.js'`
    2. Add `import { fetchActiveSession, fetchCompletedSetsForSession, startWorkoutSession, completeWorkoutSession, deleteWorkoutSession, fetchExerciseIdsWithSets, deleteSessionExercisesWithoutSets } from '@gym/shared'`
    3. Replace inline queries:
       - `fetchActiveSession` local function → call shared `fetchActiveSession(userId)` (or `fetchActiveSession` if it reads userId internally). Check the shared function signature.
       - `fetchCompletedSets` local function → call shared `fetchCompletedSetsForSession(sessionId)`
       - `useStartSession` mutation → call shared `startWorkoutSession(routineId)`
       - `useEndSession` inline multi-table operations → call shared `completeWorkoutSession(sessionId)` then `fetchExerciseIdsWithSets(sessionId)` then `deleteSessionExercisesWithoutSets(sessionId, exerciseIds)`. If the shared API already orchestrates these steps in one function, use that. If not, chain the calls in the mutationFn.
       - `useAbandonSession` → call shared `deleteWorkoutSession(sessionId)`
    4. Keep all hook-layer behavior (cache invalidation, optimistic updates, AppState sync) in the hook.

    **useCompletedSets.js (240 lines):**
    1. Remove `import { supabase } from '../lib/supabase.js'`
    2. Add `import { upsertCompletedSet, updateSetVideo, updateSetDetails, deleteCompletedSet } from '@gym/shared'`
    3. Replace inline queries:
       - `useCompleteSet` mutation's `supabase.from('completed_sets').upsert(...)` → call `upsertCompletedSet(setData)`
       - `useSyncPendingSets` same upsert → call `upsertCompletedSet(setData)` in the sync loop
       - `useUpdateSetVideo` → call `updateSetVideo(setId, videoUri)`
       - `useUpdateSetDetails` → call `updateSetDetails(setId, details)`
       - `useUncompleteSet` → call `deleteCompletedSet(setId)`
    4. CRITICAL: Keep the retry/queue/optimistic logic in the hook. The pending set queue (`pendingQueue`), `AppState` sync listener, and optimistic cache updates are hook-layer concerns. Only the actual Supabase `.upsert()` / `.update()` / `.delete()` calls move to shared API functions.
    5. Verify parameter shapes match. RN mutations may pass raw set objects. Adapt with thin wrappers if the shared API expects different parameters.

    After both files, verify no `supabase` import remains.
  </action>
  <verify>
    <automated>grep -l "from.*supabase" apps/gym-native/src/hooks/useSession.js apps/gym-native/src/hooks/useCompletedSets.js 2>/dev/null || echo "PASS: no supabase in session/completedSets"</automated>
  </verify>
  <done>useSession.js and useCompletedSets.js fully refactored to shared API calls. Retry/queue/optimistic logic preserved in hooks. Zero supabase imports remain.</done>
</task>

<task type="auto">
  <name>Task 3: Final validation — zero inline Supabase queries in all RN hooks</name>
  <files></files>
  <action>
    1. Run comprehensive check: `grep -r "from.*supabase" apps/gym-native/src/hooks/` — should return NOTHING. The only RN files allowed to import supabase are stores (`src/stores/`) and the supabase client itself (`src/lib/supabase.js`).

    2. Run web tests: `npm run test:run -w apps/web` — shared API functions must not break existing tests.

    3. Run web build: `npm run build -w apps/web` — full build verification.

    4. Quick sanity: `node -e "const idx = require('fs').readFileSync('packages/shared/src/index.js','utf8'); console.log(idx.includes('initApi') && idx.includes('exerciseApi') && idx.includes('workoutApi') ? 'BARREL OK' : 'BARREL MISSING')"` — barrel exports intact.

    If any `supabase` import remains in RN hooks, fix it. If any test fails, diagnose and fix.
  </action>
  <verify>
    <automated>grep -r "from.*supabase" apps/gym-native/src/hooks/ && echo "FAIL: supabase imports remain" || echo "PASS: zero supabase in RN hooks" && npm run test:run -w apps/web && npm run build -w apps/web</automated>
  </verify>
  <done>Zero inline Supabase queries in any RN hook file. All web tests pass. Web build succeeds. Phase 3 migration complete.</done>
</task>

</tasks>

<verification>
- `grep -r "from.*supabase" apps/gym-native/src/hooks/` returns nothing
- `grep -r "from.*supabase" packages/shared/src/api/` returns nothing
- `grep -r "lib/api/" apps/web/src/hooks/` returns nothing
- `npm run test:run -w apps/web` passes
- `npm run build -w apps/web` succeeds
- `grep "addSessionExercise" packages/shared/src/api/workoutApi.js` confirms new function exists
</verification>

<success_criteria>
All 3 complex RN hook files refactored. addSessionExercise added to shared API. Zero supabase imports in any RN hook. All tests and builds pass. Phase 3 requirements (API-01 through API-04) fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-layer-migration/03-04-SUMMARY.md`
</output>
