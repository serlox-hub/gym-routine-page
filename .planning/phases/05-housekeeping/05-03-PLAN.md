---
phase: 05-housekeeping
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - package-lock.json
  - CLAUDE.md
autonomous: true
requirements:
  - DX-01
  - DX-02

must_haves:
  truths:
    - "supabase-js, tanstack-query, and zustand have the same version in both apps"
    - "All 749+ tests pass after dependency upgrades"
    - "CLAUDE.md documents monorepo structure (apps/web, apps/gym-native, packages/shared)"
    - "CLAUDE.md documents @gym/shared import pattern and barrel exports"
    - "CLAUDE.md documents initApi/initStores/initNotifications injection layer"
    - "CLAUDE.md documents shared + thin wrapper pattern for hooks"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Updated dependency versions matching apps/gym-native"
    - path: "CLAUDE.md"
      provides: "Accurate monorepo documentation"
      contains: "@gym/shared"
  key_links:
    - from: "CLAUDE.md"
      to: "packages/shared/src/index.js"
      via: "documents the barrel import pattern"
      pattern: "@gym/shared"
---

<objective>
Sync dependency versions across apps and update CLAUDE.md to reflect monorepo reality.

Purpose: Eliminate version drift between apps and ensure project documentation matches actual architecture.
Output: Aligned dependency versions, accurate CLAUDE.md.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-housekeeping/05-RESEARCH.md

@apps/web/package.json
@apps/gym-native/package.json
@CLAUDE.md
@packages/shared/src/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync dependency versions between web and RN</name>
  <files>apps/web/package.json, package-lock.json</files>
  <action>
Upgrade the web app's three shared dependencies to match the RN app's versions. Upgrade one at a time, running `npm run check` between each.

**Step 1 — tanstack-query** (biggest gap: 5.62 -> 5.90):
```bash
cd apps/web && npm install @tanstack/react-query@^5.90.21
cd ../.. && npm run check
```

**Step 2 — supabase-js** (2.49 -> 2.98):
```bash
cd apps/web && npm install @supabase/supabase-js@^2.98.0
cd ../.. && npm run check
```

**Step 3 — zustand** (5.0.2 -> 5.0.11):
```bash
cd apps/web && npm install zustand@^5.0.11
cd ../.. && npm run check
```

If any `npm run check` fails after an upgrade:
1. Read the error output carefully
2. If it's a test failure, check if the test expectation needs updating for the new version behavior
3. If it's a breaking API change, fix the consuming code
4. Do NOT proceed to the next upgrade until the current one passes check

After all three upgrades, verify final alignment by comparing versions:
```bash
grep -E '"(@supabase/supabase-js|@tanstack/react-query|zustand)"' apps/web/package.json apps/gym-native/package.json
```
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run check</automated>
  </verify>
  <done>
    - @tanstack/react-query is ^5.90.21 in both apps
    - @supabase/supabase-js is ^2.98.0 in both apps
    - zustand is ^5.0.11 in both apps
    - `npm run check` passes (lint + test + build)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CLAUDE.md for monorepo architecture</name>
  <files>CLAUDE.md</files>
  <action>
Rewrite CLAUDE.md to reflect the current monorepo architecture. This is an ADDITIVE rewrite: keep all conventions that are still accurate, replace/add sections for monorepo specifics.

**Sections to KEEP as-is** (still accurate):
- Naming Conventions table
- Code Standards > Language section
- Component Rules (one per file, max 150 lines, etc.)
- Hooks Organization (the comment-section pattern, naming conventions)
- State Management pattern (TanStack Query + useState + Zustand)
- Error Handling
- Styling (Tailwind)
- Database Schema
- Git Commits (Spanish, no AI attribution)
- What NOT to Do / What TO Do lists
- Component Architecture: Dumb Components + Testable Utils (entire section)
- Utility File Organization table and guidelines

**Sections to REPLACE:**
- Project Overview: Add monorepo context. "Monorepo with web (React + Vite) and mobile (Expo + NativeWind) apps sharing business logic via @gym/shared."
- Tech Stack: Add @gym/shared, Expo, NativeWind. Note shared deps are synced.
- Project Structure: Replace single-app `src/` tree with monorepo tree:
  ```
  gym-routine-page/
  ├── apps/
  │   ├── web/src/          # React + Vite (same structure as before, under apps/web/)
  │   └── gym-native/src/   # Expo + NativeWind
  └── packages/
      └── shared/src/       # @gym/shared
          ├── api/           # Supabase API functions (initApi pattern)
          ├── hooks/         # Shared React hooks
          ├── stores/        # Zustand store factories (initStores pattern)
          └── lib/           # Pure utility functions
  ```
- Testing section: Update commands for monorepo (`npm run check`, `npm run test:shared`, `npm run test:run -w apps/web`)

**Sections to ADD (new):**
- **Monorepo Architecture** section covering:
  1. `@gym/shared` barrel import pattern: `import { useRoutines, exportRoutine } from '@gym/shared'`
  2. Injection layer: `initApi({ supabaseUrl, supabaseKey })`, `initStores({ authStore, workoutStore })`, `initNotifications(showToast)` — called at app startup
  3. Shared + thin wrapper pattern: shared hooks in packages/shared, per-app thin wrappers that re-export and add platform-specific behavior
  4. Callback injection for platform-specific behavior: `onVisibilityChange`, `onConnectivityChange`, `onStartError` callbacks

- **Imports** section update: Add @gym/shared import pattern alongside existing index file pattern

- **When modifying shared code** checklist:
  1. Run `npm run test:shared` for shared logic
  2. Run `npm run check` to verify both apps still build
  3. If adding exports, update `packages/shared/src/index.js` barrel

Read the existing CLAUDE.md first, then the actual monorepo structure (packages/shared/src/index.js, a sample thin wrapper), to ensure documentation accuracy. Keep the document concise — this is a reference for Claude, not a tutorial.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && grep -c "@gym/shared" CLAUDE.md && grep -c "initApi" CLAUDE.md && grep -c "monorepo\|Monorepo" CLAUDE.md && grep -c "packages/shared" CLAUDE.md</automated>
  </verify>
  <done>
    - CLAUDE.md mentions @gym/shared at least 3 times
    - CLAUDE.md documents initApi/initStores/initNotifications
    - CLAUDE.md has monorepo project structure tree
    - CLAUDE.md documents shared + thin wrapper pattern
    - All existing valid conventions preserved
  </done>
</task>

</tasks>

<verification>
- `npm run check` passes (all tests green after dep upgrades)
- `grep -E '"(@supabase|@tanstack|zustand)"' apps/web/package.json apps/gym-native/package.json` shows matching versions
- CLAUDE.md contains monorepo structure, @gym/shared pattern, injection layer docs
</verification>

<success_criteria>
Dependencies aligned across apps with all tests passing. CLAUDE.md accurately documents monorepo architecture, shared imports, injection layer, and thin wrapper pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/05-housekeeping/05-03-SUMMARY.md`
</output>
