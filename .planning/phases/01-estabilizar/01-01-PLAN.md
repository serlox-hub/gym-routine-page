---
phase: 01-estabilizar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx
  - apps/gym-native/src/lib/routineIO.js
autonomous: true
requirements:
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "WorkoutExerciseCard (RN) no tiene hooks llamados despues de un return condicional"
    - "readJsonFile y downloadRoutineAsJson no existen en el modulo RN routineIO.js"
    - "No hay comentarios eslint-disable para react-hooks/rules-of-hooks ni no-undef en los archivos modificados"
  artifacts:
    - path: "apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx"
      provides: "RegularExerciseCard extraido como componente a nivel de modulo"
      contains: "function RegularExerciseCard"
    - path: "apps/gym-native/src/lib/routineIO.js"
      provides: "Modulo sin dead code de FileReader ni downloadRoutineAsJson"
  key_links:
    - from: "WorkoutExerciseCard"
      to: "RegularExerciseCard"
      via: "render delegation after isWarmup check"
      pattern: "return <RegularExerciseCard"
---

<objective>
Fix two active crash risks in React Native: conditional hooks violation in WorkoutExerciseCard and dead FileReader code in routineIO.js.

Purpose: Eliminate runtime crash surface before any refactoring work begins.
Output: Two clean files passing lint without eslint-disable workarounds.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-estabilizar/01-RESEARCH.md

@apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx
@apps/gym-native/src/lib/routineIO.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract RegularExerciseCard to fix hooks violation (BUG-01)</name>
  <files>apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx</files>
  <action>
Extract a new `RegularExerciseCard` function component at module scope (same level as `WarmupExerciseCard`, NOT inside `WorkoutExerciseCard`). Move all logic from line 121 onwards (after the `if (isWarmup)` early return) into this new component:

1. Define `function RegularExerciseCard({ ... })` at module scope, before `WorkoutExerciseCard`.
2. Move into it: `isCompleted` computation, `prevCompletedRef = useRef(isCompleted)`, the auto-collapse `useEffect`, `menuItems`, and all JSX (the `content` variable through the modals at the end).
3. Pass all needed values as props from `WorkoutExerciseCard`: `sessionExercise`, `onCompleteSet`, `onUncompleteSet`, `onRemove`, `onReplace`, `isSuperset`, `onReorder`, `currentIndex`, `totalExercises`, `positionLabels`, `isReordering`, plus the computed values it needs: `exerciseKey`, `setsCount`, `completedCount`, `previousWorkout`, `routineDayId`, `measurementType`, `weightUnit`, `timeUnit`, `distanceUnit`, `tempoRazon`. Alternatively, compute these inside `RegularExerciseCard` to keep the props list cleaner (preferred — move the store selectors and usePreviousWorkout into the new component).
4. In `WorkoutExerciseCard`, keep only: the early destructuring, the `isWarmup` check returning `<WarmupExerciseCard>`, and a default return of `<RegularExerciseCard>` passing through all props.
5. Remove ALL `eslint-disable-line react-hooks/rules-of-hooks` comments from the file.
6. Do NOT wrap `RegularExerciseCard` in `memo` — only the outer `WorkoutExerciseCard` stays wrapped in `memo` as it already is.

Key constraint: `RegularExerciseCard` must be at module scope so React sees a stable component identity. If defined inside `WorkoutExerciseCard`, it would remount on every render.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run lint -w apps/gym-native 2>&1 | head -30</automated>
  </verify>
  <done>RN WorkoutExerciseCard has zero hooks after any conditional return. No eslint-disable comments for hooks rules remain. Lint passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead code from RN routineIO.js (BUG-02)</name>
  <files>apps/gym-native/src/lib/routineIO.js</files>
  <action>
Delete two dead-code functions from `apps/gym-native/src/lib/routineIO.js`:

1. Delete `downloadRoutineAsJson` (lines 136-143, including the JSDoc comment above it). This function throws unconditionally and is never imported anywhere in the RN app.
2. Delete `readJsonFile` (lines 328-345, including the JSDoc comment above it). This function uses `FileReader` (browser-only API) which would crash in RN. It is never imported anywhere in the RN app.
3. Remove any `eslint-disable no-undef` comments that were associated with these functions.
4. Do NOT touch any other functions in the file — `exportRoutine`, `importRoutine`, `duplicateRoutine`, `shareRoutineAsJson` etc. must remain untouched.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run lint -w apps/gym-native 2>&1 | head -30</automated>
  </verify>
  <done>readJsonFile and downloadRoutineAsJson do not exist in the file. No eslint-disable no-undef comments remain. Lint passes clean.</done>
</task>

</tasks>

<verification>
Both verifications combined:
```bash
npm run lint -w apps/gym-native && npm run lint -w apps/web
```
Lint must pass without any eslint-disable comments for react-hooks/rules-of-hooks or no-undef in the modified files.

Additional check: grep for the deleted functions to confirm removal:
```bash
grep -n "readJsonFile\|downloadRoutineAsJson" apps/gym-native/src/lib/routineIO.js
# Should return nothing
grep -rn "eslint-disable.*rules-of-hooks" apps/gym-native/src/components/Workout/WorkoutExerciseCard.jsx
# Should return nothing
```
</verification>

<success_criteria>
- RN lint passes with zero hooks-related warnings or errors
- readJsonFile and downloadRoutineAsJson are gone from RN routineIO.js
- No eslint-disable workaround comments remain in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/01-estabilizar/01-01-SUMMARY.md`
</output>
