---
phase: 03-api-layer-migration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/hooks/useAdmin.js
  - apps/web/src/hooks/useBodyMeasurements.js
  - apps/web/src/hooks/useBodyWeight.js
  - apps/web/src/hooks/useCompletedSets.js
  - apps/web/src/hooks/useExercises.js
  - apps/web/src/hooks/usePreferences.js
  - apps/web/src/hooks/useRoutines.js
  - apps/web/src/hooks/useSession.js
  - apps/web/src/hooks/useSessionExercises.js
  - apps/web/src/hooks/useWorkoutHistory.js
  - apps/web/src/hooks/useAuth.js
autonomous: true
requirements:
  - API-03

must_haves:
  truths:
    - "All web hooks import API functions from @gym/shared, not from ../lib/api/"
    - "Web app builds with no unresolved imports"
    - "apps/web/src/lib/api/ directory is deleted"
  artifacts:
    - path: "apps/web/src/hooks/useExercises.js"
      provides: "Web exercise hooks importing from @gym/shared"
      contains: "from '@gym/shared'"
    - path: "apps/web/src/hooks/useRoutines.js"
      provides: "Web routine hooks importing from @gym/shared"
      contains: "from '@gym/shared'"
  key_links:
    - from: "apps/web/src/hooks/*.js"
      to: "packages/shared/src/api/*.js"
      via: "import { ... } from '@gym/shared'"
      pattern: "from '@gym/shared'"
---

<objective>
Update all web hook files to import API functions from `@gym/shared` instead of `../lib/api/`, then delete the old `apps/web/src/lib/api/` directory.

Purpose: Completes the web side of the migration. Web hooks now consume the shared API layer.
Output: 10 web hook files with updated imports, old api/ directory deleted, web build green.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-api-layer-migration/03-RESEARCH.md
@.planning/phases/03-api-layer-migration/03-01-SUMMARY.md

<interfaces>
<!-- Web hooks currently import like this: -->
<!-- useAdmin.js:      import { fetchAllUsers, updateUserSetting as apiUpdateUserSetting } from '../lib/api/adminApi.js' -->
<!-- useBodyMeasurements.js: import { ... } from '../lib/api/bodyMeasurementsApi.js' -->
<!-- useBodyWeight.js:       import { ... } from '../lib/api/bodyWeightApi.js' -->
<!-- useCompletedSets.js:    import { ... } from '../lib/api/workoutApi.js' -->
<!-- useExercises.js:        import { ... } from '../lib/api/exerciseApi.js' -->
<!-- usePreferences.js:      import { fetchPreferences, upsertPreference } from '../lib/api/preferencesApi.js' -->
<!-- useRoutines.js:         import { ... } from '../lib/api/routineApi.js' -->
<!-- useSession.js:          import { ... } from '../lib/api/workoutApi.js' -->
<!-- useSessionExercises.js: import { ... } from '../lib/api/workoutApi.js' -->
<!-- useWorkoutHistory.js:   import { ... } from '../lib/api/workoutApi.js' -->

<!-- After plan 01, all these functions are exported from @gym/shared barrel -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all web hook imports from ../lib/api/ to @gym/shared</name>
  <files>
    apps/web/src/hooks/useAdmin.js
    apps/web/src/hooks/useBodyMeasurements.js
    apps/web/src/hooks/useBodyWeight.js
    apps/web/src/hooks/useCompletedSets.js
    apps/web/src/hooks/useExercises.js
    apps/web/src/hooks/usePreferences.js
    apps/web/src/hooks/useRoutines.js
    apps/web/src/hooks/useSession.js
    apps/web/src/hooks/useSessionExercises.js
    apps/web/src/hooks/useWorkoutHistory.js
    apps/web/src/hooks/useAuth.js
  </files>
  <action>
    1. In each of the 10 web hook files listed below, replace the `from '../lib/api/XXXApi.js'` import path with `from '@gym/shared'`. Keep the same named imports, only change the source path.

       Files and their current API import sources:
       - `useAdmin.js` → `../lib/api/adminApi.js` becomes `@gym/shared`
       - `useBodyMeasurements.js` → `../lib/api/bodyMeasurementsApi.js` becomes `@gym/shared`
       - `useBodyWeight.js` → `../lib/api/bodyWeightApi.js` becomes `@gym/shared`
       - `useCompletedSets.js` → `../lib/api/workoutApi.js` becomes `@gym/shared`
       - `useExercises.js` → `../lib/api/exerciseApi.js` becomes `@gym/shared`
       - `usePreferences.js` → `../lib/api/preferencesApi.js` becomes `@gym/shared`
       - `useRoutines.js` → `../lib/api/routineApi.js` becomes `@gym/shared`
       - `useSession.js` → `../lib/api/workoutApi.js` becomes `@gym/shared`
       - `useSessionExercises.js` → `../lib/api/workoutApi.js` becomes `@gym/shared`
       - `useWorkoutHistory.js` → `../lib/api/workoutApi.js` becomes `@gym/shared`

    2. CHECK `useAuth.js`: If it has an inline Supabase query for user_settings, add `fetchUserSettings` import from `@gym/shared` and use it. If plan 01 added `fetchUserSettings` to `adminApi.js`, import it from `@gym/shared`. If not, note it for plan 03.

    3. If any hook file already imports other things from `@gym/shared` (e.g., utils from Phase 2), merge the API function imports into the same `@gym/shared` import statement to keep imports clean.

    4. After all imports are updated, verify the web build: `npm run build -w apps/web`.

    5. Once build passes, delete the old directory: `rm -rf apps/web/src/lib/api/`.

    6. Verify build still passes after deletion (confirms no lingering references).
  </action>
  <verify>
    <automated>grep -r "lib/api/" apps/web/src/hooks/ || echo "PASS: no old api imports" && npm run build -w apps/web</automated>
  </verify>
  <done>All web hook files import API functions from @gym/shared. Old apps/web/src/lib/api/ directory deleted. Web build passes cleanly.</done>
</task>

</tasks>

<verification>
- `grep -r "lib/api/" apps/web/src/hooks/` returns nothing
- `ls apps/web/src/lib/api/ 2>/dev/null` returns error (directory deleted)
- `npm run build -w apps/web` succeeds
- `npm run test:run -w apps/web` passes
</verification>

<success_criteria>
Zero references to `../lib/api/` remain in web hooks. Old api/ directory is deleted. Web app builds and all tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-layer-migration/03-02-SUMMARY.md`
</output>
