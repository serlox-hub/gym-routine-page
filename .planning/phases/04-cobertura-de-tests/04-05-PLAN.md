---
phase: 04-cobertura-de-tests
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/shared/src/api/exerciseApi.test.js
  - packages/shared/src/api/bodyWeightApi.test.js
  - packages/shared/src/api/bodyMeasurementsApi.test.js
  - packages/shared/src/api/preferencesApi.test.js
  - packages/shared/src/api/adminApi.test.js
  - packages/shared/src/api/routineApi.test.js
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "exerciseApi functions (including Promise.all patterns) work correctly"
    - "All smaller API modules (bodyWeight, bodyMeasurements, preferences, admin) have test coverage"
    - "duplicateRoutine has test coverage (added to routineApi.test.js)"
    - "Existing routineApi.test.js tests (exportRoutine, importRoutine) still pass"
  artifacts:
    - path: "packages/shared/src/api/exerciseApi.test.js"
      provides: "Tests for all 8 exerciseApi functions"
      min_lines: 120
    - path: "packages/shared/src/api/bodyWeightApi.test.js"
      provides: "Tests for bodyWeightApi functions"
      min_lines: 40
    - path: "packages/shared/src/api/bodyMeasurementsApi.test.js"
      provides: "Tests for bodyMeasurementsApi functions"
      min_lines: 40
    - path: "packages/shared/src/api/preferencesApi.test.js"
      provides: "Tests for preferencesApi functions"
      min_lines: 20
    - path: "packages/shared/src/api/adminApi.test.js"
      provides: "Tests for adminApi functions"
      min_lines: 40
  key_links:
    - from: "packages/shared/src/api/exerciseApi.test.js"
      to: "packages/shared/src/api/_testUtils.js"
      via: "import { makeQueryMock, makeClientMock }"
      pattern: "import.*_testUtils"
---

<objective>
Write tests for exerciseApi (the most complex remaining module with Promise.all patterns), all smaller API modules, and add duplicateRoutine test to routineApi.test.js.

Purpose: Complete TEST-01 coverage across all API modules. exerciseApi needs special attention for Promise.all with multiple from() calls (Pitfall 6 from research). Smaller APIs are straightforward CRUD.
Output: Test files for exerciseApi, bodyWeightApi, bodyMeasurementsApi, preferencesApi, adminApi, plus extended routineApi.test.js.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cobertura-de-tests/04-CONTEXT.md
@.planning/phases/04-cobertura-de-tests/04-RESEARCH.md
@.planning/phases/04-cobertura-de-tests/04-01-SUMMARY.md

@packages/shared/src/api/_testUtils.js
@packages/shared/src/api/exerciseApi.js
@packages/shared/src/api/bodyWeightApi.js
@packages/shared/src/api/bodyMeasurementsApi.js
@packages/shared/src/api/preferencesApi.js
@packages/shared/src/api/adminApi.js
@packages/shared/src/api/routineIOApi.js
@packages/shared/src/api/routineApi.test.js

<interfaces>
<!-- exerciseApi.js (193 lines, 8 functions): -->
<!-- fetchExercisesWithMuscleGroup, fetchMuscleGroups, fetchExerciseStats (Promise.all x2), -->
<!-- fetchExerciseUsageDetail (Promise.all x2), fetchExercise, createExercise, updateExercise, deleteExercise (conditional soft delete) -->

<!-- PITFALL 6: fetchExerciseStats calls getClient().from('session_exercises') AND getClient().from('routine_exercises') in Promise.all -->
<!-- Solution: Use makeClientMock with per-table responses — it discriminates via from(table) -->

<!-- bodyWeightApi.js (69 lines): CRUD for body weight entries -->
<!-- bodyMeasurementsApi.js (58 lines): CRUD for body measurements -->
<!-- preferencesApi.js (23 lines): Read/update preferences -->
<!-- adminApi.js (63 lines): Admin operations -->

<!-- routineIOApi.js (313 lines, 3 functions): exportRoutine, importRoutine (already tested), duplicateRoutine (NOT tested) -->
<!-- Add duplicateRoutine test to existing routineApi.test.js per research recommendation -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test exerciseApi with Promise.all patterns</name>
  <files>packages/shared/src/api/exerciseApi.test.js</files>
  <action>
Create `packages/shared/src/api/exerciseApi.test.js` — 8 functions:

- fetchExercisesWithMuscleGroup: returns exercises with muscle group join, throws on error
- fetchMuscleGroups: returns muscle groups array, throws on error
- fetchExerciseStats: **CRITICAL — uses Promise.all with TWO getClient().from() calls** (session_exercises and routine_exercises). Use `makeClientMock` which discriminates by table name. Test: returns stats, throws if either query fails
- fetchExerciseUsageDetail: Similar Promise.all pattern — test both success paths
- fetchExercise: returns single exercise, throws on error
- createExercise: inserts and returns, throws on error
- updateExercise: updates and returns, throws on error
- deleteExercise: conditional soft delete (updates deleted_at) — test soft delete path, test error

For fetchExerciseStats and fetchExerciseUsageDetail, use:
```js
getClient.mockReturnValue(makeClientMock({
  session_exercises: { data: [...], error: null },
  routine_exercises: { data: [...], error: null },
}))
```
This works because makeClientMock's from() discriminates by table name.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose 2>&1 | grep -E "(exerciseApi|PASS|FAIL)" | head -20</automated>
  </verify>
  <done>exerciseApi.test.js covers 8 functions including Promise.all patterns. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Test smaller APIs and add duplicateRoutine test</name>
  <files>packages/shared/src/api/bodyWeightApi.test.js, packages/shared/src/api/bodyMeasurementsApi.test.js, packages/shared/src/api/preferencesApi.test.js, packages/shared/src/api/adminApi.test.js, packages/shared/src/api/routineApi.test.js</files>
  <action>
**bodyWeightApi.test.js:** Read bodyWeightApi.js to identify all exported functions. For each: happy path + error test. These are simple CRUD — keep tests concise.

**bodyMeasurementsApi.test.js:** Same approach. Read source, test all exports.

**preferencesApi.test.js:** Very small (23 lines). Test read and update preference functions.

**adminApi.test.js:** Read adminApi.js to identify exports. Test each with happy path + error.

**routineApi.test.js (EXTEND, not rewrite):** Add a new describe block for `duplicateRoutine` at the end of the existing file. The existing 2 tests (exportRoutine, importRoutine) must remain UNTOUCHED. Add:
- duplicateRoutine: success case — creates a copy of routine with "(Copia)" suffix
- duplicateRoutine: throws on error

The existing routineApi.test.js uses its own local makeQueryMock. The new duplicateRoutine test can use the same local pattern OR import from _testUtils.js — whichever integrates more cleanly with the existing file. If importing from _testUtils.js, ensure the existing tests still work (they don't use that import).

IMPORTANT: Read routineIOApi.js to understand duplicateRoutine's implementation before writing the test. It likely calls multiple from() operations (fetch original + insert copy).
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>bodyWeightApi, bodyMeasurementsApi, preferencesApi, adminApi all have test files. duplicateRoutine test added to routineApi.test.js. All existing tests (exportRoutine, importRoutine) still pass. `npm run test:shared` all green.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared -- --reporter=verbose
```
All API test files green. Complete TEST-01 coverage achieved.
</verification>

<success_criteria>
- exerciseApi.test.js handles Promise.all patterns correctly
- All 4 smaller API modules have test files
- duplicateRoutine test added to routineApi.test.js without breaking existing tests
- Complete TEST-01 API coverage: all modules have tests
- `npm run test:shared` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-cobertura-de-tests/04-05-SUMMARY.md`
</output>
