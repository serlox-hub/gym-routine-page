---
phase: 02-deduplicar
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/hooks/useWorkoutHistory.js
  - packages/shared/src/index.js
  - apps/web/src/hooks/useWorkoutHistory.js
  - apps/gym-native/src/hooks/useWorkoutHistory.js
  - apps/gym-native/src/components/Workout/ExerciseHistoryModal.jsx
  - apps/gym-native/src/screens/ExerciseProgressScreen.jsx
autonomous: true
requirements: [DUP-02]

must_haves:
  truths:
    - "useWorkoutHistory, useSessionDetail, usePreviousWorkout, useDeleteSession, useExerciseHistory, useExerciseHistorySummary all live in packages/shared"
    - "useExerciseHistory uses useInfiniteQuery in shared (not useQuery)"
    - "RN consumers use data.pages.flat() for useExerciseHistory results"
    - "Web consumers are unchanged (already use useInfiniteQuery shape)"
    - "npm run check passes"
  artifacts:
    - path: "packages/shared/src/hooks/useWorkoutHistory.js"
      provides: "All 6 workout history hooks"
      contains: "useInfiniteQuery"
    - path: "apps/web/src/hooks/useWorkoutHistory.js"
      provides: "Re-export from @gym/shared"
    - path: "apps/gym-native/src/hooks/useWorkoutHistory.js"
      provides: "Re-export from @gym/shared"
  key_links:
    - from: "apps/gym-native/src/components/Workout/ExerciseHistoryModal.jsx"
      to: "packages/shared/src/hooks/useWorkoutHistory.js"
      via: "useExerciseHistory returns infinite shape"
      pattern: "data\\?\\.pages\\.flat\\(\\)"
    - from: "apps/gym-native/src/screens/ExerciseProgressScreen.jsx"
      to: "packages/shared/src/hooks/useWorkoutHistory.js"
      via: "useExerciseHistory returns infinite shape"
      pattern: "data\\?\\.pages\\.flat\\(\\)"
---

<objective>
Move all 6 useWorkoutHistory hooks to packages/shared, migrating useExerciseHistory from useQuery to useInfiniteQuery (matching web). Update RN consumers to handle the pages array shape.

Purpose: Eliminate ~250 lines of duplication and unify the pagination strategy across platforms.
Output: Single shared hook file, RN consumers adapted for infinite query shape, npm run check green.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deduplicar/02-CONTEXT.md
@.planning/phases/02-deduplicar/02-RESEARCH.md

@apps/web/src/hooks/useWorkoutHistory.js
@apps/gym-native/src/hooks/useWorkoutHistory.js
@apps/gym-native/src/components/Workout/ExerciseHistoryModal.jsx
@apps/gym-native/src/screens/ExerciseProgressScreen.jsx
@packages/shared/src/index.js
@packages/shared/src/hooks/useRestTimer.js

<interfaces>
<!-- From apps/web/src/hooks/useWorkoutHistory.js — 6 exports -->
export function useWorkoutHistory(currentDate) { ... }      // useQuery — identical
export function useSessionDetail(sessionId) { ... }          // useQuery — identical
export function useExerciseHistorySummary(exerciseId, routineDayId = null) { ... }  // useQuery — web-only (RN never had it)
export function useExerciseHistory(exerciseId, routineDayId = null) { ... }          // useInfiniteQuery in web, useQuery in RN — DIVERGES
export function usePreviousWorkout(exerciseId) { ... }       // useQuery — identical
export function useDeleteSession() { ... }                    // useMutation — identical

<!-- From apps/gym-native/src/hooks/useWorkoutHistory.js — 5 exports (no useExerciseHistorySummary) -->
// Same as web minus useExerciseHistorySummary
// useExerciseHistory uses useQuery instead of useInfiniteQuery

<!-- RN consumers that need updating for infinite shape: -->
// apps/gym-native/src/components/Workout/ExerciseHistoryModal.jsx
//   - uses useExerciseHistory, reads `data` as flat array
//   - needs: const sessions = data?.pages.flat() ?? []
//   - also needs fetchNextPage, hasNextPage for infinite scroll

// apps/gym-native/src/screens/ExerciseProgressScreen.jsx
//   - uses useExerciseHistory, passes sessions directly
//   - needs: data?.pages.flat() ?? [] adapter

<!-- useWorkout.js re-export chain (stays intact): -->
// apps/*/src/hooks/useWorkout.js: export * from './useWorkoutHistory.js'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared useWorkoutHistory.js and replace per-app files</name>
  <files>
    packages/shared/src/hooks/useWorkoutHistory.js,
    packages/shared/src/index.js,
    apps/web/src/hooks/useWorkoutHistory.js,
    apps/gym-native/src/hooks/useWorkoutHistory.js
  </files>
  <action>
1. Read both `apps/web/src/hooks/useWorkoutHistory.js` and `apps/gym-native/src/hooks/useWorkoutHistory.js` fully.

2. Create `packages/shared/src/hooks/useWorkoutHistory.js`:
   - Copy the **web** version as base (it already has useInfiniteQuery for useExerciseHistory).
   - All 6 functions go into shared: `useWorkoutHistory`, `useSessionDetail`, `useExerciseHistorySummary`, `useExerciseHistory`, `usePreviousWorkout`, `useDeleteSession`.
   - Verify all imports resolve to shared packages (workoutApi functions, QUERY_KEYS, queryClient — these should already be available in shared).
   - No platform APIs should be present (these hooks are pure data hooks — verify).

3. Update `packages/shared/src/index.js` — add:
   ```js
   export * from './hooks/useWorkoutHistory.js'
   ```

4. **Replace** `apps/web/src/hooks/useWorkoutHistory.js` with a single-line re-export:
   ```js
   export * from '@gym/shared'
   ```
   Wait — this would re-export everything from shared. Instead, be specific:
   ```js
   export { useWorkoutHistory, useSessionDetail, useExerciseHistorySummary, useExerciseHistory, usePreviousWorkout, useDeleteSession } from '@gym/shared'
   ```
   OR if the hook names are unique enough to not conflict with other shared exports, a simple `export * from '@gym/shared'` filtered by what the file originally exported. Check if `useWorkout.js` does `export * from './useWorkoutHistory.js'` — if so, re-exporting too much from shared here would leak unrelated exports into useWorkout. **Safest: use named re-exports.**

5. **Replace** `apps/gym-native/src/hooks/useWorkoutHistory.js` with named re-exports from `@gym/shared`. Include `useExerciseHistorySummary` too — it's now available from shared even though RN never used it. No harm in exporting it.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run test:shared</automated>
  </verify>
  <done>All 6 hooks live in shared. Both per-app files are pure re-exports. useExerciseHistory uses useInfiniteQuery. test:shared passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update RN consumers for useInfiniteQuery data shape</name>
  <files>
    apps/gym-native/src/components/Workout/ExerciseHistoryModal.jsx,
    apps/gym-native/src/screens/ExerciseProgressScreen.jsx
  </files>
  <action>
1. Read `apps/gym-native/src/components/Workout/ExerciseHistoryModal.jsx` fully. Find where `useExerciseHistory` is called and how `data` is consumed.

2. Update the hook call site:
   - Before: `const { data: sessions, isLoading } = useExerciseHistory(exerciseId, routineDayId)`
   - After: `const { data, isLoading, fetchNextPage, hasNextPage, isFetchingNextPage } = useExerciseHistory(exerciseId, routineDayId)`
   - Add: `const sessions = data?.pages.flat() ?? []`
   - The rest of the component that uses `sessions` should work unchanged since it expects a flat array.

3. If the modal uses a FlatList or ScrollView for the history list, add infinite scroll support:
   - FlatList: add `onEndReached={() => hasNextPage && fetchNextPage()}` and `onEndReachedThreshold={0.5}`
   - If it's a ScrollView with manual rendering, add a "Cargar mas" button at the bottom when `hasNextPage` is true.
   - If `isFetchingNextPage`, show a small loading indicator at the bottom.

4. Read `apps/gym-native/src/screens/ExerciseProgressScreen.jsx` fully. Find the `useExerciseHistory` call.

5. Apply the same adapter: `const sessions = data?.pages.flat() ?? []`. This screen likely just displays stats/chart from the sessions — it may not need infinite scroll UI (just fetch all pages it has). Check if it uses `data` directly or if it's consumed by a child. Apply the `pages.flat()` adapter at the point of consumption.

6. Search for any other RN files importing `useExerciseHistory`:
   ```
   grep -rn "useExerciseHistory" apps/gym-native/src/
   ```
   Update any additional consumers found.

IMPORTANT: Do NOT add `useExerciseHistorySummary` to RN components — that's a feature addition, out of scope. Only adapt existing `useExerciseHistory` consumers for the new data shape.
  </action>
  <verify>
    <automated>cd /Users/sergio/MyProjects/gym-routine-page && npm run check</automated>
  </verify>
  <done>All RN consumers of useExerciseHistory use data.pages.flat() for flat array access. ExerciseHistoryModal has infinite scroll support (onEndReached or load-more button). npm run check passes.</done>
</task>

</tasks>

<verification>
- `npm run check` passes (lint + test + build for all workspaces)
- `grep -rn "useQuery" packages/shared/src/hooks/useWorkoutHistory.js` does NOT match standalone `useQuery` in useExerciseHistory (only `useInfiniteQuery`)
- `grep -rn "pages.flat" apps/gym-native/src/` shows all RN consumers adapted
- Per-app useWorkoutHistory.js files are pure re-exports (<5 lines each)
</verification>

<success_criteria>
- All 6 hooks exist in packages/shared/src/hooks/useWorkoutHistory.js
- useExerciseHistory uses useInfiniteQuery with initialPageParam and getNextPageParam
- Per-app files are named re-exports only
- RN ExerciseHistoryModal uses data.pages.flat() and supports infinite scroll
- RN ExerciseProgressScreen uses data.pages.flat()
- useWorkout.js re-export chain works in both apps
- npm run check green
</success_criteria>

<output>
After completion, create `.planning/phases/02-deduplicar/02-03-SUMMARY.md`
</output>
