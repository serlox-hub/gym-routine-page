---
phase: 03-api-layer-migration
plan: 03
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - apps/gym-native/src/hooks/useRoutines.js
  - apps/gym-native/src/hooks/useExercises.js
  - apps/gym-native/src/hooks/useBodyWeight.js
  - apps/gym-native/src/hooks/useBodyMeasurements.js
  - apps/gym-native/src/hooks/usePreferences.js
  - apps/gym-native/src/hooks/useAdmin.js
  - apps/gym-native/src/hooks/useWorkoutHistory.js
  - apps/gym-native/src/hooks/useAuth.js
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "RN hooks for routines, exercises, body weight, body measurements, preferences, admin, and workout history call shared API functions instead of inline Supabase queries"
    - "No import { supabase } remains in any of these 8 RN hook files"
    - "RN app data operations produce identical results to pre-migration behavior"
  artifacts:
    - path: "apps/gym-native/src/hooks/useRoutines.js"
      provides: "RN routine hooks using shared API"
      contains: "from '@gym/shared'"
    - path: "apps/gym-native/src/hooks/useExercises.js"
      provides: "RN exercise hooks using shared API"
      contains: "from '@gym/shared'"
    - path: "apps/gym-native/src/hooks/useWorkoutHistory.js"
      provides: "RN workout history hooks using shared API"
      contains: "from '@gym/shared'"
  key_links:
    - from: "apps/gym-native/src/hooks/useRoutines.js"
      to: "packages/shared/src/api/routineApi.js"
      via: "import { fetchRoutines, createRoutine, ... } from '@gym/shared'"
      pattern: "from '@gym/shared'"
    - from: "apps/gym-native/src/hooks/useExercises.js"
      to: "packages/shared/src/api/exerciseApi.js"
      via: "import { fetchExercisesWithMuscleGroup, ... } from '@gym/shared'"
      pattern: "from '@gym/shared'"
---

<objective>
Refactor 8 RN hook files (the "direct map" hooks) from inline Supabase queries to shared API function calls from `@gym/shared`.

Purpose: Eliminates inline Supabase queries from the simpler RN hooks. These hooks map directly to existing web API functions with no special logic.
Output: 8 RN hook files refactored to use shared API, zero `supabase` imports remaining in these files.
</objective>

<execution_context>
@/Users/sergio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-api-layer-migration/03-RESEARCH.md
@.planning/phases/03-api-layer-migration/03-01-SUMMARY.md

@apps/gym-native/src/hooks/useRoutines.js
@apps/gym-native/src/hooks/useExercises.js
@apps/gym-native/src/hooks/useBodyWeight.js
@apps/gym-native/src/hooks/useBodyMeasurements.js
@apps/gym-native/src/hooks/usePreferences.js
@apps/gym-native/src/hooks/useAdmin.js
@apps/gym-native/src/hooks/useWorkoutHistory.js
@apps/gym-native/src/hooks/useAuth.js

<interfaces>
<!-- Shared API functions available from @gym/shared (from plan 01): -->

<!-- exerciseApi.js exports: fetchExercisesWithMuscleGroup, fetchMuscleGroups, fetchExerciseStats, createExercise, updateExercise, softDeleteExercise, restoreExercise, etc. -->
<!-- routineApi.js exports: fetchRoutines, fetchRoutineDetail, createRoutine, updateRoutine, deleteRoutine, createDay, updateDay, deleteDay, createBlock, updateBlock, deleteBlock, addExerciseToBlock, updateRoutineExercise, deleteRoutineExercise, reorderExercisesInBlock, reorderBlocks, reorderDays, etc. -->
<!-- workoutApi.js exports: fetchWorkoutHistory, fetchSessionDetail, fetchExerciseHistory, fetchPreviousWorkout, startWorkoutSession, fetchActiveSession, fetchCompletedSetsForSession, completeWorkoutSession, deleteWorkoutSession, upsertCompletedSet, updateSetVideo, updateSetDetails, deleteCompletedSet, insertSessionExercise, deleteSessionExercise, reorderSessionExercises, fetchExerciseIdsWithSets, deleteSessionExercisesWithoutSets, etc. -->
<!-- bodyWeightApi.js exports: fetchBodyWeightEntries, createBodyWeightEntry, updateBodyWeightEntry, deleteBodyWeightEntry, etc. -->
<!-- bodyMeasurementsApi.js exports: fetchMeasurementEntries, createMeasurementEntry, updateMeasurementEntry, deleteMeasurementEntry, etc. -->
<!-- preferencesApi.js exports: fetchPreferences, upsertPreference -->
<!-- adminApi.js exports: fetchAllUsers, updateUserSetting, fetchUserSettings, etc. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor useRoutines, useExercises, useBodyWeight, useBodyMeasurements</name>
  <files>
    apps/gym-native/src/hooks/useRoutines.js
    apps/gym-native/src/hooks/useExercises.js
    apps/gym-native/src/hooks/useBodyWeight.js
    apps/gym-native/src/hooks/useBodyMeasurements.js
  </files>
  <action>
    For each of these 4 files, apply the same mechanical transformation:

    1. Remove `import { supabase } from '../lib/supabase.js'`
    2. Add `import { functionA, functionB, ... } from '@gym/shared'` with all the API functions the hook needs
    3. Replace every inline query block:
       ```js
       // BEFORE:
       queryFn: async () => {
         const { data, error } = await supabase.from('table').select(...)
         if (error) throw error
         return data
       }
       // AFTER:
       queryFn: fetchTableEntries
       ```
    4. Replace every inline mutation:
       ```js
       // BEFORE:
       mutationFn: async (input) => {
         const { data, error } = await supabase.from('table').insert(input).select().single()
         if (error) throw error
         return data
       }
       // AFTER:
       mutationFn: (input) => createTableEntry(input)
       ```

    IMPORTANT per file:
    - **useRoutines.js (610 lines):** This is the largest file. It imports `duplicateRoutine` from `../lib/routineIO.js`. If `duplicateRoutine` was moved to shared in Phase 2, update that import to `@gym/shared`. If it's still in the local file, leave that import as-is (it's not an inline Supabase query — it's a lib function). Map each mutation's inline query to the matching routineApi function. Pay attention to parameter shapes — RN mutations may pass different argument structures than web. Adapt the mutationFn wrapper to match the shared API function's expected parameters.
    - **useExercises.js (274 lines):** Maps directly to exerciseApi functions. Check if RN has any exercise queries not in web API (e.g., different select fields) — if so, note it.
    - **useBodyWeight.js (127 lines):** Maps directly to bodyWeightApi functions.
    - **useBodyMeasurements.js (114 lines):** Maps directly to bodyMeasurementsApi functions.

    After each file, verify no `supabase` import remains: `grep "supabase" {file}` should return nothing.

    NOTE: If any RN hook passes parameters in a different shape than the web API function expects, create a thin wrapper in the mutationFn that adapts the shape. Do NOT modify the shared API function signatures — those match web callers.
  </action>
  <verify>
    <automated>grep -l "from.*supabase" apps/gym-native/src/hooks/useRoutines.js apps/gym-native/src/hooks/useExercises.js apps/gym-native/src/hooks/useBodyWeight.js apps/gym-native/src/hooks/useBodyMeasurements.js 2>/dev/null || echo "PASS: no supabase imports in 4 files"</automated>
  </verify>
  <done>4 RN hook files (useRoutines, useExercises, useBodyWeight, useBodyMeasurements) refactored to use shared API. Zero supabase imports remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor usePreferences, useAdmin, useWorkoutHistory, useAuth</name>
  <files>
    apps/gym-native/src/hooks/usePreferences.js
    apps/gym-native/src/hooks/useAdmin.js
    apps/gym-native/src/hooks/useWorkoutHistory.js
    apps/gym-native/src/hooks/useAuth.js
  </files>
  <action>
    Apply the same mechanical transformation as Task 1 to these 4 files:

    1. Remove `import { supabase } from '../lib/supabase.js'`
    2. Add `import { ... } from '@gym/shared'` with needed API functions
    3. Replace inline queries/mutations with shared API function calls

    Per-file notes:
    - **usePreferences.js (86 lines):** Direct map to `fetchPreferences` and `upsertPreference`.
    - **useAdmin.js (81 lines):** Direct map to `fetchAllUsers` and `updateUserSetting`.
    - **useWorkoutHistory.js (302 lines):** Maps to `fetchWorkoutHistory`, `fetchSessionDetail`, `fetchExerciseHistory`, `fetchPreviousWorkout` from workoutApi. Note: RN's `useSessionDetail` may transform raw data (mapping `session_exercises` to flat `exercises` array). Keep that transformation in the hook's queryFn — call `fetchSessionDetail` then transform. The transformation is view-layer concern, not API concern.
    - **useAuth.js (90 lines):** Has `useUserSettings` with inline `supabase.from('user_settings').select(...)`. If plan 01 added `fetchUserSettings` to adminApi.js, use it. If not, add it now to `packages/shared/src/api/adminApi.js` as:
      ```js
      export async function fetchUserSettings(userId) {
        const { data, error } = await getClient()
          .from('user_settings').select('key, value').eq('user_id', userId)
        if (error) throw error
        return (data || []).reduce((acc, { key, value }) => { acc[key] = value; return acc }, {})
      }
      ```
      Then import and use it in both RN and web useAuth.js.
      NOTE: `useAuth.js` may also have Supabase Auth SDK calls (signIn, signOut) that live in the auth store, not this hook. Those stay as-is — they're store concerns, not API layer concerns. Only refactor data queries (user_settings table).

    After each file, verify no `supabase` import remains.
  </action>
  <verify>
    <automated>grep -l "from.*supabase" apps/gym-native/src/hooks/usePreferences.js apps/gym-native/src/hooks/useAdmin.js apps/gym-native/src/hooks/useWorkoutHistory.js apps/gym-native/src/hooks/useAuth.js 2>/dev/null || echo "PASS: no supabase imports in 4 files"</automated>
  </verify>
  <done>4 RN hook files (usePreferences, useAdmin, useWorkoutHistory, useAuth) refactored to use shared API. Zero supabase imports remain. fetchUserSettings added to shared API if needed.</done>
</task>

</tasks>

<verification>
- `grep -r "from.*supabase" apps/gym-native/src/hooks/useRoutines.js apps/gym-native/src/hooks/useExercises.js apps/gym-native/src/hooks/useBodyWeight.js apps/gym-native/src/hooks/useBodyMeasurements.js apps/gym-native/src/hooks/usePreferences.js apps/gym-native/src/hooks/useAdmin.js apps/gym-native/src/hooks/useWorkoutHistory.js apps/gym-native/src/hooks/useAuth.js` returns nothing
- `npm run test:run -w apps/web` passes (shared API functions still valid)
</verification>

<success_criteria>
8 RN hook files refactored from inline Supabase queries to shared API function calls. Zero `import { supabase }` lines remain in these files. All data operations produce identical results.
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-layer-migration/03-03-SUMMARY.md`
</output>
